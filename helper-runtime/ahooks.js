/**
 * Parcel module id: 9sqtS
 * Resolved path: ahooks.js
 * Dependencies:
 *   @parcel/transformer-js/src/esmodule-helpers.js -> cHUbl  =>  @parcel/transformer-js/src/esmodule-helpers.js
 *   react -> 329PG  =>  react-reexport.js
 *   react-dom -> f20Gy  =>  react-dom-reexport.js
 *   react/jsx-runtime -> 8iOxN  =>  react/jsx-runtime.js
 *
 * npm-backed (ahooks). Generated by scripts/replace-remaining-vendors-with-npm.mjs — do not hand-edit.
 */

var helpers = e("@parcel/transformer-js/src/esmodule-helpers.js")
helpers.defineInteropFlag(r)

var __jrReq = e

var __mod = (function () {
  var __cjsModule = { exports: {} }
  var module = __cjsModule
  var exports = __cjsModule.exports
;var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/lodash/isObject.js
var require_isObject = __commonJS({
  "node_modules/lodash/isObject.js"(exports, module2) {
    function isObject2(value) {
      var type = typeof value;
      return value != null && (type == "object" || type == "function");
    }
    module2.exports = isObject2;
  }
});

// node_modules/lodash/_freeGlobal.js
var require_freeGlobal = __commonJS({
  "node_modules/lodash/_freeGlobal.js"(exports, module2) {
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    module2.exports = freeGlobal;
  }
});

// node_modules/lodash/_root.js
var require_root = __commonJS({
  "node_modules/lodash/_root.js"(exports, module2) {
    var freeGlobal = require_freeGlobal();
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root = freeGlobal || freeSelf || Function("return this")();
    module2.exports = root;
  }
});

// node_modules/lodash/now.js
var require_now = __commonJS({
  "node_modules/lodash/now.js"(exports, module2) {
    var root = require_root();
    var now = function() {
      return root.Date.now();
    };
    module2.exports = now;
  }
});

// node_modules/lodash/_trimmedEndIndex.js
var require_trimmedEndIndex = __commonJS({
  "node_modules/lodash/_trimmedEndIndex.js"(exports, module2) {
    var reWhitespace = /\s/;
    function trimmedEndIndex(string) {
      var index2 = string.length;
      while (index2-- && reWhitespace.test(string.charAt(index2))) {
      }
      return index2;
    }
    module2.exports = trimmedEndIndex;
  }
});

// node_modules/lodash/_baseTrim.js
var require_baseTrim = __commonJS({
  "node_modules/lodash/_baseTrim.js"(exports, module2) {
    var trimmedEndIndex = require_trimmedEndIndex();
    var reTrimStart = /^\s+/;
    function baseTrim(string) {
      return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
    }
    module2.exports = baseTrim;
  }
});

// node_modules/lodash/_Symbol.js
var require_Symbol = __commonJS({
  "node_modules/lodash/_Symbol.js"(exports, module2) {
    var root = require_root();
    var Symbol2 = root.Symbol;
    module2.exports = Symbol2;
  }
});

// node_modules/lodash/_getRawTag.js
var require_getRawTag = __commonJS({
  "node_modules/lodash/_getRawTag.js"(exports, module2) {
    var Symbol2 = require_Symbol();
    var objectProto = Object.prototype;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var nativeObjectToString = objectProto.toString;
    var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
    function getRawTag(value) {
      var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
      try {
        value[symToStringTag] = void 0;
        var unmasked = true;
      } catch (e) {
      }
      var result = nativeObjectToString.call(value);
      if (unmasked) {
        if (isOwn) {
          value[symToStringTag] = tag;
        } else {
          delete value[symToStringTag];
        }
      }
      return result;
    }
    module2.exports = getRawTag;
  }
});

// node_modules/lodash/_objectToString.js
var require_objectToString = __commonJS({
  "node_modules/lodash/_objectToString.js"(exports, module2) {
    var objectProto = Object.prototype;
    var nativeObjectToString = objectProto.toString;
    function objectToString(value) {
      return nativeObjectToString.call(value);
    }
    module2.exports = objectToString;
  }
});

// node_modules/lodash/_baseGetTag.js
var require_baseGetTag = __commonJS({
  "node_modules/lodash/_baseGetTag.js"(exports, module2) {
    var Symbol2 = require_Symbol();
    var getRawTag = require_getRawTag();
    var objectToString = require_objectToString();
    var nullTag = "[object Null]";
    var undefinedTag = "[object Undefined]";
    var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
    function baseGetTag(value) {
      if (value == null) {
        return value === void 0 ? undefinedTag : nullTag;
      }
      return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
    }
    module2.exports = baseGetTag;
  }
});

// node_modules/lodash/isObjectLike.js
var require_isObjectLike = __commonJS({
  "node_modules/lodash/isObjectLike.js"(exports, module2) {
    function isObjectLike(value) {
      return value != null && typeof value == "object";
    }
    module2.exports = isObjectLike;
  }
});

// node_modules/lodash/isSymbol.js
var require_isSymbol = __commonJS({
  "node_modules/lodash/isSymbol.js"(exports, module2) {
    var baseGetTag = require_baseGetTag();
    var isObjectLike = require_isObjectLike();
    var symbolTag = "[object Symbol]";
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
    }
    module2.exports = isSymbol;
  }
});

// node_modules/lodash/toNumber.js
var require_toNumber = __commonJS({
  "node_modules/lodash/toNumber.js"(exports, module2) {
    var baseTrim = require_baseTrim();
    var isObject2 = require_isObject();
    var isSymbol = require_isSymbol();
    var NAN = 0 / 0;
    var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
    var reIsBinary = /^0b[01]+$/i;
    var reIsOctal = /^0o[0-7]+$/i;
    var freeParseInt = parseInt;
    function toNumber(value) {
      if (typeof value == "number") {
        return value;
      }
      if (isSymbol(value)) {
        return NAN;
      }
      if (isObject2(value)) {
        var other = typeof value.valueOf == "function" ? value.valueOf() : value;
        value = isObject2(other) ? other + "" : other;
      }
      if (typeof value != "string") {
        return value === 0 ? value : +value;
      }
      value = baseTrim(value);
      var isBinary = reIsBinary.test(value);
      return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
    }
    module2.exports = toNumber;
  }
});

// node_modules/lodash/debounce.js
var require_debounce = __commonJS({
  "node_modules/lodash/debounce.js"(exports, module2) {
    var isObject2 = require_isObject();
    var now = require_now();
    var toNumber = require_toNumber();
    var FUNC_ERROR_TEXT = "Expected a function";
    var nativeMax = Math.max;
    var nativeMin = Math.min;
    function debounce3(func, wait, options) {
      var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      wait = toNumber(wait) || 0;
      if (isObject2(options)) {
        leading = !!options.leading;
        maxing = "maxWait" in options;
        maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      function invokeFunc(time) {
        var args = lastArgs, thisArg = lastThis;
        lastArgs = lastThis = void 0;
        lastInvokeTime = time;
        result = func.apply(thisArg, args);
        return result;
      }
      function leadingEdge(time) {
        lastInvokeTime = time;
        timerId = setTimeout(timerExpired, wait);
        return leading ? invokeFunc(time) : result;
      }
      function remainingWait(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, timeWaiting = wait - timeSinceLastCall;
        return maxing ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
      }
      function shouldInvoke(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
        return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
      }
      function timerExpired() {
        var time = now();
        if (shouldInvoke(time)) {
          return trailingEdge(time);
        }
        timerId = setTimeout(timerExpired, remainingWait(time));
      }
      function trailingEdge(time) {
        timerId = void 0;
        if (trailing && lastArgs) {
          return invokeFunc(time);
        }
        lastArgs = lastThis = void 0;
        return result;
      }
      function cancel() {
        if (timerId !== void 0) {
          clearTimeout(timerId);
        }
        lastInvokeTime = 0;
        lastArgs = lastCallTime = lastThis = timerId = void 0;
      }
      function flush() {
        return timerId === void 0 ? result : trailingEdge(now());
      }
      function debounced() {
        var time = now(), isInvoking = shouldInvoke(time);
        lastArgs = arguments;
        lastThis = this;
        lastCallTime = time;
        if (isInvoking) {
          if (timerId === void 0) {
            return leadingEdge(lastCallTime);
          }
          if (maxing) {
            clearTimeout(timerId);
            timerId = setTimeout(timerExpired, wait);
            return invokeFunc(lastCallTime);
          }
        }
        if (timerId === void 0) {
          timerId = setTimeout(timerExpired, wait);
        }
        return result;
      }
      debounced.cancel = cancel;
      debounced.flush = flush;
      return debounced;
    }
    module2.exports = debounce3;
  }
});

// node_modules/lodash/throttle.js
var require_throttle = __commonJS({
  "node_modules/lodash/throttle.js"(exports, module2) {
    var debounce3 = require_debounce();
    var isObject2 = require_isObject();
    var FUNC_ERROR_TEXT = "Expected a function";
    function throttle4(func, wait, options) {
      var leading = true, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      if (isObject2(options)) {
        leading = "leading" in options ? !!options.leading : leading;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      return debounce3(func, wait, {
        "leading": leading,
        "maxWait": wait,
        "trailing": trailing
      });
    }
    module2.exports = throttle4;
  }
});

// node_modules/dayjs/dayjs.min.js
var require_dayjs_min = __commonJS({
  "node_modules/dayjs/dayjs.min.js"(exports, module2) {
    !(function(t, e) {
      "object" == typeof exports && "undefined" != typeof module2 ? module2.exports = e() : "function" == typeof define && define.amd ? define(e) : (t = "undefined" != typeof globalThis ? globalThis : t || self).dayjs = e();
    })(exports, (function() {
      "use strict";
      var t = 1e3, e = 6e4, n = 36e5, r = "millisecond", i = "second", s = "minute", u = "hour", a = "day", o = "week", c = "month", f = "quarter", h = "year", d = "date", l = "Invalid Date", $ = /^(\d{4})[-/]?(\d{1,2})?[-/]?(\d{0,2})[Tt\s]*(\d{1,2})?:?(\d{1,2})?:?(\d{1,2})?[.:]?(\d+)?$/, y = /\[([^\]]+)]|YYYY|YY|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, M = { name: "en", weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_"), ordinal: function(t2) {
        var e2 = ["th", "st", "nd", "rd"], n2 = t2 % 100;
        return "[" + t2 + (e2[(n2 - 20) % 10] || e2[n2] || e2[0]) + "]";
      } }, m = function(t2, e2, n2) {
        var r2 = String(t2);
        return !r2 || r2.length >= e2 ? t2 : "" + Array(e2 + 1 - r2.length).join(n2) + t2;
      }, v = { s: m, z: function(t2) {
        var e2 = -t2.utcOffset(), n2 = Math.abs(e2), r2 = Math.floor(n2 / 60), i2 = n2 % 60;
        return (e2 <= 0 ? "+" : "-") + m(r2, 2, "0") + ":" + m(i2, 2, "0");
      }, m: function t2(e2, n2) {
        if (e2.date() < n2.date()) return -t2(n2, e2);
        var r2 = 12 * (n2.year() - e2.year()) + (n2.month() - e2.month()), i2 = e2.clone().add(r2, c), s2 = n2 - i2 < 0, u2 = e2.clone().add(r2 + (s2 ? -1 : 1), c);
        return +(-(r2 + (n2 - i2) / (s2 ? i2 - u2 : u2 - i2)) || 0);
      }, a: function(t2) {
        return t2 < 0 ? Math.ceil(t2) || 0 : Math.floor(t2);
      }, p: function(t2) {
        return { M: c, y: h, w: o, d: a, D: d, h: u, m: s, s: i, ms: r, Q: f }[t2] || String(t2 || "").toLowerCase().replace(/s$/, "");
      }, u: function(t2) {
        return void 0 === t2;
      } }, g = "en", D = {};
      D[g] = M;
      var p = "$isDayjsObject", S = function(t2) {
        return t2 instanceof _ || !(!t2 || !t2[p]);
      }, w = function t2(e2, n2, r2) {
        var i2;
        if (!e2) return g;
        if ("string" == typeof e2) {
          var s2 = e2.toLowerCase();
          D[s2] && (i2 = s2), n2 && (D[s2] = n2, i2 = s2);
          var u2 = e2.split("-");
          if (!i2 && u2.length > 1) return t2(u2[0]);
        } else {
          var a2 = e2.name;
          D[a2] = e2, i2 = a2;
        }
        return !r2 && i2 && (g = i2), i2 || !r2 && g;
      }, O = function(t2, e2) {
        if (S(t2)) return t2.clone();
        var n2 = "object" == typeof e2 ? e2 : {};
        return n2.date = t2, n2.args = arguments, new _(n2);
      }, b = v;
      b.l = w, b.i = S, b.w = function(t2, e2) {
        return O(t2, { locale: e2.$L, utc: e2.$u, x: e2.$x, $offset: e2.$offset });
      };
      var _ = (function() {
        function M2(t2) {
          this.$L = w(t2.locale, null, true), this.parse(t2), this.$x = this.$x || t2.x || {}, this[p] = true;
        }
        var m2 = M2.prototype;
        return m2.parse = function(t2) {
          this.$d = (function(t3) {
            var e2 = t3.date, n2 = t3.utc;
            if (null === e2) return /* @__PURE__ */ new Date(NaN);
            if (b.u(e2)) return /* @__PURE__ */ new Date();
            if (e2 instanceof Date) return new Date(e2);
            if ("string" == typeof e2 && !/Z$/i.test(e2)) {
              var r2 = e2.match($);
              if (r2) {
                var i2 = r2[2] - 1 || 0, s2 = (r2[7] || "0").substring(0, 3);
                return n2 ? new Date(Date.UTC(r2[1], i2, r2[3] || 1, r2[4] || 0, r2[5] || 0, r2[6] || 0, s2)) : new Date(r2[1], i2, r2[3] || 1, r2[4] || 0, r2[5] || 0, r2[6] || 0, s2);
              }
            }
            return new Date(e2);
          })(t2), this.init();
        }, m2.init = function() {
          var t2 = this.$d;
          this.$y = t2.getFullYear(), this.$M = t2.getMonth(), this.$D = t2.getDate(), this.$W = t2.getDay(), this.$H = t2.getHours(), this.$m = t2.getMinutes(), this.$s = t2.getSeconds(), this.$ms = t2.getMilliseconds();
        }, m2.$utils = function() {
          return b;
        }, m2.isValid = function() {
          return !(this.$d.toString() === l);
        }, m2.isSame = function(t2, e2) {
          var n2 = O(t2);
          return this.startOf(e2) <= n2 && n2 <= this.endOf(e2);
        }, m2.isAfter = function(t2, e2) {
          return O(t2) < this.startOf(e2);
        }, m2.isBefore = function(t2, e2) {
          return this.endOf(e2) < O(t2);
        }, m2.$g = function(t2, e2, n2) {
          return b.u(t2) ? this[e2] : this.set(n2, t2);
        }, m2.unix = function() {
          return Math.floor(this.valueOf() / 1e3);
        }, m2.valueOf = function() {
          return this.$d.getTime();
        }, m2.startOf = function(t2, e2) {
          var n2 = this, r2 = !!b.u(e2) || e2, f2 = b.p(t2), l2 = function(t3, e3) {
            var i2 = b.w(n2.$u ? Date.UTC(n2.$y, e3, t3) : new Date(n2.$y, e3, t3), n2);
            return r2 ? i2 : i2.endOf(a);
          }, $2 = function(t3, e3) {
            return b.w(n2.toDate()[t3].apply(n2.toDate("s"), (r2 ? [0, 0, 0, 0] : [23, 59, 59, 999]).slice(e3)), n2);
          }, y2 = this.$W, M3 = this.$M, m3 = this.$D, v2 = "set" + (this.$u ? "UTC" : "");
          switch (f2) {
            case h:
              return r2 ? l2(1, 0) : l2(31, 11);
            case c:
              return r2 ? l2(1, M3) : l2(0, M3 + 1);
            case o:
              var g2 = this.$locale().weekStart || 0, D2 = (y2 < g2 ? y2 + 7 : y2) - g2;
              return l2(r2 ? m3 - D2 : m3 + (6 - D2), M3);
            case a:
            case d:
              return $2(v2 + "Hours", 0);
            case u:
              return $2(v2 + "Minutes", 1);
            case s:
              return $2(v2 + "Seconds", 2);
            case i:
              return $2(v2 + "Milliseconds", 3);
            default:
              return this.clone();
          }
        }, m2.endOf = function(t2) {
          return this.startOf(t2, false);
        }, m2.$set = function(t2, e2) {
          var n2, o2 = b.p(t2), f2 = "set" + (this.$u ? "UTC" : ""), l2 = (n2 = {}, n2[a] = f2 + "Date", n2[d] = f2 + "Date", n2[c] = f2 + "Month", n2[h] = f2 + "FullYear", n2[u] = f2 + "Hours", n2[s] = f2 + "Minutes", n2[i] = f2 + "Seconds", n2[r] = f2 + "Milliseconds", n2)[o2], $2 = o2 === a ? this.$D + (e2 - this.$W) : e2;
          if (o2 === c || o2 === h) {
            var y2 = this.clone().set(d, 1);
            y2.$d[l2]($2), y2.init(), this.$d = y2.set(d, Math.min(this.$D, y2.daysInMonth())).$d;
          } else l2 && this.$d[l2]($2);
          return this.init(), this;
        }, m2.set = function(t2, e2) {
          return this.clone().$set(t2, e2);
        }, m2.get = function(t2) {
          return this[b.p(t2)]();
        }, m2.add = function(r2, f2) {
          var d2, l2 = this;
          r2 = Number(r2);
          var $2 = b.p(f2), y2 = function(t2) {
            var e2 = O(l2);
            return b.w(e2.date(e2.date() + Math.round(t2 * r2)), l2);
          };
          if ($2 === c) return this.set(c, this.$M + r2);
          if ($2 === h) return this.set(h, this.$y + r2);
          if ($2 === a) return y2(1);
          if ($2 === o) return y2(7);
          var M3 = (d2 = {}, d2[s] = e, d2[u] = n, d2[i] = t, d2)[$2] || 1, m3 = this.$d.getTime() + r2 * M3;
          return b.w(m3, this);
        }, m2.subtract = function(t2, e2) {
          return this.add(-1 * t2, e2);
        }, m2.format = function(t2) {
          var e2 = this, n2 = this.$locale();
          if (!this.isValid()) return n2.invalidDate || l;
          var r2 = t2 || "YYYY-MM-DDTHH:mm:ssZ", i2 = b.z(this), s2 = this.$H, u2 = this.$m, a2 = this.$M, o2 = n2.weekdays, c2 = n2.months, f2 = n2.meridiem, h2 = function(t3, n3, i3, s3) {
            return t3 && (t3[n3] || t3(e2, r2)) || i3[n3].slice(0, s3);
          }, d2 = function(t3) {
            return b.s(s2 % 12 || 12, t3, "0");
          }, $2 = f2 || function(t3, e3, n3) {
            var r3 = t3 < 12 ? "AM" : "PM";
            return n3 ? r3.toLowerCase() : r3;
          };
          return r2.replace(y, (function(t3, r3) {
            return r3 || (function(t4) {
              switch (t4) {
                case "YY":
                  return String(e2.$y).slice(-2);
                case "YYYY":
                  return b.s(e2.$y, 4, "0");
                case "M":
                  return a2 + 1;
                case "MM":
                  return b.s(a2 + 1, 2, "0");
                case "MMM":
                  return h2(n2.monthsShort, a2, c2, 3);
                case "MMMM":
                  return h2(c2, a2);
                case "D":
                  return e2.$D;
                case "DD":
                  return b.s(e2.$D, 2, "0");
                case "d":
                  return String(e2.$W);
                case "dd":
                  return h2(n2.weekdaysMin, e2.$W, o2, 2);
                case "ddd":
                  return h2(n2.weekdaysShort, e2.$W, o2, 3);
                case "dddd":
                  return o2[e2.$W];
                case "H":
                  return String(s2);
                case "HH":
                  return b.s(s2, 2, "0");
                case "h":
                  return d2(1);
                case "hh":
                  return d2(2);
                case "a":
                  return $2(s2, u2, true);
                case "A":
                  return $2(s2, u2, false);
                case "m":
                  return String(u2);
                case "mm":
                  return b.s(u2, 2, "0");
                case "s":
                  return String(e2.$s);
                case "ss":
                  return b.s(e2.$s, 2, "0");
                case "SSS":
                  return b.s(e2.$ms, 3, "0");
                case "Z":
                  return i2;
              }
              return null;
            })(t3) || i2.replace(":", "");
          }));
        }, m2.utcOffset = function() {
          return 15 * -Math.round(this.$d.getTimezoneOffset() / 15);
        }, m2.diff = function(r2, d2, l2) {
          var $2, y2 = this, M3 = b.p(d2), m3 = O(r2), v2 = (m3.utcOffset() - this.utcOffset()) * e, g2 = this - m3, D2 = function() {
            return b.m(y2, m3);
          };
          switch (M3) {
            case h:
              $2 = D2() / 12;
              break;
            case c:
              $2 = D2();
              break;
            case f:
              $2 = D2() / 3;
              break;
            case o:
              $2 = (g2 - v2) / 6048e5;
              break;
            case a:
              $2 = (g2 - v2) / 864e5;
              break;
            case u:
              $2 = g2 / n;
              break;
            case s:
              $2 = g2 / e;
              break;
            case i:
              $2 = g2 / t;
              break;
            default:
              $2 = g2;
          }
          return l2 ? $2 : b.a($2);
        }, m2.daysInMonth = function() {
          return this.endOf(c).$D;
        }, m2.$locale = function() {
          return D[this.$L];
        }, m2.locale = function(t2, e2) {
          if (!t2) return this.$L;
          var n2 = this.clone(), r2 = w(t2, e2, true);
          return r2 && (n2.$L = r2), n2;
        }, m2.clone = function() {
          return b.w(this.$d, this);
        }, m2.toDate = function() {
          return new Date(this.valueOf());
        }, m2.toJSON = function() {
          return this.isValid() ? this.toISOString() : null;
        }, m2.toISOString = function() {
          return this.$d.toISOString();
        }, m2.toString = function() {
          return this.$d.toUTCString();
        }, M2;
      })(), Y = _.prototype;
      return O.prototype = Y, [["$ms", r], ["$s", i], ["$m", s], ["$H", u], ["$W", a], ["$M", c], ["$y", h], ["$D", d]].forEach((function(t2) {
        Y[t2[1]] = function(e2) {
          return this.$g(e2, t2[0], t2[1]);
        };
      })), O.extend = function(t2, e2) {
        return t2.$i || (t2(e2, _, O), t2.$i = true), O;
      }, O.locale = w, O.isDayjs = S, O.unix = function(t2) {
        return O(1e3 * t2);
      }, O.en = D[g], O.Ls = D, O.p = {}, O;
    }));
  }
});

// node_modules/react-fast-compare/index.js
var require_react_fast_compare = __commonJS({
  "node_modules/react-fast-compare/index.js"(exports, module2) {
    var hasElementType = typeof Element !== "undefined";
    var hasMap = typeof Map === "function";
    var hasSet = typeof Set === "function";
    var hasArrayBuffer = typeof ArrayBuffer === "function" && !!ArrayBuffer.isView;
    function equal(a, b) {
      if (a === b) return true;
      if (a && b && typeof a == "object" && typeof b == "object") {
        if (a.constructor !== b.constructor) return false;
        var length, i, keys;
        if (Array.isArray(a)) {
          length = a.length;
          if (length != b.length) return false;
          for (i = length; i-- !== 0; )
            if (!equal(a[i], b[i])) return false;
          return true;
        }
        var it;
        if (hasMap && a instanceof Map && b instanceof Map) {
          if (a.size !== b.size) return false;
          it = a.entries();
          while (!(i = it.next()).done)
            if (!b.has(i.value[0])) return false;
          it = a.entries();
          while (!(i = it.next()).done)
            if (!equal(i.value[1], b.get(i.value[0]))) return false;
          return true;
        }
        if (hasSet && a instanceof Set && b instanceof Set) {
          if (a.size !== b.size) return false;
          it = a.entries();
          while (!(i = it.next()).done)
            if (!b.has(i.value[0])) return false;
          return true;
        }
        if (hasArrayBuffer && ArrayBuffer.isView(a) && ArrayBuffer.isView(b)) {
          length = a.length;
          if (length != b.length) return false;
          for (i = length; i-- !== 0; )
            if (a[i] !== b[i]) return false;
          return true;
        }
        if (a.constructor === RegExp) return a.source === b.source && a.flags === b.flags;
        if (a.valueOf !== Object.prototype.valueOf && typeof a.valueOf === "function" && typeof b.valueOf === "function") return a.valueOf() === b.valueOf();
        if (a.toString !== Object.prototype.toString && typeof a.toString === "function" && typeof b.toString === "function") return a.toString() === b.toString();
        keys = Object.keys(a);
        length = keys.length;
        if (length !== Object.keys(b).length) return false;
        for (i = length; i-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(b, keys[i])) return false;
        if (hasElementType && a instanceof Element) return false;
        for (i = length; i-- !== 0; ) {
          if ((keys[i] === "_owner" || keys[i] === "__v" || keys[i] === "__o") && a.$$typeof) {
            continue;
          }
          if (!equal(a[keys[i]], b[keys[i]])) return false;
        }
        return true;
      }
      return a !== a && b !== b;
    }
    module2.exports = function isEqual2(a, b) {
      try {
        return equal(a, b);
      } catch (error) {
        if ((error.message || "").match(/stack|recursion/i)) {
          console.warn("react-fast-compare cannot handle circular refs");
          return false;
        }
        throw error;
      }
    };
  }
});

// node_modules/screenfull/dist/screenfull.js
var require_screenfull = __commonJS({
  "node_modules/screenfull/dist/screenfull.js"(exports, module2) {
    (function() {
      "use strict";
      var document2 = typeof window !== "undefined" && typeof window.document !== "undefined" ? window.document : {};
      var isCommonjs = typeof module2 !== "undefined" && module2.exports;
      var fn = (function() {
        var val;
        var fnMap = [
          [
            "requestFullscreen",
            "exitFullscreen",
            "fullscreenElement",
            "fullscreenEnabled",
            "fullscreenchange",
            "fullscreenerror"
          ],
          // New WebKit
          [
            "webkitRequestFullscreen",
            "webkitExitFullscreen",
            "webkitFullscreenElement",
            "webkitFullscreenEnabled",
            "webkitfullscreenchange",
            "webkitfullscreenerror"
          ],
          // Old WebKit
          [
            "webkitRequestFullScreen",
            "webkitCancelFullScreen",
            "webkitCurrentFullScreenElement",
            "webkitCancelFullScreen",
            "webkitfullscreenchange",
            "webkitfullscreenerror"
          ],
          [
            "mozRequestFullScreen",
            "mozCancelFullScreen",
            "mozFullScreenElement",
            "mozFullScreenEnabled",
            "mozfullscreenchange",
            "mozfullscreenerror"
          ],
          [
            "msRequestFullscreen",
            "msExitFullscreen",
            "msFullscreenElement",
            "msFullscreenEnabled",
            "MSFullscreenChange",
            "MSFullscreenError"
          ]
        ];
        var i = 0;
        var l = fnMap.length;
        var ret = {};
        for (; i < l; i++) {
          val = fnMap[i];
          if (val && val[1] in document2) {
            for (i = 0; i < val.length; i++) {
              ret[fnMap[0][i]] = val[i];
            }
            return ret;
          }
        }
        return false;
      })();
      var eventNameMap = {
        change: fn.fullscreenchange,
        error: fn.fullscreenerror
      };
      var screenfull2 = {
        request: function(element, options) {
          return new Promise(function(resolve, reject) {
            var onFullScreenEntered = function() {
              this.off("change", onFullScreenEntered);
              resolve();
            }.bind(this);
            this.on("change", onFullScreenEntered);
            element = element || document2.documentElement;
            var returnPromise = element[fn.requestFullscreen](options);
            if (returnPromise instanceof Promise) {
              returnPromise.then(onFullScreenEntered).catch(reject);
            }
          }.bind(this));
        },
        exit: function() {
          return new Promise(function(resolve, reject) {
            if (!this.isFullscreen) {
              resolve();
              return;
            }
            var onFullScreenExit = function() {
              this.off("change", onFullScreenExit);
              resolve();
            }.bind(this);
            this.on("change", onFullScreenExit);
            var returnPromise = document2[fn.exitFullscreen]();
            if (returnPromise instanceof Promise) {
              returnPromise.then(onFullScreenExit).catch(reject);
            }
          }.bind(this));
        },
        toggle: function(element, options) {
          return this.isFullscreen ? this.exit() : this.request(element, options);
        },
        onchange: function(callback) {
          this.on("change", callback);
        },
        onerror: function(callback) {
          this.on("error", callback);
        },
        on: function(event, callback) {
          var eventName = eventNameMap[event];
          if (eventName) {
            document2.addEventListener(eventName, callback, false);
          }
        },
        off: function(event, callback) {
          var eventName = eventNameMap[event];
          if (eventName) {
            document2.removeEventListener(eventName, callback, false);
          }
        },
        raw: fn
      };
      if (!fn) {
        if (isCommonjs) {
          module2.exports = { isEnabled: false };
        } else {
          window.screenfull = { isEnabled: false };
        }
        return;
      }
      Object.defineProperties(screenfull2, {
        isFullscreen: {
          get: function() {
            return Boolean(document2[fn.fullscreenElement]);
          }
        },
        element: {
          enumerable: true,
          get: function() {
            return document2[fn.fullscreenElement];
          }
        },
        isEnabled: {
          enumerable: true,
          get: function() {
            return Boolean(document2[fn.fullscreenEnabled]);
          }
        }
      });
      if (isCommonjs) {
        module2.exports = screenfull2;
      } else {
        window.screenfull = screenfull2;
      }
    })();
  }
});

// node_modules/lodash/_overArg.js
var require_overArg = __commonJS({
  "node_modules/lodash/_overArg.js"(exports, module2) {
    function overArg(func, transform) {
      return function(arg) {
        return func(transform(arg));
      };
    }
    module2.exports = overArg;
  }
});

// node_modules/lodash/_getPrototype.js
var require_getPrototype = __commonJS({
  "node_modules/lodash/_getPrototype.js"(exports, module2) {
    var overArg = require_overArg();
    var getPrototype = overArg(Object.getPrototypeOf, Object);
    module2.exports = getPrototype;
  }
});

// node_modules/lodash/isPlainObject.js
var require_isPlainObject = __commonJS({
  "node_modules/lodash/isPlainObject.js"(exports, module2) {
    var baseGetTag = require_baseGetTag();
    var getPrototype = require_getPrototype();
    var isObjectLike = require_isObjectLike();
    var objectTag = "[object Object]";
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var funcToString = funcProto.toString;
    var hasOwnProperty = objectProto.hasOwnProperty;
    var objectCtorString = funcToString.call(Object);
    function isPlainObject3(value) {
      if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
        return false;
      }
      var proto = getPrototype(value);
      if (proto === null) {
        return true;
      }
      var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
      return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
    }
    module2.exports = isPlainObject3;
  }
});

// scripts/_remaining-vendor-tmp/entry-9sqtS.mjs
var entry_9sqtS_exports = {};
__export(entry_9sqtS_exports, {
  CancelledError: () => CancelledError,
  clearCache: () => clearCache,
  configResponsive: () => configResponsive,
  createUpdateEffect: () => createUpdateEffect,
  default: () => entry_9sqtS_default,
  isCancelledError: () => isCancelledError,
  useAntdTable: () => useAntdTable_default,
  useAsyncEffect: () => useAsyncEffect_default,
  useBoolean: () => useBoolean,
  useClickAway: () => useClickAway_default,
  useControllableValue: () => useControllableValue_default,
  useCookieState: () => useCookieState_default,
  useCountDown: () => useCountDown_default,
  useCounter: () => useCounter_default,
  useCreation: () => useCreation_default,
  useDebounce: () => useDebounce_default,
  useDebounceEffect: () => useDebounceEffect_default,
  useDebounceFn: () => useDebounceFn_default,
  useDeepCompareEffect: () => useDeepCompareEffect_default,
  useDeepCompareLayoutEffect: () => useDeepCompareLayoutEffect_default,
  useDocumentVisibility: () => useDocumentVisibility_default,
  useDrag: () => useDrag_default,
  useDrop: () => useDrop_default,
  useDynamicList: () => useDynamicList_default,
  useEventEmitter: () => useEventEmitter_default,
  useEventListener: () => useEventListener_default,
  useEventTarget: () => useEventTarget_default,
  useExternal: () => useExternal_default,
  useFavicon: () => useFavicon_default,
  useFocusWithin: () => useFocusWithin,
  useFullscreen: () => useFullscreen_default,
  useFusionTable: () => useFusionTable_default,
  useGetState: () => useGetState_default,
  useHistoryTravel: () => useHistoryTravel,
  useHover: () => useHover_default,
  useInViewport: () => useInViewport_default,
  useInfiniteScroll: () => useInfiniteScroll_default,
  useInterval: () => useInterval_default,
  useIsomorphicLayoutEffect: () => useIsomorphicLayoutEffect_default,
  useKeyPress: () => useKeyPress_default,
  useLatest: () => useLatest_default,
  useLocalStorageState: () => useLocalStorageState_default,
  useLockFn: () => useLockFn_default,
  useLongPress: () => useLongPress_default,
  useMap: () => useMap_default,
  useMemoizedFn: () => useMemoizedFn_default,
  useMount: () => useMount_default,
  useMouse: () => useMouse_default,
  useMutationObserver: () => useMutationObserver_default,
  useNetwork: () => useNetwork_default,
  usePagination: () => usePagination_default,
  usePrevious: () => usePrevious_default,
  useRafInterval: () => useRafInterval_default,
  useRafState: () => useRafState_default,
  useRafTimeout: () => useRafTimeout_default,
  useReactive: () => useReactive_default,
  useRequest: () => useRequest_default2,
  useResetState: () => useResetState_default,
  useResponsive: () => useResponsive_default,
  useSafeState: () => useSafeState_default,
  useScroll: () => useScroll_default,
  useSelections: () => useSelections_default,
  useSessionStorageState: () => useSessionStorageState_default,
  useSet: () => useSet_default,
  useSetState: () => useSetState_default,
  useSize: () => useSize_default,
  useTextSelection: () => useTextSelection_default,
  useTheme: () => useTheme,
  useThrottle: () => useThrottle_default,
  useThrottleEffect: () => useThrottleEffect_default,
  useThrottleFn: () => useThrottleFn_default,
  useTimeout: () => useTimeout_default,
  useTitle: () => useTitle_default,
  useToggle: () => useToggle_default,
  useTrackedEffect: () => useTrackedEffect_default,
  useUnmount: () => useUnmount_default,
  useUnmountedRef: () => useUnmountedRef_default,
  useUpdate: () => useUpdate_default,
  useUpdateEffect: () => useUpdateEffect_default,
  useUpdateLayoutEffect: () => useUpdateLayoutEffect_default,
  useVirtualList: () => useVirtualList_default,
  useWebSocket: () => useWebSocket_default,
  useWhyDidYouUpdate: () => useWhyDidYouUpdate_default
});
module.exports = __toCommonJS(entry_9sqtS_exports);

// node_modules/ahooks/es/index.js
var es_exports = {};
__export(es_exports, {
  CancelledError: () => CancelledError,
  clearCache: () => clearCache,
  configResponsive: () => configResponsive,
  createUpdateEffect: () => createUpdateEffect,
  isCancelledError: () => isCancelledError,
  useAntdTable: () => useAntdTable_default,
  useAsyncEffect: () => useAsyncEffect_default,
  useBoolean: () => useBoolean,
  useClickAway: () => useClickAway_default,
  useControllableValue: () => useControllableValue_default,
  useCookieState: () => useCookieState_default,
  useCountDown: () => useCountDown_default,
  useCounter: () => useCounter_default,
  useCreation: () => useCreation_default,
  useDebounce: () => useDebounce_default,
  useDebounceEffect: () => useDebounceEffect_default,
  useDebounceFn: () => useDebounceFn_default,
  useDeepCompareEffect: () => useDeepCompareEffect_default,
  useDeepCompareLayoutEffect: () => useDeepCompareLayoutEffect_default,
  useDocumentVisibility: () => useDocumentVisibility_default,
  useDrag: () => useDrag_default,
  useDrop: () => useDrop_default,
  useDynamicList: () => useDynamicList_default,
  useEventEmitter: () => useEventEmitter_default,
  useEventListener: () => useEventListener_default,
  useEventTarget: () => useEventTarget_default,
  useExternal: () => useExternal_default,
  useFavicon: () => useFavicon_default,
  useFocusWithin: () => useFocusWithin,
  useFullscreen: () => useFullscreen_default,
  useFusionTable: () => useFusionTable_default,
  useGetState: () => useGetState_default,
  useHistoryTravel: () => useHistoryTravel,
  useHover: () => useHover_default,
  useInViewport: () => useInViewport_default,
  useInfiniteScroll: () => useInfiniteScroll_default,
  useInterval: () => useInterval_default,
  useIsomorphicLayoutEffect: () => useIsomorphicLayoutEffect_default,
  useKeyPress: () => useKeyPress_default,
  useLatest: () => useLatest_default,
  useLocalStorageState: () => useLocalStorageState_default,
  useLockFn: () => useLockFn_default,
  useLongPress: () => useLongPress_default,
  useMap: () => useMap_default,
  useMemoizedFn: () => useMemoizedFn_default,
  useMount: () => useMount_default,
  useMouse: () => useMouse_default,
  useMutationObserver: () => useMutationObserver_default,
  useNetwork: () => useNetwork_default,
  usePagination: () => usePagination_default,
  usePrevious: () => usePrevious_default,
  useRafInterval: () => useRafInterval_default,
  useRafState: () => useRafState_default,
  useRafTimeout: () => useRafTimeout_default,
  useReactive: () => useReactive_default,
  useRequest: () => useRequest_default2,
  useResetState: () => useResetState_default,
  useResponsive: () => useResponsive_default,
  useSafeState: () => useSafeState_default,
  useScroll: () => useScroll_default,
  useSelections: () => useSelections_default,
  useSessionStorageState: () => useSessionStorageState_default,
  useSet: () => useSet_default,
  useSetState: () => useSetState_default,
  useSize: () => useSize_default,
  useTextSelection: () => useTextSelection_default,
  useTheme: () => useTheme,
  useThrottle: () => useThrottle_default,
  useThrottleEffect: () => useThrottleEffect_default,
  useThrottleFn: () => useThrottleFn_default,
  useTimeout: () => useTimeout_default,
  useTitle: () => useTitle_default,
  useToggle: () => useToggle_default,
  useTrackedEffect: () => useTrackedEffect_default,
  useUnmount: () => useUnmount_default,
  useUnmountedRef: () => useUnmountedRef_default,
  useUpdate: () => useUpdate_default,
  useUpdateEffect: () => useUpdateEffect_default,
  useUpdateLayoutEffect: () => useUpdateLayoutEffect_default,
  useVirtualList: () => useVirtualList_default,
  useWebSocket: () => useWebSocket_default,
  useWhyDidYouUpdate: () => useWhyDidYouUpdate_default
});

// node_modules/ahooks/es/createUpdateEffect/index.js
var import_react = __jrReq("react");
var createUpdateEffect = (hook) => (effect, deps) => {
  const isMounted = (0, import_react.useRef)(false);
  hook(() => {
    return () => {
      isMounted.current = false;
    };
  }, []);
  hook(() => {
    if (!isMounted.current) {
      isMounted.current = true;
    } else {
      return effect();
    }
  }, deps);
};

// node_modules/tslib/tslib.es6.mjs
function __rest(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
}
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}

// node_modules/ahooks/es/useAntdTable/index.js
var import_react18 = __jrReq("react");

// node_modules/ahooks/es/useMemoizedFn/index.js
var import_react2 = __jrReq("react");

// node_modules/ahooks/es/utils/index.js
var isObject = (val) => {
  return val !== null && typeof val === "object";
};
var isFunction = (val) => {
  return typeof val === "function";
};
var isString = (val) => {
  return typeof val === "string";
};
var isBoolean = (val) => {
  return typeof val === "boolean";
};
var isNumber = (val) => {
  return typeof val === "number" && !Number.isNaN(val);
};
var isUndef = (val) => {
  return typeof val === "undefined";
};
var isNonNullable = (val) => {
  return val !== void 0 && val !== null;
};
var isThenable = (val) => {
  return isNonNullable(val) && isFunction(val.then);
};

// node_modules/ahooks/es/utils/isDev.js
var isDev = false;
var isDev_default = isDev;

// node_modules/ahooks/es/useMemoizedFn/index.js
var useMemoizedFn = (fn) => {
  if (isDev_default) {
    if (!isFunction(fn)) {
      console.error(`useMemoizedFn expected parameter is a function, got ${typeof fn}`);
    }
  }
  const fnRef = (0, import_react2.useRef)(fn);
  fnRef.current = (0, import_react2.useMemo)(() => fn, [fn]);
  const memoizedFn = (0, import_react2.useRef)(void 0);
  if (!memoizedFn.current) {
    memoizedFn.current = function(...args) {
      return fnRef.current.apply(this, args);
    };
  }
  return memoizedFn.current;
};
var useMemoizedFn_default = useMemoizedFn;

// node_modules/ahooks/es/usePagination/index.js
var import_react17 = __jrReq("react");

// node_modules/ahooks/es/useRequest/src/plugins/useAutoRunPlugin.js
var import_react4 = __jrReq("react");

// node_modules/ahooks/es/useUpdateEffect/index.js
var import_react3 = __jrReq("react");
var useUpdateEffect_default = createUpdateEffect(import_react3.useEffect);

// node_modules/ahooks/es/useRequest/src/plugins/useAutoRunPlugin.js
var useAutoRunPlugin = (fetchInstance, { manual, ready = true, defaultParams = [], refreshDeps = [], refreshDepsAction }) => {
  const hasAutoRun = (0, import_react4.useRef)(false);
  hasAutoRun.current = false;
  useUpdateEffect_default(() => {
    if (!manual && ready) {
      hasAutoRun.current = true;
      fetchInstance.run(...defaultParams);
    }
  }, [ready]);
  useUpdateEffect_default(() => {
    if (hasAutoRun.current) {
      return;
    }
    if (!manual) {
      hasAutoRun.current = true;
      if (refreshDepsAction) {
        refreshDepsAction();
      } else {
        fetchInstance.refresh();
      }
    }
  }, [...refreshDeps]);
  return {
    onBefore: () => {
      if (!ready) {
        return {
          stopNow: true
        };
      }
    }
  };
};
useAutoRunPlugin.onInit = ({ ready = true, manual }) => {
  return {
    loading: !manual && ready
  };
};
var useAutoRunPlugin_default = useAutoRunPlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useCachePlugin.js
var import_react8 = __jrReq("react");

// node_modules/ahooks/es/useCreation/index.js
var import_react5 = __jrReq("react");

// node_modules/ahooks/es/utils/depsAreSame.js
function depsAreSame(oldDeps, deps) {
  if (oldDeps === deps) {
    return true;
  }
  for (let i = 0; i < oldDeps.length; i++) {
    if (!Object.is(oldDeps[i], deps[i])) {
      return false;
    }
  }
  return true;
}
var depsAreSame_default = depsAreSame;

// node_modules/ahooks/es/useCreation/index.js
var useCreation = (factory, deps) => {
  const { current } = (0, import_react5.useRef)({
    deps,
    obj: void 0,
    initialized: false
  });
  if (current.initialized === false || !depsAreSame_default(current.deps, deps)) {
    current.deps = deps;
    current.obj = factory();
    current.initialized = true;
  }
  return current.obj;
};
var useCreation_default = useCreation;

// node_modules/ahooks/es/useUnmount/index.js
var import_react7 = __jrReq("react");

// node_modules/ahooks/es/useLatest/index.js
var import_react6 = __jrReq("react");
function useLatest(value) {
  const ref = (0, import_react6.useRef)(value);
  ref.current = value;
  return ref;
}
var useLatest_default = useLatest;

// node_modules/ahooks/es/useUnmount/index.js
var useUnmount = (fn) => {
  if (isDev_default) {
    if (!isFunction(fn)) {
      console.error(`useUnmount expected parameter is a function, got ${typeof fn}`);
    }
  }
  const fnRef = useLatest_default(fn);
  (0, import_react7.useEffect)(() => () => {
    fnRef.current();
  }, []);
};
var useUnmount_default = useUnmount;

// node_modules/ahooks/es/useRequest/src/utils/cache.js
var cache = /* @__PURE__ */ new Map();
var clearTimer = (cachedData) => {
  if (cachedData === null || cachedData === void 0 ? void 0 : cachedData.timer) {
    clearTimeout(cachedData.timer);
  }
};
var setCache = (key, cacheTime, cachedData) => {
  const currentCache = cache.get(key);
  clearTimer(currentCache);
  let timer = void 0;
  if (cacheTime > -1) {
    timer = setTimeout(() => {
      cache.delete(key);
    }, cacheTime);
  }
  cache.set(key, Object.assign(Object.assign({}, cachedData), { timer }));
};
var getCache = (key) => {
  return cache.get(key);
};
var clearCache = (key) => {
  if (key) {
    const cacheKeys = Array.isArray(key) ? key : [key];
    cacheKeys.forEach((cacheKey) => {
      const currentCache = cache.get(cacheKey);
      clearTimer(currentCache);
      cache.delete(cacheKey);
    });
  } else {
    for (const currentCache of cache.values()) {
      clearTimer(currentCache);
    }
    cache.clear();
  }
};

// node_modules/ahooks/es/useRequest/src/utils/cachePromise.js
var cachePromise = /* @__PURE__ */ new Map();
var getCachePromise = (cacheKey) => {
  return cachePromise.get(cacheKey);
};
var setCachePromise = (cacheKey, promise) => {
  cachePromise.set(cacheKey, promise);
  promise.then((res) => {
    cachePromise.delete(cacheKey);
    return res;
  }).catch(() => {
    cachePromise.delete(cacheKey);
  });
};

// node_modules/ahooks/es/useRequest/src/utils/cacheSubscribe.js
var listeners = {};
var trigger = (key, data) => {
  if (listeners[key]) {
    listeners[key].forEach((item) => item(data));
  }
};
var subscribe = (key, listener) => {
  if (!listeners[key]) {
    listeners[key] = [];
  }
  listeners[key].push(listener);
  return function unsubscribe() {
    const index2 = listeners[key].indexOf(listener);
    listeners[key].splice(index2, 1);
  };
};

// node_modules/ahooks/es/useRequest/src/plugins/useCachePlugin.js
var useCachePlugin = (fetchInstance, { cacheKey, cacheTime = 5 * 60 * 1e3, staleTime = 0, setCache: customSetCache, getCache: customGetCache }) => {
  const unSubscribeRef = (0, import_react8.useRef)(void 0);
  const currentPromiseRef = (0, import_react8.useRef)(void 0);
  const _setCache = (key, cachedData) => {
    if (customSetCache) {
      customSetCache(cachedData);
    } else {
      setCache(key, cacheTime, cachedData);
    }
    trigger(key, cachedData.data);
  };
  const _getCache = (key, params = []) => {
    if (customGetCache) {
      return customGetCache(params);
    }
    return getCache(key);
  };
  useCreation_default(() => {
    if (!cacheKey) {
      return;
    }
    const cacheData = _getCache(cacheKey);
    if (cacheData && Object.hasOwnProperty.call(cacheData, "data")) {
      fetchInstance.state.data = cacheData.data;
      fetchInstance.state.params = cacheData.params;
      if (staleTime === -1 || Date.now() - cacheData.time <= staleTime) {
        fetchInstance.state.loading = false;
      }
    }
    unSubscribeRef.current = subscribe(cacheKey, (data) => {
      fetchInstance.setState({ data });
    });
  }, []);
  useUnmount_default(() => {
    var _a2;
    (_a2 = unSubscribeRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unSubscribeRef);
  });
  if (!cacheKey) {
    return {};
  }
  return {
    onBefore: (params) => {
      const cacheData = _getCache(cacheKey, params);
      if (!cacheData || !Object.hasOwnProperty.call(cacheData, "data")) {
        return {};
      }
      if (staleTime === -1 || Date.now() - cacheData.time <= staleTime) {
        return {
          loading: false,
          data: cacheData === null || cacheData === void 0 ? void 0 : cacheData.data,
          error: void 0,
          returnNow: true
        };
      } else {
        return {
          data: cacheData === null || cacheData === void 0 ? void 0 : cacheData.data,
          error: void 0
        };
      }
    },
    onRequest: (service, args) => {
      let servicePromise = getCachePromise(cacheKey);
      if (servicePromise && servicePromise !== currentPromiseRef.current) {
        return { servicePromise };
      }
      servicePromise = service(...args);
      currentPromiseRef.current = servicePromise;
      setCachePromise(cacheKey, servicePromise);
      return { servicePromise };
    },
    onSuccess: (data, params) => {
      var _a2;
      if (cacheKey) {
        (_a2 = unSubscribeRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unSubscribeRef);
        _setCache(cacheKey, {
          data,
          params,
          time: Date.now()
        });
        unSubscribeRef.current = subscribe(cacheKey, (d) => {
          fetchInstance.setState({ data: d });
        });
      }
    },
    onMutate: (data) => {
      var _a2;
      if (cacheKey) {
        (_a2 = unSubscribeRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unSubscribeRef);
        _setCache(cacheKey, {
          data,
          params: fetchInstance.state.params,
          time: Date.now()
        });
        unSubscribeRef.current = subscribe(cacheKey, (d) => {
          fetchInstance.setState({ data: d });
        });
      }
    }
  };
};
var useCachePlugin_default = useCachePlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useDebouncePlugin.js
var import_debounce = __toESM(require_debounce());
var import_react9 = __jrReq("react");

// node_modules/ahooks/es/useRequest/src/utils/cancelledError.js
var _a;
var CANCELLED_ERROR_FLAG = "__AHOOKS_CANCELLED_ERROR__";
var CancelledError = class _CancelledError extends Error {
  constructor(message = "useRequest: the request was cancelled or superseded.") {
    super(message);
    this[_a] = true;
    this.name = "CancelledError";
    Object.setPrototypeOf(this, _CancelledError.prototype);
  }
};
_a = CANCELLED_ERROR_FLAG;
function isCancelledError(error) {
  return error instanceof CancelledError || typeof error === "object" && error !== null && error[CANCELLED_ERROR_FLAG] === true;
}
function cancelPendingPromise(pendingRejectRef) {
  var _b;
  (_b = pendingRejectRef.current) === null || _b === void 0 ? void 0 : _b.call(pendingRejectRef, new CancelledError());
  pendingRejectRef.current = void 0;
}

// node_modules/ahooks/es/useRequest/src/plugins/useDebouncePlugin.js
var useDebouncePlugin = (fetchInstance, { debounceWait, debounceLeading, debounceTrailing, debounceMaxWait, ready }) => {
  const debouncedRef = (0, import_react9.useRef)(void 0);
  const pendingRejectRef = (0, import_react9.useRef)(void 0);
  const options = (0, import_react9.useMemo)(() => {
    const ret = {};
    if (debounceLeading !== void 0) {
      ret.leading = debounceLeading;
    }
    if (debounceTrailing !== void 0) {
      ret.trailing = debounceTrailing;
    }
    if (debounceMaxWait !== void 0) {
      ret.maxWait = debounceMaxWait;
    }
    return ret;
  }, [debounceLeading, debounceTrailing, debounceMaxWait]);
  (0, import_react9.useEffect)(() => {
    if (debounceWait) {
      const _originRunAsync = fetchInstance.runAsync.bind(fetchInstance);
      debouncedRef.current = (0, import_debounce.default)((callback) => {
        callback();
      }, debounceWait, options);
      fetchInstance.runAsync = (...args) => {
        if (fetchInstance.options.ready === false) {
          return _originRunAsync(...args);
        }
        cancelPendingPromise(pendingRejectRef);
        return new Promise((resolve, reject) => {
          var _a2;
          let callbackInvoked = false;
          pendingRejectRef.current = reject;
          (_a2 = debouncedRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(debouncedRef, () => {
            callbackInvoked = true;
            pendingRejectRef.current = void 0;
            _originRunAsync(...args).then(resolve).catch(reject);
          });
          if (!callbackInvoked && options.trailing === false) {
            cancelPendingPromise(pendingRejectRef);
          }
        });
      };
      return () => {
        var _a2;
        (_a2 = debouncedRef.current) === null || _a2 === void 0 ? void 0 : _a2.cancel();
        cancelPendingPromise(pendingRejectRef);
        fetchInstance.runAsync = _originRunAsync;
      };
    }
  }, [debounceWait, options, fetchInstance]);
  useUpdateEffect_default(() => {
    var _a2;
    if (ready === false) {
      (_a2 = debouncedRef.current) === null || _a2 === void 0 ? void 0 : _a2.cancel();
      cancelPendingPromise(pendingRejectRef);
    }
  }, [ready]);
  if (!debounceWait) {
    return {};
  }
  return {
    onCancel: () => {
      var _a2;
      (_a2 = debouncedRef.current) === null || _a2 === void 0 ? void 0 : _a2.cancel();
      cancelPendingPromise(pendingRejectRef);
    }
  };
};
var useDebouncePlugin_default = useDebouncePlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useLoadingDelayPlugin.js
var import_react10 = __jrReq("react");
var useLoadingDelayPlugin = (fetchInstance, { loadingDelay, ready }) => {
  const timerRef = (0, import_react10.useRef)(void 0);
  if (!loadingDelay) {
    return {};
  }
  const cancelTimeout = () => {
    if (timerRef.current) {
      clearTimeout(timerRef.current);
    }
  };
  return {
    onBefore: () => {
      cancelTimeout();
      if (ready !== false) {
        timerRef.current = setTimeout(() => {
          fetchInstance.setState({
            loading: true
          });
        }, loadingDelay);
      }
      return {
        loading: false
      };
    },
    onFinally: () => {
      cancelTimeout();
    },
    onCancel: () => {
      cancelTimeout();
    }
  };
};
var useLoadingDelayPlugin_default = useLoadingDelayPlugin;

// node_modules/ahooks/es/useRequest/src/plugins/usePollingPlugin.js
var import_react11 = __jrReq("react");

// node_modules/ahooks/es/utils/isBrowser.js
var isBrowser = !!(typeof window !== "undefined" && window.document && window.document.createElement);
var isBrowser_default = isBrowser;

// node_modules/ahooks/es/useRequest/src/utils/isDocumentVisible.js
function isDocumentVisible() {
  if (isBrowser_default) {
    return document.visibilityState !== "hidden";
  }
  return true;
}

// node_modules/ahooks/es/useRequest/src/utils/subscribeReVisible.js
var listeners2 = /* @__PURE__ */ new Set();
function subscribe2(listener) {
  listeners2.add(listener);
  return function unsubscribe() {
    listeners2.has(listener) && listeners2.delete(listener);
  };
}
if (isBrowser_default) {
  const revalidate = () => {
    if (!isDocumentVisible())
      return;
    listeners2.forEach((listener) => listener());
  };
  window.addEventListener("visibilitychange", revalidate, false);
}
var subscribeReVisible_default = subscribe2;

// node_modules/ahooks/es/useRequest/src/plugins/usePollingPlugin.js
var usePollingPlugin = (fetchInstance, { pollingInterval, pollingWhenHidden = true, pollingErrorRetryCount = -1 }) => {
  const timerRef = (0, import_react11.useRef)(void 0);
  const unsubscribeRef = (0, import_react11.useRef)(void 0);
  const countRef = (0, import_react11.useRef)(0);
  const stopPolling = () => {
    var _a2;
    if (timerRef.current) {
      clearTimeout(timerRef.current);
    }
    (_a2 = unsubscribeRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unsubscribeRef);
  };
  useUpdateEffect_default(() => {
    if (!pollingInterval) {
      stopPolling();
    }
  }, [pollingInterval]);
  if (!pollingInterval) {
    return {};
  }
  return {
    onBefore: () => {
      stopPolling();
    },
    onError: () => {
      countRef.current += 1;
    },
    onSuccess: () => {
      countRef.current = 0;
    },
    onFinally: () => {
      if (pollingErrorRetryCount === -1 || // When an error occurs, the request is not repeated after pollingErrorRetryCount retries
      pollingErrorRetryCount !== -1 && countRef.current <= pollingErrorRetryCount) {
        timerRef.current = setTimeout(() => {
          if (!pollingWhenHidden && !isDocumentVisible()) {
            unsubscribeRef.current = subscribeReVisible_default(() => {
              fetchInstance.refresh();
            });
          } else {
            fetchInstance.refresh();
          }
        }, pollingInterval);
      } else {
        countRef.current = 0;
      }
    },
    onCancel: () => {
      stopPolling();
    }
  };
};
var usePollingPlugin_default = usePollingPlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useRefreshOnWindowFocusPlugin.js
var import_react12 = __jrReq("react");

// node_modules/ahooks/es/useRequest/src/utils/limit.js
function limit(fn, timespan) {
  let pending = false;
  return (...args) => {
    if (pending)
      return;
    pending = true;
    fn(...args);
    setTimeout(() => {
      pending = false;
    }, timespan);
  };
}

// node_modules/ahooks/es/useRequest/src/utils/isOnline.js
var isOnline = () => {
  if (isBrowser_default && typeof navigator.onLine !== "undefined") {
    return navigator.onLine;
  }
  return true;
};
var isOnline_default = isOnline;

// node_modules/ahooks/es/useRequest/src/utils/subscribeFocus.js
var listeners3 = /* @__PURE__ */ new Set();
function subscribe3(listener) {
  listeners3.add(listener);
  return function unsubscribe() {
    listeners3.has(listener) && listeners3.delete(listener);
  };
}
if (isBrowser_default) {
  const revalidate = () => {
    if (!isDocumentVisible() || !isOnline_default())
      return;
    listeners3.forEach((listener) => listener());
  };
  window.addEventListener("visibilitychange", revalidate, false);
  window.addEventListener("focus", revalidate, false);
}
var subscribeFocus_default = subscribe3;

// node_modules/ahooks/es/useRequest/src/plugins/useRefreshOnWindowFocusPlugin.js
var useRefreshOnWindowFocusPlugin = (fetchInstance, { refreshOnWindowFocus, focusTimespan = 5e3 }) => {
  const unsubscribeRef = (0, import_react12.useRef)(void 0);
  const stopSubscribe = () => {
    var _a2;
    (_a2 = unsubscribeRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unsubscribeRef);
  };
  (0, import_react12.useEffect)(() => {
    if (refreshOnWindowFocus) {
      const limitRefresh = limit(fetchInstance.refresh.bind(fetchInstance), focusTimespan);
      unsubscribeRef.current = subscribeFocus_default(() => {
        limitRefresh();
      });
    }
    return () => {
      stopSubscribe();
    };
  }, [refreshOnWindowFocus, focusTimespan]);
  useUnmount_default(() => {
    stopSubscribe();
  });
  return {};
};
var useRefreshOnWindowFocusPlugin_default = useRefreshOnWindowFocusPlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useRetryPlugin.js
var import_react13 = __jrReq("react");
var useRetryPlugin = (fetchInstance, { retryInterval, retryCount }) => {
  const timerRef = (0, import_react13.useRef)(void 0);
  const countRef = (0, import_react13.useRef)(0);
  const triggerByRetry = (0, import_react13.useRef)(false);
  if (!retryCount) {
    return {};
  }
  return {
    onBefore: () => {
      if (!triggerByRetry.current) {
        countRef.current = 0;
      }
      triggerByRetry.current = false;
      if (timerRef.current) {
        clearTimeout(timerRef.current);
      }
    },
    onSuccess: () => {
      countRef.current = 0;
    },
    onError: () => {
      countRef.current += 1;
      if (retryCount === -1 || countRef.current <= retryCount) {
        const timeout = retryInterval !== null && retryInterval !== void 0 ? retryInterval : Math.min(1e3 * Math.pow(2, countRef.current), 3e4);
        timerRef.current = setTimeout(() => {
          triggerByRetry.current = true;
          fetchInstance.refresh();
        }, timeout);
      } else {
        countRef.current = 0;
      }
    },
    onCancel: () => {
      countRef.current = 0;
      if (timerRef.current) {
        clearTimeout(timerRef.current);
      }
    }
  };
};
var useRetryPlugin_default = useRetryPlugin;

// node_modules/ahooks/es/useRequest/src/plugins/useThrottlePlugin.js
var import_throttle = __toESM(require_throttle());
var import_react14 = __jrReq("react");
var useThrottlePlugin = (fetchInstance, { throttleWait, throttleLeading, throttleTrailing }) => {
  const throttledRef = (0, import_react14.useRef)(void 0);
  const pendingRejectRef = (0, import_react14.useRef)(void 0);
  const options = {};
  if (throttleLeading !== void 0) {
    options.leading = throttleLeading;
  }
  if (throttleTrailing !== void 0) {
    options.trailing = throttleTrailing;
  }
  (0, import_react14.useEffect)(() => {
    if (throttleWait) {
      const _originRunAsync = fetchInstance.runAsync.bind(fetchInstance);
      throttledRef.current = (0, import_throttle.default)((callback) => {
        callback();
      }, throttleWait, options);
      fetchInstance.runAsync = (...args) => {
        cancelPendingPromise(pendingRejectRef);
        return new Promise((resolve, reject) => {
          var _a2;
          let callbackInvoked = false;
          pendingRejectRef.current = reject;
          (_a2 = throttledRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(throttledRef, () => {
            callbackInvoked = true;
            pendingRejectRef.current = void 0;
            _originRunAsync(...args).then(resolve).catch(reject);
          });
          if (!callbackInvoked && options.trailing === false) {
            cancelPendingPromise(pendingRejectRef);
          }
        });
      };
      return () => {
        var _a2;
        fetchInstance.runAsync = _originRunAsync;
        (_a2 = throttledRef.current) === null || _a2 === void 0 ? void 0 : _a2.cancel();
        cancelPendingPromise(pendingRejectRef);
      };
    }
  }, [throttleWait, throttleLeading, throttleTrailing]);
  if (!throttleWait) {
    return {};
  }
  return {
    onCancel: () => {
      var _a2;
      (_a2 = throttledRef.current) === null || _a2 === void 0 ? void 0 : _a2.cancel();
      cancelPendingPromise(pendingRejectRef);
    }
  };
};
var useThrottlePlugin_default = useThrottlePlugin;

// node_modules/ahooks/es/useMount/index.js
var import_react15 = __jrReq("react");
var useMount = (fn) => {
  if (isDev_default) {
    if (!isFunction(fn)) {
      console.error(`useMount: parameter \`fn\` expected to be a function, but got "${typeof fn}".`);
    }
  }
  (0, import_react15.useEffect)(() => {
    const result = fn === null || fn === void 0 ? void 0 : fn();
    if (isThenable(result)) {
      return;
    }
    return result;
  }, []);
};
var useMount_default = useMount;

// node_modules/ahooks/es/useUpdate/index.js
var import_react16 = __jrReq("react");
var useUpdate = () => {
  const [, setState] = (0, import_react16.useState)({});
  return useMemoizedFn_default(() => setState({}));
};
var useUpdate_default = useUpdate;

// node_modules/ahooks/es/useRequest/src/Fetch.js
var Fetch = class {
  constructor(serviceRef, options, subscribe4, initState3 = {}) {
    this.serviceRef = serviceRef;
    this.options = options;
    this.subscribe = subscribe4;
    this.initState = initState3;
    this.pluginImpls = [];
    this.count = 0;
    this.state = {
      loading: false,
      params: void 0,
      data: void 0,
      error: void 0
    };
    this.state = Object.assign(Object.assign(Object.assign({}, this.state), { loading: !options.manual }), initState3);
  }
  setState(s = {}) {
    this.state = Object.assign(Object.assign({}, this.state), s);
    this.subscribe();
  }
  runPluginHandler(event, ...rest) {
    const r = this.pluginImpls.map((i) => {
      var _a2;
      return (_a2 = i[event]) === null || _a2 === void 0 ? void 0 : _a2.call(i, ...rest);
    }).filter(Boolean);
    return Object.assign({}, ...r);
  }
  runAsync(...params) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b, _c, _d, _e, _f, _g, _h, _j, _k;
      this.count += 1;
      const currentCount = this.count;
      const _l = this.runPluginHandler("onBefore", params), { stopNow = false, returnNow = false } = _l, state = __rest(_l, ["stopNow", "returnNow"]);
      if (stopNow) {
        return Promise.resolve(state.data);
      }
      this.setState(Object.assign({ loading: true, params }, state));
      if (returnNow) {
        return Promise.resolve(state.data);
      }
      (_b = (_a2 = this.options).onBefore) === null || _b === void 0 ? void 0 : _b.call(_a2, params);
      try {
        let { servicePromise } = this.runPluginHandler("onRequest", this.serviceRef.current, params);
        if (!servicePromise) {
          servicePromise = this.serviceRef.current(...params);
        }
        const res = yield servicePromise;
        if (currentCount !== this.count) {
          throw new CancelledError();
        }
        this.setState({
          data: res,
          error: void 0,
          loading: false
        });
        (_d = (_c = this.options).onSuccess) === null || _d === void 0 ? void 0 : _d.call(_c, res, params);
        this.runPluginHandler("onSuccess", res, params);
        (_f = (_e = this.options).onFinally) === null || _f === void 0 ? void 0 : _f.call(_e, params, res, void 0);
        if (currentCount === this.count) {
          this.runPluginHandler("onFinally", params, res, void 0);
        }
        return res;
      } catch (error) {
        if (currentCount !== this.count) {
          throw isCancelledError(error) ? error : new CancelledError();
        }
        this.setState({
          error,
          loading: false
        });
        (_h = (_g = this.options).onError) === null || _h === void 0 ? void 0 : _h.call(_g, error, params);
        this.runPluginHandler("onError", error, params);
        (_k = (_j = this.options).onFinally) === null || _k === void 0 ? void 0 : _k.call(_j, params, void 0, error);
        if (currentCount === this.count) {
          this.runPluginHandler("onFinally", params, void 0, error);
        }
        throw error;
      }
    });
  }
  run(...params) {
    this.runAsync(...params).catch((error) => {
      if (isCancelledError(error)) {
        return;
      }
      if (!this.options.onError) {
        console.error(error);
      }
    });
  }
  cancel() {
    this.count += 1;
    this.setState({
      loading: false
    });
    this.runPluginHandler("onCancel");
  }
  refresh() {
    this.run(...this.state.params || []);
  }
  refreshAsync() {
    return this.runAsync(...this.state.params || []);
  }
  mutate(data) {
    const targetData = isFunction(data) ? data(this.state.data) : data;
    this.runPluginHandler("onMutate", targetData);
    this.setState({
      data: targetData
    });
  }
};

// node_modules/ahooks/es/useRequest/src/useRequestImplement.js
function useRequestImplement(service, options = {}, plugins = []) {
  const { manual = false, ready = true } = options, rest = __rest(options, ["manual", "ready"]);
  if (isDev_default) {
    if (options.defaultParams && !Array.isArray(options.defaultParams)) {
      console.warn(`expected defaultParams is array, got ${typeof options.defaultParams}`);
    }
  }
  const fetchOptions = Object.assign({
    manual,
    ready
  }, rest);
  const serviceRef = useLatest_default(service);
  const update = useUpdate_default();
  const fetchInstance = useCreation_default(() => {
    const initState3 = plugins.map((p) => {
      var _a2;
      return (_a2 = p === null || p === void 0 ? void 0 : p.onInit) === null || _a2 === void 0 ? void 0 : _a2.call(p, fetchOptions);
    }).filter(Boolean);
    return new Fetch(serviceRef, fetchOptions, update, Object.assign({}, ...initState3));
  }, []);
  fetchInstance.options = fetchOptions;
  fetchInstance.pluginImpls = plugins.map((p) => p(fetchInstance, fetchOptions));
  useMount_default(() => {
    if (!manual && ready) {
      const params = fetchInstance.state.params || options.defaultParams || [];
      fetchInstance.run(...params);
    }
  });
  useUnmount_default(() => {
    fetchInstance.cancel();
  });
  return {
    loading: fetchInstance.state.loading,
    data: fetchInstance.state.data,
    error: fetchInstance.state.error,
    params: fetchInstance.state.params || [],
    cancel: useMemoizedFn_default(fetchInstance.cancel.bind(fetchInstance)),
    refresh: useMemoizedFn_default(fetchInstance.refresh.bind(fetchInstance)),
    refreshAsync: useMemoizedFn_default(fetchInstance.refreshAsync.bind(fetchInstance)),
    run: useMemoizedFn_default(fetchInstance.run.bind(fetchInstance)),
    runAsync: useMemoizedFn_default((...args) => fetchInstance.runAsync(...args)),
    mutate: useMemoizedFn_default(fetchInstance.mutate.bind(fetchInstance))
  };
}
var useRequestImplement_default = useRequestImplement;

// node_modules/ahooks/es/useRequest/src/useRequest.js
function useRequest(service, options, plugins) {
  return useRequestImplement_default(service, options, [
    ...plugins || [],
    useDebouncePlugin_default,
    useLoadingDelayPlugin_default,
    usePollingPlugin_default,
    useRefreshOnWindowFocusPlugin_default,
    useThrottlePlugin_default,
    useAutoRunPlugin_default,
    useCachePlugin_default,
    useRetryPlugin_default
  ]);
}
var useRequest_default = useRequest;

// node_modules/ahooks/es/useRequest/index.js
var useRequest_default2 = useRequest_default;

// node_modules/ahooks/es/usePagination/index.js
var usePagination = (service, options = {}) => {
  var _a2;
  const { defaultPageSize = 10, defaultCurrent = 1 } = options, rest = __rest(options, ["defaultPageSize", "defaultCurrent"]);
  const result = useRequest_default2(service, Object.assign({ defaultParams: [{ current: defaultCurrent, pageSize: defaultPageSize }], refreshDepsAction: () => {
    changeCurrent(1);
  } }, rest));
  const { current = 1, pageSize = defaultPageSize } = result.params[0] || {};
  const total = ((_a2 = result.data) === null || _a2 === void 0 ? void 0 : _a2.total) || 0;
  const totalPage = (0, import_react17.useMemo)(() => Math.ceil(total / pageSize), [pageSize, total]);
  const onChange = (c, p) => {
    let toCurrent = c <= 0 ? 1 : c;
    const toPageSize = p <= 0 ? 1 : p;
    const tempTotalPage = Math.ceil(total / toPageSize);
    if (toCurrent > tempTotalPage) {
      toCurrent = Math.max(1, tempTotalPage);
    }
    const [oldPaginationParams = {}, ...restParams] = result.params || [];
    result.run(...[
      Object.assign(Object.assign({}, oldPaginationParams), { current: toCurrent, pageSize: toPageSize }),
      ...restParams
    ]);
  };
  const changeCurrent = (c) => {
    onChange(c, pageSize);
  };
  const changePageSize = (p) => {
    onChange(current, p);
  };
  return Object.assign(Object.assign({}, result), { pagination: {
    current,
    pageSize,
    total,
    totalPage,
    onChange: useMemoizedFn_default(onChange),
    changeCurrent: useMemoizedFn_default(changeCurrent),
    changePageSize: useMemoizedFn_default(changePageSize)
  } });
};
var usePagination_default = usePagination;

// node_modules/ahooks/es/useAntdTable/index.js
var useAntdTable = (service, options = {}) => {
  var _a2;
  const { form, defaultType = "simple", defaultParams, manual = false, refreshDeps = [], ready = true } = options, rest = __rest(options, ["form", "defaultType", "defaultParams", "manual", "refreshDeps", "ready"]);
  const result = usePagination_default(service, Object.assign(Object.assign({ ready, manual: true }, rest), { onSuccess(...args) {
    var _a3;
    runSuccessRef.current = true;
    (_a3 = rest.onSuccess) === null || _a3 === void 0 ? void 0 : _a3.call(rest, ...args);
  } }));
  const { params = [], run } = result;
  const cacheFormTableData = params[2] || {};
  const [type, setType] = (0, import_react18.useState)((cacheFormTableData === null || cacheFormTableData === void 0 ? void 0 : cacheFormTableData.type) || defaultType);
  const allFormDataRef = (0, import_react18.useRef)({});
  const defaultDataSourceRef = (0, import_react18.useRef)([]);
  const runSuccessRef = (0, import_react18.useRef)(false);
  const isAntdV4 = !!(form === null || form === void 0 ? void 0 : form.getInternalHooks);
  const getActiveFieldValues = () => {
    if (!form) {
      return {};
    }
    if (isAntdV4) {
      return form.getFieldsValue(null, () => true);
    }
    const allFieldsValue = form.getFieldsValue();
    const activeFieldsValue = {};
    Object.keys(allFieldsValue).forEach((key) => {
      if (form.getFieldInstance ? form.getFieldInstance(key) : true) {
        activeFieldsValue[key] = allFieldsValue[key];
      }
    });
    return activeFieldsValue;
  };
  const validateFields = () => {
    if (!form) {
      return Promise.resolve({});
    }
    const activeFieldsValue = getActiveFieldValues();
    const fields = Object.keys(activeFieldsValue);
    if (isAntdV4) {
      if (form.validateFieldsReturnFormatValue) {
        return form.validateFieldsReturnFormatValue(fields);
      }
      return form.validateFields(fields);
    }
    return new Promise((resolve, reject) => {
      form.validateFields(fields, (errors, values) => {
        if (errors) {
          reject(errors);
        } else {
          resolve(values);
        }
      });
    });
  };
  const restoreForm = () => {
    if (!form) {
      return;
    }
    if (isAntdV4) {
      return form.setFieldsValue(allFormDataRef.current);
    }
    const activeFieldsValue = {};
    Object.keys(allFormDataRef.current).forEach((key) => {
      if (form.getFieldInstance ? form.getFieldInstance(key) : true) {
        activeFieldsValue[key] = allFormDataRef.current[key];
      }
    });
    form.setFieldsValue(activeFieldsValue);
  };
  const changeType = () => {
    const activeFieldsValue = getActiveFieldValues();
    allFormDataRef.current = Object.assign(Object.assign({}, allFormDataRef.current), activeFieldsValue);
    setType((t) => t === "simple" ? "advance" : "simple");
  };
  const _submit = (initPagination) => {
    if (!ready) {
      return;
    }
    setTimeout(() => {
      validateFields().then((values = {}) => {
        const pagination = initPagination || Object.assign(Object.assign({ pageSize: options.defaultPageSize || 10 }, (params === null || params === void 0 ? void 0 : params[0]) || {}), { current: 1 });
        if (!form) {
          run(pagination);
          return;
        }
        allFormDataRef.current = Object.assign(Object.assign({}, allFormDataRef.current), values);
        run(pagination, values, {
          allFormData: allFormDataRef.current,
          type
        });
      }).catch((err) => err);
    });
  };
  const reset = () => {
    var _a3, _b;
    if (form) {
      form.resetFields();
    }
    _submit(Object.assign(Object.assign({}, (defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[0]) || {}), { pageSize: options.defaultPageSize || ((_b = (_a3 = options.defaultParams) === null || _a3 === void 0 ? void 0 : _a3[0]) === null || _b === void 0 ? void 0 : _b.pageSize) || 10, current: 1 }));
  };
  const submit = (e) => {
    var _a3, _b, _c;
    (_a3 = e === null || e === void 0 ? void 0 : e.preventDefault) === null || _a3 === void 0 ? void 0 : _a3.call(e);
    _submit(runSuccessRef.current ? void 0 : Object.assign({ pageSize: options.defaultPageSize || ((_c = (_b = options.defaultParams) === null || _b === void 0 ? void 0 : _b[0]) === null || _c === void 0 ? void 0 : _c.pageSize) || 10, current: 1 }, (defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[0]) || {}));
  };
  const onTableChange = (pagination, filters, sorter, extra) => {
    const [oldPaginationParams, ...restParams] = params || [];
    run(Object.assign(Object.assign({}, oldPaginationParams), {
      current: pagination.current,
      pageSize: pagination.pageSize,
      filters,
      sorter,
      extra
    }), ...restParams);
  };
  (0, import_react18.useEffect)(() => {
    if (params.length > 0) {
      allFormDataRef.current = (cacheFormTableData === null || cacheFormTableData === void 0 ? void 0 : cacheFormTableData.allFormData) || {};
      restoreForm();
      run(...params);
      return;
    }
    if (ready) {
      allFormDataRef.current = (defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[1]) || {};
      restoreForm();
      if (!manual) {
        _submit(defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[0]);
      }
    }
  }, []);
  useUpdateEffect_default(() => {
    if (!ready) {
      return;
    }
    restoreForm();
  }, [type]);
  const hasAutoRun = (0, import_react18.useRef)(false);
  hasAutoRun.current = false;
  useUpdateEffect_default(() => {
    if (!manual && ready) {
      hasAutoRun.current = true;
      if (form) {
        form.resetFields();
      }
      allFormDataRef.current = (defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[1]) || {};
      restoreForm();
      _submit(defaultParams === null || defaultParams === void 0 ? void 0 : defaultParams[0]);
    }
  }, [ready]);
  useUpdateEffect_default(() => {
    if (hasAutoRun.current) {
      return;
    }
    if (!ready) {
      return;
    }
    if (!manual) {
      hasAutoRun.current = true;
      if (options.refreshDepsAction) {
        options.refreshDepsAction();
      } else {
        result.pagination.changeCurrent(1);
      }
    }
  }, [...refreshDeps]);
  return Object.assign(Object.assign({}, result), { tableProps: {
    dataSource: ((_a2 = result.data) === null || _a2 === void 0 ? void 0 : _a2.list) || defaultDataSourceRef.current,
    loading: result.loading,
    onChange: useMemoizedFn_default(onTableChange),
    pagination: {
      current: result.pagination.current,
      pageSize: result.pagination.pageSize,
      total: result.pagination.total
    }
  }, search: {
    submit: useMemoizedFn_default(submit),
    type,
    changeType: useMemoizedFn_default(changeType),
    reset: useMemoizedFn_default(reset)
  } });
};
var useAntdTable_default = useAntdTable;

// node_modules/ahooks/es/useAsyncEffect/index.js
var import_react19 = __jrReq("react");
function isAsyncGenerator(val) {
  return isFunction(val[Symbol.asyncIterator]);
}
function useAsyncEffect(effect, deps) {
  (0, import_react19.useEffect)(() => {
    const e = effect();
    if (typeof e === "function") {
      return e;
    }
    const asyncEffect = e;
    let cancelled = false;
    function execute() {
      return __awaiter(this, void 0, void 0, function* () {
        if (isAsyncGenerator(asyncEffect)) {
          while (true) {
            const result = yield asyncEffect.next();
            if (result.done || cancelled) {
              break;
            }
          }
        } else {
          yield asyncEffect;
        }
      });
    }
    execute();
    return () => {
      cancelled = true;
    };
  }, deps);
}
var useAsyncEffect_default = useAsyncEffect;

// node_modules/ahooks/es/useBoolean/index.js
var import_react21 = __jrReq("react");

// node_modules/ahooks/es/useToggle/index.js
var import_react20 = __jrReq("react");
function useToggle(defaultValue = false, reverseValue) {
  const [state, setState] = (0, import_react20.useState)(defaultValue);
  const actions = (0, import_react20.useMemo)(() => {
    const reverseValueOrigin = reverseValue === void 0 ? !defaultValue : reverseValue;
    const toggle = () => setState((s) => s === defaultValue ? reverseValueOrigin : defaultValue);
    const set = (value) => setState(value);
    const setLeft = () => setState(defaultValue);
    const setRight = () => setState(reverseValueOrigin);
    return {
      toggle,
      set,
      setLeft,
      setRight
    };
  }, []);
  return [state, actions];
}
var useToggle_default = useToggle;

// node_modules/ahooks/es/useBoolean/index.js
function useBoolean(defaultValue = false) {
  const [state, { toggle, set }] = useToggle_default(!!defaultValue);
  const actions = (0, import_react21.useMemo)(() => {
    const setTrue = () => set(true);
    const setFalse = () => set(false);
    return {
      toggle,
      set: (v) => set(!!v),
      setTrue,
      setFalse
    };
  }, []);
  return [state, actions];
}

// node_modules/ahooks/es/utils/domTarget.js
function getTargetElement(target, defaultElement) {
  if (!isBrowser_default) {
    return void 0;
  }
  if (!target) {
    return defaultElement;
  }
  let targetElement;
  if (isFunction(target)) {
    targetElement = target();
  } else if ("current" in target) {
    targetElement = target.current;
  } else {
    targetElement = target;
  }
  return targetElement;
}

// node_modules/ahooks/es/utils/getDocumentOrShadow.js
var checkIfAllInShadow = (targets) => {
  return targets.every((item) => {
    const targetElement = getTargetElement(item);
    if (!targetElement) {
      return false;
    }
    if (targetElement.getRootNode() instanceof ShadowRoot) {
      return true;
    }
    return false;
  });
};
var getShadow = (node) => {
  if (!node) {
    return document;
  }
  return node.getRootNode();
};
var getDocumentOrShadow = (target) => {
  if (!target || !document.getRootNode) {
    return document;
  }
  const targets = Array.isArray(target) ? target : [target];
  if (checkIfAllInShadow(targets)) {
    return getShadow(getTargetElement(targets[0]));
  }
  return document;
};
var getDocumentOrShadow_default = getDocumentOrShadow;

// node_modules/ahooks/es/utils/useEffectWithTarget.js
var import_react23 = __jrReq("react");

// node_modules/ahooks/es/utils/createEffectWithTarget.js
var import_react22 = __jrReq("react");
var createEffectWithTarget = (useEffectType) => {
  const useEffectWithTarget3 = (effect, deps, target) => {
    const hasInitRef = (0, import_react22.useRef)(false);
    const lastElementRef = (0, import_react22.useRef)([]);
    const lastDepsRef = (0, import_react22.useRef)([]);
    const unLoadRef = (0, import_react22.useRef)(void 0);
    useEffectType(() => {
      var _a2;
      const targets = Array.isArray(target) ? target : [target];
      const els = targets.map((item) => getTargetElement(item));
      if (!hasInitRef.current) {
        hasInitRef.current = true;
        lastElementRef.current = els;
        lastDepsRef.current = deps;
        unLoadRef.current = effect();
        return;
      }
      if (els.length !== lastElementRef.current.length || !depsAreSame_default(lastElementRef.current, els) || !depsAreSame_default(lastDepsRef.current, deps)) {
        (_a2 = unLoadRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unLoadRef);
        lastElementRef.current = els;
        lastDepsRef.current = deps;
        unLoadRef.current = effect();
      }
    });
    useUnmount_default(() => {
      var _a2;
      (_a2 = unLoadRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(unLoadRef);
      hasInitRef.current = false;
    });
  };
  return useEffectWithTarget3;
};
var createEffectWithTarget_default = createEffectWithTarget;

// node_modules/ahooks/es/utils/useEffectWithTarget.js
var useEffectWithTarget = createEffectWithTarget_default(import_react23.useEffect);
var useEffectWithTarget_default = useEffectWithTarget;

// node_modules/ahooks/es/useClickAway/index.js
var useClickAway = (onClickAway, target, eventName = "click") => {
  const onClickAwayRef = useLatest_default(onClickAway);
  useEffectWithTarget_default(() => {
    const handler = (event) => {
      const targets = Array.isArray(target) ? target : [target];
      if (targets.some((item) => {
        const targetElement = getTargetElement(item);
        return !targetElement || targetElement.contains(event.target);
      })) {
        return;
      }
      onClickAwayRef.current(event);
    };
    const documentOrShadow = getDocumentOrShadow_default(target);
    const eventNames = Array.isArray(eventName) ? eventName : [eventName];
    eventNames.forEach((event) => {
      documentOrShadow.addEventListener(event, handler);
    });
    return () => {
      eventNames.forEach((event) => {
        documentOrShadow.removeEventListener(event, handler);
      });
    };
  }, Array.isArray(eventName) ? eventName : [eventName], target);
};
var useClickAway_default = useClickAway;

// node_modules/ahooks/es/useControllableValue/index.js
var import_react24 = __jrReq("react");
function useControllableValue(defaultProps, options = {}) {
  const props = defaultProps !== null && defaultProps !== void 0 ? defaultProps : {};
  const { defaultValue, defaultValuePropName = "defaultValue", valuePropName = "value", trigger: trigger2 = "onChange" } = options;
  const value = props[valuePropName];
  const isControlled = Object.prototype.hasOwnProperty.call(props, valuePropName);
  const initialValue = (0, import_react24.useMemo)(() => {
    if (isControlled) {
      return value;
    }
    if (Object.prototype.hasOwnProperty.call(props, defaultValuePropName)) {
      return props[defaultValuePropName];
    }
    return defaultValue;
  }, []);
  const stateRef = (0, import_react24.useRef)(initialValue);
  if (isControlled) {
    stateRef.current = value;
  }
  const update = useUpdate_default();
  function setState(v, ...args) {
    const r = isFunction(v) ? v(stateRef.current) : v;
    if (!isControlled) {
      stateRef.current = r;
      update();
    }
    if (props[trigger2]) {
      props[trigger2](r, ...args);
    }
  }
  return [stateRef.current, useMemoizedFn_default(setState)];
}
var useControllableValue_default = useControllableValue;

// node_modules/js-cookie/dist/js.cookie.mjs
function assign(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];
    for (var key in source) {
      if (key === "__proto__") continue;
      target[key] = source[key];
    }
  }
  return target;
}
var defaultConverter = {
  read: function(value) {
    if (value[0] === '"') {
      value = value.slice(1, -1);
    }
    return value.replace(/(%[\dA-F]{2})+/gi, decodeURIComponent);
  },
  write: function(value) {
    return encodeURIComponent(value).replace(
      /%(2[346BF]|3[AC-F]|40|5[BDE]|60|7[BCD])/g,
      decodeURIComponent
    );
  }
};
function init(converter, defaultAttributes) {
  function set(name, value, attributes) {
    if (typeof document === "undefined") {
      return;
    }
    attributes = assign({}, defaultAttributes, attributes);
    if (typeof attributes.expires === "number") {
      attributes.expires = new Date(Date.now() + attributes.expires * 864e5);
    }
    if (attributes.expires) {
      attributes.expires = attributes.expires.toUTCString();
    }
    name = encodeURIComponent(name).replace(/%(2[346B]|5E|60|7C)/g, decodeURIComponent).replace(/[()]/g, escape);
    var stringifiedAttributes = "";
    for (var attributeName in attributes) {
      if (!attributes[attributeName]) {
        continue;
      }
      stringifiedAttributes += "; " + attributeName;
      if (attributes[attributeName] === true) {
        continue;
      }
      stringifiedAttributes += "=" + attributes[attributeName].split(";")[0];
    }
    return document.cookie = name + "=" + converter.write(value, name) + stringifiedAttributes;
  }
  function get(name) {
    if (typeof document === "undefined" || arguments.length && !name) {
      return;
    }
    var cookies = document.cookie ? document.cookie.split("; ") : [];
    var jar = {};
    for (var i = 0; i < cookies.length; i++) {
      var parts = cookies[i].split("=");
      var value = parts.slice(1).join("=");
      try {
        var found = decodeURIComponent(parts[0]);
        if (!(found in jar)) jar[found] = converter.read(value, found);
        if (name === found) {
          break;
        }
      } catch (_e) {
      }
    }
    return name ? jar[name] : jar;
  }
  return Object.create(
    {
      set,
      get,
      remove: function(name, attributes) {
        set(
          name,
          "",
          assign({}, attributes, {
            expires: -1
          })
        );
      },
      withAttributes: function(attributes) {
        return init(this.converter, assign({}, this.attributes, attributes));
      },
      withConverter: function(converter2) {
        return init(assign({}, this.converter, converter2), this.attributes);
      }
    },
    {
      attributes: { value: Object.freeze(defaultAttributes) },
      converter: { value: Object.freeze(converter) }
    }
  );
}
var api = init(defaultConverter, { path: "/" });

// node_modules/ahooks/es/useCookieState/index.js
var import_react25 = __jrReq("react");
var useCookieState = (cookieKey, options = {}) => {
  const [state, setState] = (0, import_react25.useState)(() => {
    const cookieValue = api.get(cookieKey);
    if (isString(cookieValue)) {
      return cookieValue;
    }
    if (isFunction(options.defaultValue)) {
      return options.defaultValue();
    }
    return options.defaultValue;
  });
  const updateState = useMemoizedFn_default((newValue, newOptions = {}) => {
    const _a2 = Object.assign(Object.assign({}, options), newOptions), { defaultValue } = _a2, restOptions = __rest(_a2, ["defaultValue"]);
    const value = isFunction(newValue) ? newValue(state) : newValue;
    setState(value);
    if (value === void 0) {
      api.remove(cookieKey);
    } else {
      api.set(cookieKey, value, restOptions);
    }
  });
  return [state, updateState];
};
var useCookieState_default = useCookieState;

// node_modules/ahooks/es/useCountDown/index.js
var import_dayjs = __toESM(require_dayjs_min());
var import_react26 = __jrReq("react");
var calcLeft = (target) => {
  if (!target) {
    return 0;
  }
  const left = (0, import_dayjs.default)(target).valueOf() - Date.now();
  return left < 0 ? 0 : left;
};
var parseMs = (milliseconds) => {
  return {
    days: Math.floor(milliseconds / 864e5),
    hours: Math.floor(milliseconds / 36e5) % 24,
    minutes: Math.floor(milliseconds / 6e4) % 60,
    seconds: Math.floor(milliseconds / 1e3) % 60,
    milliseconds: Math.floor(milliseconds) % 1e3
  };
};
var useCountdown = (options = {}) => {
  const { leftTime, targetDate, interval = 1e3, onEnd } = options || {};
  const memoLeftTime = (0, import_react26.useMemo)(() => {
    return isNumber(leftTime) && leftTime > 0 ? Date.now() + leftTime : void 0;
  }, [leftTime]);
  const target = "leftTime" in options ? memoLeftTime : targetDate;
  const [timeLeft, setTimeLeft] = (0, import_react26.useState)(() => calcLeft(target));
  const onEndRef = useLatest_default(onEnd);
  (0, import_react26.useEffect)(() => {
    if (!target) {
      setTimeLeft(0);
      return;
    }
    setTimeLeft(calcLeft(target));
    const timer = setInterval(() => {
      var _a2;
      const targetLeft = calcLeft(target);
      setTimeLeft(targetLeft);
      if (targetLeft === 0) {
        clearInterval(timer);
        (_a2 = onEndRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onEndRef);
      }
    }, interval);
    return () => clearInterval(timer);
  }, [target, interval]);
  const formattedRes = (0, import_react26.useMemo)(() => parseMs(timeLeft), [timeLeft]);
  return [timeLeft, formattedRes];
};
var useCountDown_default = useCountdown;

// node_modules/ahooks/es/useCounter/index.js
var import_react27 = __jrReq("react");
function getTargetValue(val, options = {}) {
  const { min, max } = options;
  let target = val;
  if (isNumber(max)) {
    target = Math.min(max, target);
  }
  if (isNumber(min)) {
    target = Math.max(min, target);
  }
  return target;
}
function useCounter(initialValue = 0, options = {}) {
  const { min, max } = options;
  const [current, setCurrent] = (0, import_react27.useState)(() => {
    return getTargetValue(initialValue, {
      min,
      max
    });
  });
  const setValue = (value) => {
    setCurrent((c) => {
      const target = isNumber(value) ? value : value(c);
      return getTargetValue(target, {
        max,
        min
      });
    });
  };
  const inc = (delta = 1) => {
    setValue((c) => c + delta);
  };
  const dec = (delta = 1) => {
    setValue((c) => c - delta);
  };
  const set = (value) => {
    setValue(value);
  };
  const reset = () => {
    setValue(initialValue);
  };
  return [
    current,
    {
      inc: useMemoizedFn_default(inc),
      dec: useMemoizedFn_default(dec),
      set: useMemoizedFn_default(set),
      reset: useMemoizedFn_default(reset)
    }
  ];
}
var useCounter_default = useCounter;

// node_modules/ahooks/es/useDebounce/index.js
var import_react29 = __jrReq("react");

// node_modules/ahooks/es/utils/lodash-polyfill.js
var import_debounce2 = __toESM(require_debounce());
function isNodeOrWeb() {
  const freeGlobal = (typeof global === "undefined" ? "undefined" : typeof global) == "object" && global && global.Object === Object && global;
  const freeSelf = typeof self == "object" && self && self.Object === Object && self;
  return freeGlobal || freeSelf;
}
if (!isNodeOrWeb()) {
  global.Date = Date;
}

// node_modules/ahooks/es/useDebounceFn/index.js
var import_react28 = __jrReq("react");
function useDebounceFn(fn, options) {
  var _a2;
  if (isDev_default) {
    if (!isFunction(fn)) {
      console.error(`useDebounceFn expected parameter is a function, got ${typeof fn}`);
    }
  }
  const fnRef = useLatest_default(fn);
  const wait = (_a2 = options === null || options === void 0 ? void 0 : options.wait) !== null && _a2 !== void 0 ? _a2 : 1e3;
  const debounced = (0, import_react28.useMemo)(() => (0, import_debounce2.default)((...args) => {
    return fnRef.current(...args);
  }, wait, options), []);
  useUnmount_default(() => {
    debounced.cancel();
  });
  return {
    run: debounced,
    cancel: debounced.cancel,
    flush: debounced.flush
  };
}
var useDebounceFn_default = useDebounceFn;

// node_modules/ahooks/es/useDebounce/index.js
function useDebounce(value, options) {
  const [debounced, setDebounced] = (0, import_react29.useState)(value);
  const { run } = useDebounceFn_default(() => {
    setDebounced(value);
  }, options);
  (0, import_react29.useEffect)(() => {
    run();
  }, [value]);
  return debounced;
}
var useDebounce_default = useDebounce;

// node_modules/ahooks/es/useDebounceEffect/index.js
var import_react30 = __jrReq("react");
function useDebounceEffect(effect, deps, options) {
  const [flag, setFlag] = (0, import_react30.useState)({});
  const { run } = useDebounceFn_default(() => {
    setFlag({});
  }, options);
  (0, import_react30.useEffect)(() => {
    return run();
  }, deps);
  useUpdateEffect_default(effect, [flag]);
}
var useDebounceEffect_default = useDebounceEffect;

// node_modules/ahooks/es/useDeepCompareEffect/index.js
var import_react32 = __jrReq("react");

// node_modules/ahooks/es/createDeepCompareEffect/index.js
var import_react31 = __jrReq("react");

// node_modules/ahooks/es/utils/depsEqual.js
var import_react_fast_compare = __toESM(require_react_fast_compare());
var depsEqual = (aDeps = [], bDeps = []) => (0, import_react_fast_compare.default)(aDeps, bDeps);

// node_modules/ahooks/es/createDeepCompareEffect/index.js
var createDeepCompareEffect = (hook) => (effect, deps) => {
  const ref = (0, import_react31.useRef)(void 0);
  const signalRef = (0, import_react31.useRef)(0);
  if (deps === void 0 || !depsEqual(deps, ref.current)) {
    signalRef.current += 1;
  }
  ref.current = deps;
  hook(effect, [signalRef.current]);
};

// node_modules/ahooks/es/useDeepCompareEffect/index.js
var useDeepCompareEffect_default = createDeepCompareEffect(import_react32.useEffect);

// node_modules/ahooks/es/useDeepCompareLayoutEffect/index.js
var import_react33 = __jrReq("react");
var useDeepCompareLayoutEffect_default = createDeepCompareEffect(import_react33.useLayoutEffect);

// node_modules/ahooks/es/useDocumentVisibility/index.js
var import_react34 = __jrReq("react");

// node_modules/ahooks/es/useEventListener/index.js
function useEventListener(eventName, handler, options = {}) {
  const { enable = true } = options;
  const handlerRef = useLatest_default(handler);
  useEffectWithTarget_default(() => {
    if (!enable) {
      return;
    }
    const targetElement = getTargetElement(options.target, window);
    if (!(targetElement === null || targetElement === void 0 ? void 0 : targetElement.addEventListener)) {
      return;
    }
    const eventListener = (event) => {
      return handlerRef.current(event);
    };
    const eventNameArray = Array.isArray(eventName) ? eventName : [eventName];
    eventNameArray.forEach((event) => {
      targetElement.addEventListener(event, eventListener, {
        capture: options.capture,
        once: options.once,
        passive: options.passive
      });
    });
    return () => {
      eventNameArray.forEach((event) => {
        targetElement.removeEventListener(event, eventListener, {
          capture: options.capture
        });
      });
    };
  }, [eventName, options.capture, options.once, options.passive, enable], options.target);
}
var useEventListener_default = useEventListener;

// node_modules/ahooks/es/useDocumentVisibility/index.js
var getVisibility = () => {
  if (!isBrowser_default) {
    return "visible";
  }
  return document.visibilityState;
};
function useDocumentVisibility() {
  const [documentVisibility, setDocumentVisibility] = (0, import_react34.useState)(getVisibility);
  useEventListener_default("visibilitychange", () => {
    setDocumentVisibility(getVisibility());
  }, {
    target: () => document
  });
  return documentVisibility;
}
var useDocumentVisibility_default = useDocumentVisibility;

// node_modules/ahooks/es/useDrag/index.js
var import_react35 = __jrReq("react");
var useDrag = (data, target, options = {}) => {
  const optionsRef = useLatest_default(options);
  const dataRef = useLatest_default(data);
  const imageElementRef = (0, import_react35.useRef)(void 0);
  const { dragImage } = optionsRef.current;
  useMount_default(() => {
    if (dragImage === null || dragImage === void 0 ? void 0 : dragImage.image) {
      const { image } = dragImage;
      if (isString(image)) {
        const imageElement = new Image();
        imageElement.src = image;
        imageElementRef.current = imageElement;
      } else {
        imageElementRef.current = image;
      }
    }
  });
  useEffectWithTarget_default(() => {
    const targetElement = getTargetElement(target);
    if (!(targetElement === null || targetElement === void 0 ? void 0 : targetElement.addEventListener)) {
      return;
    }
    const onDragStart = (event) => {
      var _a2, _b;
      (_b = (_a2 = optionsRef.current).onDragStart) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
      event.dataTransfer.setData("custom", JSON.stringify(dataRef.current));
      if ((dragImage === null || dragImage === void 0 ? void 0 : dragImage.image) && imageElementRef.current) {
        const { offsetX = 0, offsetY = 0 } = dragImage;
        event.dataTransfer.setDragImage(imageElementRef.current, offsetX, offsetY);
      }
    };
    const onDragEnd = (event) => {
      var _a2, _b;
      (_b = (_a2 = optionsRef.current).onDragEnd) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
    };
    targetElement.setAttribute("draggable", "true");
    targetElement.addEventListener("dragstart", onDragStart);
    targetElement.addEventListener("dragend", onDragEnd);
    return () => {
      targetElement.removeEventListener("dragstart", onDragStart);
      targetElement.removeEventListener("dragend", onDragEnd);
    };
  }, [], target);
};
var useDrag_default = useDrag;

// node_modules/ahooks/es/useDrop/index.js
var import_react36 = __jrReq("react");
var useDrop = (target, options = {}) => {
  const optionsRef = useLatest_default(options);
  const dragEnterTarget = (0, import_react36.useRef)(void 0);
  useEffectWithTarget_default(() => {
    const targetElement = getTargetElement(target);
    if (!(targetElement === null || targetElement === void 0 ? void 0 : targetElement.addEventListener)) {
      return;
    }
    const onData = (dataTransfer, event) => {
      const uri = dataTransfer.getData("text/uri-list");
      const dom = dataTransfer.getData("custom");
      if (dom && optionsRef.current.onDom) {
        let data = dom;
        try {
          data = JSON.parse(dom);
        } catch (_a2) {
          data = dom;
        }
        optionsRef.current.onDom(data, event);
        return;
      }
      if (uri && optionsRef.current.onUri) {
        optionsRef.current.onUri(uri, event);
        return;
      }
      if (dataTransfer.files && dataTransfer.files.length && optionsRef.current.onFiles) {
        optionsRef.current.onFiles(Array.from(dataTransfer.files), event);
        return;
      }
      if (dataTransfer.items && dataTransfer.items.length && optionsRef.current.onText) {
        dataTransfer.items[0].getAsString((text) => {
          optionsRef.current.onText(text, event);
        });
      }
    };
    const onDragEnter = (event) => {
      var _a2, _b;
      event.preventDefault();
      event.stopPropagation();
      dragEnterTarget.current = event.target;
      (_b = (_a2 = optionsRef.current).onDragEnter) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
    };
    const onDragOver = (event) => {
      var _a2, _b;
      event.preventDefault();
      (_b = (_a2 = optionsRef.current).onDragOver) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
    };
    const onDragLeave = (event) => {
      var _a2, _b;
      if (event.target === dragEnterTarget.current) {
        (_b = (_a2 = optionsRef.current).onDragLeave) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
      }
    };
    const onDrop = (event) => {
      var _a2, _b;
      event.preventDefault();
      onData(event.dataTransfer, event);
      (_b = (_a2 = optionsRef.current).onDrop) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
    };
    const onPaste = (event) => {
      var _a2, _b;
      onData(event.clipboardData, event);
      (_b = (_a2 = optionsRef.current).onPaste) === null || _b === void 0 ? void 0 : _b.call(_a2, event);
    };
    targetElement.addEventListener("dragenter", onDragEnter);
    targetElement.addEventListener("dragover", onDragOver);
    targetElement.addEventListener("dragleave", onDragLeave);
    targetElement.addEventListener("drop", onDrop);
    targetElement.addEventListener("paste", onPaste);
    return () => {
      targetElement.removeEventListener("dragenter", onDragEnter);
      targetElement.removeEventListener("dragover", onDragOver);
      targetElement.removeEventListener("dragleave", onDragLeave);
      targetElement.removeEventListener("drop", onDrop);
      targetElement.removeEventListener("paste", onPaste);
    };
  }, [], target);
};
var useDrop_default = useDrop;

// node_modules/ahooks/es/useDynamicList/index.js
var import_react37 = __jrReq("react");
var useDynamicList = (initialList = []) => {
  const counterRef = (0, import_react37.useRef)(-1);
  const keyList = (0, import_react37.useRef)([]);
  const setKey = (0, import_react37.useCallback)((index2) => {
    counterRef.current += 1;
    keyList.current.splice(index2, 0, counterRef.current);
  }, []);
  const [list, setList] = (0, import_react37.useState)(() => {
    initialList.forEach((_, index2) => {
      setKey(index2);
    });
    return initialList;
  });
  const resetList = (0, import_react37.useCallback)((newList) => {
    keyList.current = [];
    setList(() => {
      newList.forEach((_, index2) => {
        setKey(index2);
      });
      return newList;
    });
  }, []);
  const insert = (0, import_react37.useCallback)((index2, item) => {
    setList((l) => {
      const temp = [...l];
      temp.splice(index2, 0, item);
      setKey(index2);
      return temp;
    });
  }, []);
  const getKey = (0, import_react37.useCallback)((index2) => keyList.current[index2], []);
  const getIndex = (0, import_react37.useCallback)((key) => keyList.current.findIndex((ele) => ele === key), []);
  const merge = (0, import_react37.useCallback)((index2, items) => {
    setList((l) => {
      const temp = [...l];
      items.forEach((_, i) => {
        setKey(index2 + i);
      });
      temp.splice(index2, 0, ...items);
      return temp;
    });
  }, []);
  const replace = (0, import_react37.useCallback)((index2, item) => {
    setList((l) => {
      const temp = [...l];
      temp[index2] = item;
      return temp;
    });
  }, []);
  const remove = (0, import_react37.useCallback)((index2) => {
    setList((l) => {
      const temp = [...l];
      temp.splice(index2, 1);
      try {
        keyList.current.splice(index2, 1);
      } catch (e) {
        console.error(e);
      }
      return temp;
    });
  }, []);
  const batchRemove = (0, import_react37.useCallback)((indexes) => {
    if (!Array.isArray(indexes)) {
      if (isDev_default) {
        console.error(`\`indexes\` parameter of \`batchRemove\` function expected to be an array, but got "${typeof indexes}".`);
      }
      return;
    }
    if (!indexes.length) {
      return;
    }
    setList((prevList) => {
      const newKeyList = [];
      const newList = prevList.filter((item, index2) => {
        const shouldKeep = !indexes.includes(index2);
        if (shouldKeep) {
          newKeyList.push(getKey(index2));
        }
        return shouldKeep;
      });
      keyList.current = newKeyList;
      return newList;
    });
  }, []);
  const move = (0, import_react37.useCallback)((oldIndex, newIndex) => {
    if (oldIndex === newIndex) {
      return;
    }
    setList((l) => {
      const newList = [...l];
      const temp = newList.filter((_, index2) => index2 !== oldIndex);
      temp.splice(newIndex, 0, newList[oldIndex]);
      try {
        const keyTemp = keyList.current.filter((_, index2) => index2 !== oldIndex);
        keyTemp.splice(newIndex, 0, keyList.current[oldIndex]);
        keyList.current = keyTemp;
      } catch (e) {
        console.error(e);
      }
      return temp;
    });
  }, []);
  const push = (0, import_react37.useCallback)((item) => {
    setList((l) => {
      setKey(l.length);
      return l.concat([item]);
    });
  }, []);
  const pop = (0, import_react37.useCallback)(() => {
    try {
      keyList.current = keyList.current.slice(0, keyList.current.length - 1);
    } catch (e) {
      console.error(e);
    }
    setList((l) => l.slice(0, l.length - 1));
  }, []);
  const unshift = (0, import_react37.useCallback)((item) => {
    setList((l) => {
      setKey(0);
      return [item].concat(l);
    });
  }, []);
  const shift = (0, import_react37.useCallback)(() => {
    try {
      keyList.current = keyList.current.slice(1, keyList.current.length);
    } catch (e) {
      console.error(e);
    }
    setList((l) => l.slice(1, l.length));
  }, []);
  const sortList = (0, import_react37.useCallback)(
    (result) => result.map((item, index2) => ({ key: index2, item })).sort((a, b) => getIndex(a.key) - getIndex(b.key)).filter((item) => !!item.item).map((item) => item.item),
    // retrive the data
    []
  );
  return {
    list,
    insert,
    merge,
    replace,
    remove,
    batchRemove,
    getKey,
    getIndex,
    move,
    push,
    pop,
    unshift,
    shift,
    sortList,
    resetList
  };
};
var useDynamicList_default = useDynamicList;

// node_modules/ahooks/es/useEventEmitter/index.js
var import_react38 = __jrReq("react");
var EventEmitter = class {
  constructor() {
    this.subscriptions = /* @__PURE__ */ new Set();
    this.emit = (val) => {
      for (const subscription of this.subscriptions) {
        subscription(val);
      }
    };
    this.useSubscription = (callback) => {
      const callbackRef = (0, import_react38.useRef)(void 0);
      callbackRef.current = callback;
      (0, import_react38.useEffect)(() => {
        function subscription(val) {
          if (callbackRef.current) {
            callbackRef.current(val);
          }
        }
        this.subscriptions.add(subscription);
        return () => {
          this.subscriptions.delete(subscription);
        };
      }, []);
    };
  }
};
var useEventEmitter = () => {
  const ref = (0, import_react38.useRef)(void 0);
  if (!ref.current) {
    ref.current = new EventEmitter();
  }
  return ref.current;
};
var useEventEmitter_default = useEventEmitter;

// node_modules/ahooks/es/useEventTarget/index.js
var import_react39 = __jrReq("react");
function useEventTarget(options) {
  const { initialValue, transformer } = options || {};
  const [value, setValue] = (0, import_react39.useState)(initialValue);
  const transformerRef = useLatest_default(transformer);
  const reset = (0, import_react39.useCallback)(() => setValue(initialValue), []);
  const onChange = (0, import_react39.useCallback)((e) => {
    const _value = e.target.value;
    if (isFunction(transformerRef.current)) {
      return setValue(transformerRef.current(_value));
    }
    return setValue(_value);
  }, []);
  return [
    value,
    {
      onChange,
      reset
    }
  ];
}
var useEventTarget_default = useEventTarget;

// node_modules/ahooks/es/useExternal/index.js
var import_react40 = __jrReq("react");
var EXTERNAL_USED_COUNT = {};
var loadScript = (path, props = {}) => {
  const script = document.querySelector(`script[src="${path}"]`);
  if (!script) {
    const newScript = document.createElement("script");
    newScript.src = path;
    Object.keys(props).forEach((key) => {
      newScript[key] = props[key];
    });
    newScript.setAttribute("data-status", "loading");
    document.body.appendChild(newScript);
    return {
      ref: newScript,
      status: "loading"
    };
  }
  return {
    ref: script,
    status: script.getAttribute("data-status") || "ready"
  };
};
var loadCss = (path, props = {}) => {
  const css = document.querySelector(`link[href="${path}"]`);
  if (!css) {
    const newCss = document.createElement("link");
    newCss.rel = "stylesheet";
    newCss.href = path;
    Object.keys(props).forEach((key) => {
      newCss[key] = props[key];
    });
    const isLegacyIECss = "hideFocus" in newCss;
    if (isLegacyIECss && newCss.relList) {
      newCss.rel = "preload";
      newCss.as = "style";
    }
    newCss.setAttribute("data-status", "loading");
    document.head.appendChild(newCss);
    return {
      ref: newCss,
      status: "loading"
    };
  }
  return {
    ref: css,
    status: css.getAttribute("data-status") || "ready"
  };
};
var useExternal = (path, options) => {
  const [status, setStatus] = (0, import_react40.useState)(path ? "loading" : "unset");
  const ref = (0, import_react40.useRef)(void 0);
  (0, import_react40.useEffect)(() => {
    if (!path) {
      setStatus("unset");
      return;
    }
    const pathname = path.replace(/[|#].*$/, "");
    if ((options === null || options === void 0 ? void 0 : options.type) === "css" || !(options === null || options === void 0 ? void 0 : options.type) && /(^css!|\.css$)/.test(pathname)) {
      const result = loadCss(path, options === null || options === void 0 ? void 0 : options.css);
      ref.current = result.ref;
      setStatus(result.status);
    } else if ((options === null || options === void 0 ? void 0 : options.type) === "js" || !(options === null || options === void 0 ? void 0 : options.type) && /(^js!|\.js$)/.test(pathname)) {
      const result = loadScript(path, options === null || options === void 0 ? void 0 : options.js);
      ref.current = result.ref;
      setStatus(result.status);
    } else {
      console.error("Cannot infer the type of external resource, and please provide a type ('js' | 'css'). Refer to the https://ahooks.js.org/hooks/dom/use-external/#options");
    }
    if (!ref.current) {
      return;
    }
    if (EXTERNAL_USED_COUNT[path] === void 0) {
      EXTERNAL_USED_COUNT[path] = 1;
    } else {
      EXTERNAL_USED_COUNT[path] += 1;
    }
    const handler = (event) => {
      var _a2;
      const targetStatus = event.type === "load" ? "ready" : "error";
      (_a2 = ref.current) === null || _a2 === void 0 ? void 0 : _a2.setAttribute("data-status", targetStatus);
      setStatus(targetStatus);
    };
    ref.current.addEventListener("load", handler);
    ref.current.addEventListener("error", handler);
    return () => {
      var _a2, _b, _c;
      (_a2 = ref.current) === null || _a2 === void 0 ? void 0 : _a2.removeEventListener("load", handler);
      (_b = ref.current) === null || _b === void 0 ? void 0 : _b.removeEventListener("error", handler);
      EXTERNAL_USED_COUNT[path] -= 1;
      if (EXTERNAL_USED_COUNT[path] === 0 && !(options === null || options === void 0 ? void 0 : options.keepWhenUnused)) {
        (_c = ref.current) === null || _c === void 0 ? void 0 : _c.remove();
      }
      ref.current = void 0;
    };
  }, [path]);
  return status;
};
var useExternal_default = useExternal;

// node_modules/ahooks/es/useFavicon/index.js
var import_react41 = __jrReq("react");
var ImgTypeMap = {
  SVG: "image/svg+xml",
  ICO: "image/x-icon",
  GIF: "image/gif",
  PNG: "image/png"
};
var useFavicon = (href) => {
  (0, import_react41.useEffect)(() => {
    if (!href) {
      return;
    }
    const cutUrl = href.split(".");
    const imgSuffix = cutUrl[cutUrl.length - 1].toLocaleUpperCase();
    const link = document.querySelector("link[rel*='icon']") || document.createElement("link");
    link.type = ImgTypeMap[imgSuffix];
    link.href = href;
    link.rel = "shortcut icon";
    document.getElementsByTagName("head")[0].appendChild(link);
  }, [href]);
};
var useFavicon_default = useFavicon;

// node_modules/ahooks/es/useFocusWithin/index.js
var import_react42 = __jrReq("react");
function useFocusWithin(target, options) {
  const [isFocusWithin, setIsFocusWithin] = (0, import_react42.useState)(false);
  const { onFocus, onBlur, onChange } = options || {};
  useEventListener_default("focusin", (e) => {
    if (!isFocusWithin) {
      onFocus === null || onFocus === void 0 ? void 0 : onFocus(e);
      onChange === null || onChange === void 0 ? void 0 : onChange(true);
      setIsFocusWithin(true);
    }
  }, {
    target
  });
  useEventListener_default("focusout", (e) => {
    var _a2, _b;
    if (isFocusWithin && !((_b = (_a2 = e.currentTarget) === null || _a2 === void 0 ? void 0 : _a2.contains) === null || _b === void 0 ? void 0 : _b.call(_a2, e.relatedTarget))) {
      onBlur === null || onBlur === void 0 ? void 0 : onBlur(e);
      onChange === null || onChange === void 0 ? void 0 : onChange(false);
      setIsFocusWithin(false);
    }
  }, {
    target
  });
  return isFocusWithin;
}

// node_modules/ahooks/es/useFullscreen/index.js
var import_react43 = __jrReq("react");
var import_screenfull = __toESM(require_screenfull());
var useFullscreen = (target, options) => {
  const { onExit, onEnter, pageFullscreen = false } = options || {};
  const { className = "ahooks-page-fullscreen", zIndex = 999999 } = isBoolean(pageFullscreen) || !pageFullscreen ? {} : pageFullscreen;
  const onExitRef = useLatest_default(onExit);
  const onEnterRef = useLatest_default(onEnter);
  const [state, setState] = (0, import_react43.useState)(getIsFullscreen);
  const stateRef = (0, import_react43.useRef)(getIsFullscreen());
  function getIsFullscreen() {
    return import_screenfull.default.isEnabled && !!import_screenfull.default.element && import_screenfull.default.element === getTargetElement(target);
  }
  const invokeCallback = (fullscreen) => {
    var _a2, _b;
    if (fullscreen) {
      (_a2 = onEnterRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onEnterRef);
    } else {
      (_b = onExitRef.current) === null || _b === void 0 ? void 0 : _b.call(onExitRef);
    }
  };
  const updateFullscreenState = (fullscreen) => {
    if (stateRef.current !== fullscreen) {
      invokeCallback(fullscreen);
      setState(fullscreen);
      stateRef.current = fullscreen;
    }
  };
  const onScreenfullChange = () => {
    const fullscreen = getIsFullscreen();
    updateFullscreenState(fullscreen);
  };
  const togglePageFullscreen = (fullscreen) => {
    const el = getTargetElement(target);
    if (!el) {
      return;
    }
    let styleElem = document.getElementById(className);
    if (fullscreen) {
      el.classList.add(className);
      if (!styleElem) {
        styleElem = document.createElement("style");
        styleElem.setAttribute("id", className);
        styleElem.textContent = `
          .${className} {
            position: fixed; left: 0; top: 0; right: 0; bottom: 0;
            width: 100% !important; height: 100% !important;
            z-index: ${zIndex};
          }`;
        el.appendChild(styleElem);
      }
    } else {
      el.classList.remove(className);
      if (styleElem) {
        styleElem.remove();
      }
    }
    updateFullscreenState(fullscreen);
  };
  const enterFullscreen = () => {
    const el = getTargetElement(target);
    if (!el) {
      return;
    }
    if (pageFullscreen) {
      togglePageFullscreen(true);
      return;
    }
    if (import_screenfull.default.isEnabled) {
      try {
        import_screenfull.default.request(el);
      } catch (error) {
        console.error(error);
      }
    }
  };
  const exitFullscreen = () => {
    const el = getTargetElement(target);
    if (!el) {
      return;
    }
    if (pageFullscreen) {
      togglePageFullscreen(false);
      return;
    }
    if (import_screenfull.default.isEnabled && import_screenfull.default.element === el) {
      import_screenfull.default.exit();
    }
  };
  const toggleFullscreen = () => {
    if (state) {
      exitFullscreen();
    } else {
      enterFullscreen();
    }
  };
  (0, import_react43.useEffect)(() => {
    if (!import_screenfull.default.isEnabled || pageFullscreen) {
      return;
    }
    import_screenfull.default.on("change", onScreenfullChange);
    return () => {
      import_screenfull.default.off("change", onScreenfullChange);
    };
  }, []);
  return [
    state,
    {
      enterFullscreen: useMemoizedFn_default(enterFullscreen),
      exitFullscreen: useMemoizedFn_default(exitFullscreen),
      toggleFullscreen: useMemoizedFn_default(toggleFullscreen),
      isEnabled: import_screenfull.default.isEnabled
    }
  ];
};
var useFullscreen_default = useFullscreen;

// node_modules/ahooks/es/useFusionTable/fusionAdapter.js
var fieldAdapter = (field) => ({
  getFieldInstance: (name) => field.getNames().includes(name),
  setFieldsValue: field.setValues,
  getFieldsValue: field.getValues,
  resetFields: field.resetToDefault,
  validateFields: (fields, callback) => {
    field.validate(fields, callback);
  }
});
var resultAdapter = (result) => {
  const tableProps = {
    dataSource: result.tableProps.dataSource,
    loading: result.tableProps.loading,
    onSort: (dataIndex, order) => {
      var _a2;
      result.tableProps.onChange({ current: result.pagination.current, pageSize: result.pagination.pageSize }, (_a2 = result.params[0]) === null || _a2 === void 0 ? void 0 : _a2.filters, {
        field: dataIndex,
        order
      });
    },
    onFilter: (filterParams) => {
      var _a2;
      result.tableProps.onChange({ current: result.pagination.current, pageSize: result.pagination.pageSize }, filterParams, (_a2 = result.params[0]) === null || _a2 === void 0 ? void 0 : _a2.sorter);
    }
  };
  const paginationProps = {
    onChange: result.pagination.changeCurrent,
    onPageSizeChange: result.pagination.changePageSize,
    current: result.pagination.current,
    pageSize: result.pagination.pageSize,
    total: result.pagination.total
  };
  return Object.assign(Object.assign({}, result), {
    tableProps,
    paginationProps
  });
};

// node_modules/ahooks/es/useFusionTable/index.js
var useFusionTable = (service, options = {}) => {
  const ret = useAntdTable_default(service, Object.assign(Object.assign({}, options), { form: options.field ? fieldAdapter(options.field) : void 0 }));
  return resultAdapter(ret);
};
var useFusionTable_default = useFusionTable;

// node_modules/ahooks/es/useGetState/index.js
var import_react44 = __jrReq("react");
function useGetState(initialState) {
  const [state, setState] = (0, import_react44.useState)(initialState);
  const stateRef = useLatest_default(state);
  const getState = (0, import_react44.useCallback)(() => stateRef.current, []);
  return [state, setState, getState];
}
var useGetState_default = useGetState;

// node_modules/ahooks/es/useHistoryTravel/index.js
var import_react45 = __jrReq("react");
var dumpIndex = (step, arr) => {
  let index2 = step > 0 ? step - 1 : arr.length + step;
  if (index2 >= arr.length - 1) {
    index2 = arr.length - 1;
  }
  if (index2 < 0) {
    index2 = 0;
  }
  return index2;
};
var split = (step, targetArr) => {
  const index2 = dumpIndex(step, targetArr);
  return {
    _current: targetArr[index2],
    _before: targetArr.slice(0, index2),
    _after: targetArr.slice(index2 + 1)
  };
};
function useHistoryTravel(initialValue, maxLength = 0) {
  const [history, setHistory] = (0, import_react45.useState)({
    present: initialValue,
    past: [],
    future: []
  });
  const { present, past, future } = history;
  const initialValueRef = (0, import_react45.useRef)(initialValue);
  const reset = (...params) => {
    const _initial = params.length > 0 ? params[0] : initialValueRef.current;
    initialValueRef.current = _initial;
    setHistory({
      present: _initial,
      future: [],
      past: []
    });
  };
  const updateValue = (val) => {
    const _past = [...past, present];
    const maxLengthNum = isNumber(maxLength) ? maxLength : Number(maxLength);
    if (maxLengthNum > 0 && _past.length > maxLengthNum) {
      _past.splice(0, 1);
    }
    setHistory({
      present: val,
      future: [],
      past: _past
    });
  };
  const _forward = (step = 1) => {
    if (future.length === 0) {
      return;
    }
    const { _before, _current, _after } = split(step, future);
    setHistory({
      past: [...past, present, ..._before],
      present: _current,
      future: _after
    });
  };
  const _backward = (step = -1) => {
    if (past.length === 0) {
      return;
    }
    const { _before, _current, _after } = split(step, past);
    setHistory({
      past: _before,
      present: _current,
      future: [..._after, present, ...future]
    });
  };
  const go = (step) => {
    const stepNum = isNumber(step) ? step : Number(step);
    if (stepNum === 0) {
      return;
    }
    if (stepNum > 0) {
      return _forward(stepNum);
    }
    _backward(stepNum);
  };
  return {
    value: present,
    backLength: past.length,
    forwardLength: future.length,
    setValue: useMemoizedFn_default(updateValue),
    go: useMemoizedFn_default(go),
    back: useMemoizedFn_default(() => {
      go(-1);
    }),
    forward: useMemoizedFn_default(() => {
      go(1);
    }),
    reset: useMemoizedFn_default(reset)
  };
}

// node_modules/ahooks/es/useHover/index.js
var useHover_default = (target, options) => {
  const { onEnter, onLeave, onChange } = options || {};
  const [state, { setTrue, setFalse }] = useBoolean(false);
  useEventListener_default("mouseenter", () => {
    onEnter === null || onEnter === void 0 ? void 0 : onEnter();
    setTrue();
    onChange === null || onChange === void 0 ? void 0 : onChange(true);
  }, {
    target
  });
  useEventListener_default("mouseleave", () => {
    onLeave === null || onLeave === void 0 ? void 0 : onLeave();
    setFalse();
    onChange === null || onChange === void 0 ? void 0 : onChange(false);
  }, {
    target
  });
  return state;
};

// node_modules/ahooks/es/useInfiniteScroll/index.js
var import_react46 = __jrReq("react");

// node_modules/ahooks/es/utils/rect.js
var getScrollTop = (el) => {
  if (el === document || el === document.documentElement || el === document.body) {
    return Math.max(window.pageYOffset, document.documentElement.scrollTop, document.body.scrollTop);
  }
  return el.scrollTop;
};
var getScrollHeight = (el) => {
  return el.scrollHeight || Math.max(document.documentElement.scrollHeight, document.body.scrollHeight);
};
var getClientHeight = (el) => {
  return el.clientHeight || Math.max(document.documentElement.clientHeight, document.body.clientHeight);
};

// node_modules/ahooks/es/useInfiniteScroll/index.js
var useInfiniteScroll = (service, options = {}) => {
  const { target, isNoMore, threshold = 100, direction = "bottom", reloadDeps = [], manual, onBefore, onSuccess, onError, onFinally } = options;
  const [finalData, setFinalData] = (0, import_react46.useState)();
  const [loadingMore, setLoadingMore] = (0, import_react46.useState)(false);
  const isScrollToTop = direction === "top";
  const lastScrollTop = (0, import_react46.useRef)(void 0);
  const scrollBottom = (0, import_react46.useRef)(0);
  const pendingBottomScrollCheckRef = (0, import_react46.useRef)(false);
  const noMore = (0, import_react46.useMemo)(() => {
    if (!isNoMore) {
      return false;
    }
    return isNoMore(finalData);
  }, [finalData]);
  const { loading, error, run, runAsync, cancel } = useRequest_default2((lastData) => __awaiter(void 0, void 0, void 0, function* () {
    const currentData = yield service(lastData);
    return { currentData, lastData };
  }), {
    manual,
    onFinally: (_, d, e) => {
      setLoadingMore(false);
      onFinally === null || onFinally === void 0 ? void 0 : onFinally(d === null || d === void 0 ? void 0 : d.currentData, e);
    },
    onBefore: () => onBefore === null || onBefore === void 0 ? void 0 : onBefore(),
    onSuccess: (d) => {
      var _a2, _b, _c;
      if (!d.lastData) {
        setFinalData(Object.assign(Object.assign({}, d.currentData), { list: [...(_a2 = d.currentData.list) !== null && _a2 !== void 0 ? _a2 : []] }));
      } else {
        setFinalData(Object.assign(Object.assign({}, d.currentData), { list: isScrollToTop ? [...d.currentData.list, ...(_b = d.lastData.list) !== null && _b !== void 0 ? _b : []] : [...(_c = d.lastData.list) !== null && _c !== void 0 ? _c : [], ...d.currentData.list] }));
      }
      if (isScrollToTop) {
        setTimeout(() => {
          requestAnimationFrame(() => {
            let el = getTargetElement(target);
            el = el === document ? document.documentElement : el;
            if (el) {
              const scrollHeight = getScrollHeight(el);
              el.scrollTo(0, scrollHeight - scrollBottom.current);
            }
          });
        });
      } else {
        pendingBottomScrollCheckRef.current = true;
      }
      onSuccess === null || onSuccess === void 0 ? void 0 : onSuccess(d.currentData);
    },
    onError: (e) => onError === null || onError === void 0 ? void 0 : onError(e)
  });
  const loadMore = useMemoizedFn_default(() => {
    if (noMore) {
      return;
    }
    setLoadingMore(true);
    run(finalData);
  });
  const runAsyncForCurrent = (data) => __awaiter(void 0, void 0, void 0, function* () {
    const res = yield runAsync(data);
    return res.currentData;
  });
  const loadMoreAsync = useMemoizedFn_default(() => {
    if (noMore) {
      return Promise.reject();
    }
    setLoadingMore(true);
    return runAsyncForCurrent(finalData);
  });
  const reload = () => {
    setLoadingMore(false);
    return run();
  };
  const reloadAsync = () => {
    setLoadingMore(false);
    return runAsyncForCurrent();
  };
  const scrollMethod = () => {
    const el = getTargetElement(target);
    if (!el) {
      return;
    }
    const targetEl = el === document ? document.documentElement : el;
    const scrollTop = getScrollTop(targetEl);
    const scrollHeight = getScrollHeight(targetEl);
    const clientHeight = getClientHeight(targetEl);
    if (isScrollToTop) {
      if (lastScrollTop.current !== void 0 && lastScrollTop.current > scrollTop && scrollTop <= threshold) {
        loadMore();
      }
      lastScrollTop.current = scrollTop;
      scrollBottom.current = scrollHeight - scrollTop;
    } else if (scrollHeight - scrollTop <= clientHeight + threshold) {
      loadMore();
    }
  };
  useUpdateEffect_default(() => {
    if (!pendingBottomScrollCheckRef.current) {
      return;
    }
    pendingBottomScrollCheckRef.current = false;
    scrollMethod();
  }, [finalData]);
  useEventListener_default("scroll", () => {
    if (loading || loadingMore) {
      return;
    }
    scrollMethod();
  }, { target });
  useUpdateEffect_default(() => {
    run();
  }, [...reloadDeps]);
  return {
    data: finalData,
    loading: !loadingMore && loading,
    error,
    loadingMore,
    noMore,
    loadMore,
    loadMoreAsync,
    reload: useMemoizedFn_default(reload),
    reloadAsync: useMemoizedFn_default(reloadAsync),
    mutate: setFinalData,
    cancel
  };
};
var useInfiniteScroll_default = useInfiniteScroll;

// node_modules/ahooks/es/useInterval/index.js
var import_react47 = __jrReq("react");
var useInterval = (fn, delay, options = {}) => {
  const timerCallback = useMemoizedFn_default(fn);
  const timerRef = (0, import_react47.useRef)(null);
  const clear = (0, import_react47.useCallback)(() => {
    if (timerRef.current) {
      clearInterval(timerRef.current);
    }
  }, []);
  (0, import_react47.useEffect)(() => {
    if (!isNumber(delay) || delay < 0) {
      return;
    }
    if (options.immediate) {
      timerCallback();
    }
    timerRef.current = setInterval(timerCallback, delay);
    return clear;
  }, [delay, options.immediate]);
  return clear;
};
var useInterval_default = useInterval;

// node_modules/ahooks/es/useInViewport/index.js
var import_react48 = __jrReq("react");
function useInViewport(target, options) {
  const _a2 = options || {}, { callback } = _a2, option = __rest(_a2, ["callback"]);
  const [state, setState] = (0, import_react48.useState)();
  const [ratio, setRatio] = (0, import_react48.useState)();
  useEffectWithTarget_default(() => {
    const targets = Array.isArray(target) ? target : [target];
    const els = targets.map((element) => getTargetElement(element)).filter(Boolean);
    if (!els.length) {
      return;
    }
    const observer2 = new IntersectionObserver((entries) => {
      for (const entry of entries) {
        setRatio(entry.intersectionRatio);
        setState(entry.isIntersecting);
        callback === null || callback === void 0 ? void 0 : callback(entry);
      }
    }, Object.assign(Object.assign({}, option), { root: getTargetElement(options === null || options === void 0 ? void 0 : options.root) }));
    els.forEach((el) => observer2.observe(el));
    return () => {
      observer2.disconnect();
    };
  }, [options === null || options === void 0 ? void 0 : options.rootMargin, options === null || options === void 0 ? void 0 : options.threshold, callback], target);
  return [state, ratio];
}
var useInViewport_default = useInViewport;

// node_modules/ahooks/es/useIsomorphicLayoutEffect/index.js
var import_react49 = __jrReq("react");

// node_modules/ahooks/es/utils/noop.js
var noop = () => {
};
var noop_default = noop;

// node_modules/ahooks/es/useIsomorphicLayoutEffect/index.js
var useIsomorphicLayoutEffect = isBrowser_default ? import_react49.useLayoutEffect : noop_default;
var useIsomorphicLayoutEffect_default = useIsomorphicLayoutEffect;

// node_modules/ahooks/es/utils/useDeepCompareWithTarget.js
var import_react50 = __jrReq("react");
var useDeepCompareEffectWithTarget = (effect, deps, target) => {
  const ref = (0, import_react50.useRef)(void 0);
  const signalRef = (0, import_react50.useRef)(0);
  if (!depsEqual(deps, ref.current)) {
    signalRef.current += 1;
  }
  ref.current = deps;
  useEffectWithTarget_default(effect, [signalRef.current], target);
};
var useDeepCompareWithTarget_default = useDeepCompareEffectWithTarget;

// node_modules/ahooks/es/utils/isAppleDevice.js
var isAppleDevice = /(mac|iphone|ipod|ipad)/i.test(typeof navigator !== "undefined" ? navigator === null || navigator === void 0 ? void 0 : navigator.platform : "");
var isAppleDevice_default = isAppleDevice;

// node_modules/ahooks/es/useKeyPress/index.js
var aliasKeyCodeMap = {
  "0": 48,
  "1": 49,
  "2": 50,
  "3": 51,
  "4": 52,
  "5": 53,
  "6": 54,
  "7": 55,
  "8": 56,
  "9": 57,
  backspace: 8,
  tab: 9,
  enter: 13,
  shift: 16,
  // Legacy alias kept for compatibility; standard name is "control".
  ctrl: 17,
  control: 17,
  alt: 18,
  // Legacy alias kept for compatibility; standard name is "pause".
  pausebreak: 19,
  pause: 19,
  capslock: 20,
  // Legacy alias kept for compatibility; standard name is "escape".
  esc: 27,
  escape: 27,
  // Legacy alias kept for compatibility; standard name is "spacebar" (non-standard but widely used).
  space: 32,
  spacebar: 32,
  pageup: 33,
  pagedown: 34,
  end: 35,
  home: 36,
  // Legacy aliases kept for compatibility; standard names are "arrowleft/arrowup/arrowright/arrowdown".
  leftarrow: 37,
  arrowleft: 37,
  uparrow: 38,
  arrowup: 38,
  rightarrow: 39,
  arrowright: 39,
  downarrow: 40,
  arrowdown: 40,
  insert: 45,
  delete: 46,
  a: 65,
  b: 66,
  c: 67,
  d: 68,
  e: 69,
  f: 70,
  g: 71,
  h: 72,
  i: 73,
  j: 74,
  k: 75,
  l: 76,
  m: 77,
  n: 78,
  o: 79,
  p: 80,
  q: 81,
  r: 82,
  s: 83,
  t: 84,
  u: 85,
  v: 86,
  w: 87,
  x: 88,
  y: 89,
  z: 90,
  leftwindowkey: 91,
  rightwindowkey: 92,
  meta: isAppleDevice_default ? [91, 93] : [91, 92],
  // Legacy alias kept for compatibility; standard name is "contextmenu".
  selectkey: 93,
  contextmenu: 93,
  numpad0: 96,
  numpad1: 97,
  numpad2: 98,
  numpad3: 99,
  numpad4: 100,
  numpad5: 101,
  numpad6: 102,
  numpad7: 103,
  numpad8: 104,
  numpad9: 105,
  multiply: 106,
  add: 107,
  subtract: 109,
  decimalpoint: 110,
  divide: 111,
  f1: 112,
  f2: 113,
  f3: 114,
  f4: 115,
  f5: 116,
  f6: 117,
  f7: 118,
  f8: 119,
  f9: 120,
  f10: 121,
  f11: 122,
  f12: 123,
  numlock: 144,
  scrolllock: 145,
  semicolon: 186,
  equalsign: 187,
  comma: 188,
  dash: 189,
  period: 190,
  forwardslash: 191,
  graveaccent: 192,
  openbracket: 219,
  backslash: 220,
  closebracket: 221,
  singlequote: 222
};
var modifierKey = {
  ctrl: (event) => event.ctrlKey || event.type === "keyup" && event.keyCode === 17,
  shift: (event) => event.shiftKey || event.type === "keyup" && event.keyCode === 16,
  alt: (event) => event.altKey || event.type === "keyup" && event.keyCode === 18,
  meta: (event) => event.metaKey || event.type === "keyup" && aliasKeyCodeMap.meta.includes(event.keyCode)
};
function isValidKeyType(value) {
  return isString(value) || isNumber(value);
}
function countKeyByEvent(event) {
  const countOfModifier = Object.keys(modifierKey).reduce((total, key) => {
    if (modifierKey[key](event)) {
      return total + 1;
    }
    return total;
  }, 0);
  return [16, 17, 18, 91, 92].includes(event.keyCode) ? countOfModifier : countOfModifier + 1;
}
function genFilterKey(event, keyFilter, exactMatch) {
  if (!event.key) {
    return false;
  }
  if (isNumber(keyFilter)) {
    return event.keyCode === keyFilter ? keyFilter : false;
  }
  const genArr = keyFilter.split(".");
  let genLen = 0;
  for (const key of genArr) {
    const genModifier = modifierKey[key];
    const aliasKeyCode = aliasKeyCodeMap[key.toLowerCase()];
    if (genModifier && genModifier(event) || aliasKeyCode && aliasKeyCode === event.keyCode) {
      genLen++;
    }
  }
  if (exactMatch) {
    return genLen === genArr.length && countKeyByEvent(event) === genArr.length ? keyFilter : false;
  }
  return genLen === genArr.length ? keyFilter : false;
}
function genKeyFormatter(keyFilter, exactMatch) {
  if (isFunction(keyFilter)) {
    return keyFilter;
  }
  if (isValidKeyType(keyFilter)) {
    return (event) => genFilterKey(event, keyFilter, exactMatch);
  }
  if (Array.isArray(keyFilter)) {
    return (event) => keyFilter.find((item) => genFilterKey(event, item, exactMatch));
  }
  return () => Boolean(keyFilter);
}
var defaultEvents = ["keydown"];
function useKeyPress(keyFilter, eventHandler, option) {
  const { events = defaultEvents, target, exactMatch = false, useCapture = false } = option || {};
  const eventHandlerRef = useLatest_default(eventHandler);
  const keyFilterRef = useLatest_default(keyFilter);
  useDeepCompareWithTarget_default(() => {
    var _a2;
    const el = getTargetElement(target, window);
    if (!el) {
      return;
    }
    const callbackHandler = (event) => {
      var _a3;
      const keyEvent = event;
      const genGuard = genKeyFormatter(keyFilterRef.current, exactMatch);
      const keyGuard = genGuard(keyEvent);
      const firedKey = isValidKeyType(keyGuard) ? keyGuard : keyEvent.key;
      if (keyGuard) {
        return (_a3 = eventHandlerRef.current) === null || _a3 === void 0 ? void 0 : _a3.call(eventHandlerRef, keyEvent, firedKey);
      }
    };
    for (const eventName of events) {
      (_a2 = el === null || el === void 0 ? void 0 : el.addEventListener) === null || _a2 === void 0 ? void 0 : _a2.call(el, eventName, callbackHandler, useCapture);
    }
    return () => {
      var _a3;
      for (const eventName of events) {
        (_a3 = el === null || el === void 0 ? void 0 : el.removeEventListener) === null || _a3 === void 0 ? void 0 : _a3.call(el, eventName, callbackHandler, useCapture);
      }
    };
  }, [events], target);
}
var useKeyPress_default = useKeyPress;

// node_modules/ahooks/es/createUseStorageState/index.js
var import_react51 = __jrReq("react");
var SYNC_STORAGE_EVENT_NAME = "AHOOKS_SYNC_STORAGE_EVENT_NAME";
var createUseStorageState = (getStorage) => {
  const useStorageState = (key, options = {}) => {
    let storage;
    const { listenStorageChange = false, getInitialValueInEffect = false } = options;
    const serializer = isFunction(options.serializer) ? options.serializer : JSON.stringify;
    const deserializer = isFunction(options.deserializer) ? options.deserializer : JSON.parse;
    const onError = isFunction(options.onError) ? options.onError : console.error;
    try {
      storage = getStorage();
    } catch (err) {
      onError(err);
    }
    const getStoredValue = () => {
      try {
        const raw = storage === null || storage === void 0 ? void 0 : storage.getItem(key);
        if (raw) {
          return deserializer(raw);
        }
      } catch (e) {
        onError(e);
      }
      return getDefaultValue();
    };
    function getDefaultValue() {
      if (isFunction(options.defaultValue)) {
        return options.defaultValue();
      }
      return options.defaultValue;
    }
    const [state, setState] = (0, import_react51.useState)(() => {
      if (getInitialValueInEffect) {
        return getDefaultValue();
      }
      return getStoredValue();
    });
    useMount_default(() => {
      if (!getInitialValueInEffect) {
        return;
      }
      setState(getStoredValue());
    });
    const stateRef = (0, import_react51.useRef)(state);
    stateRef.current = state;
    useUpdateEffect_default(() => {
      const nextState = getStoredValue();
      if (Object.is(nextState, stateRef.current)) {
        return;
      }
      stateRef.current = nextState;
      setState(nextState);
    }, [key]);
    const updateState = (value) => {
      const previousState = stateRef.current;
      const currentState = isFunction(value) ? value(previousState) : value;
      if (Object.is(currentState, previousState)) {
        return;
      }
      if (!listenStorageChange) {
        stateRef.current = currentState;
        setState(currentState);
      }
      try {
        let newValue;
        const oldValue = storage === null || storage === void 0 ? void 0 : storage.getItem(key);
        if (isUndef(currentState)) {
          newValue = null;
          storage === null || storage === void 0 ? void 0 : storage.removeItem(key);
        } else {
          newValue = serializer(currentState);
          storage === null || storage === void 0 ? void 0 : storage.setItem(key, newValue);
        }
        dispatchEvent(
          // send custom event to communicate within same page
          // importantly this should not be a StorageEvent since those cannot
          // be constructed with a non-built-in storage area
          new CustomEvent(SYNC_STORAGE_EVENT_NAME, {
            detail: {
              key,
              newValue,
              oldValue,
              storageArea: storage
            }
          })
        );
      } catch (e) {
        onError(e);
      }
    };
    const syncState = (event) => {
      if (event.key !== key || event.storageArea !== storage) {
        return;
      }
      const nextState = getStoredValue();
      if (Object.is(nextState, stateRef.current)) {
        return;
      }
      stateRef.current = nextState;
      setState(nextState);
    };
    const syncStateFromCustomEvent = (event) => {
      syncState(event.detail);
    };
    useEventListener_default("storage", syncState, {
      enable: listenStorageChange
    });
    useEventListener_default(SYNC_STORAGE_EVENT_NAME, syncStateFromCustomEvent, {
      enable: listenStorageChange
    });
    return [state, useMemoizedFn_default(updateState)];
  };
  return useStorageState;
};

// node_modules/ahooks/es/useLocalStorageState/index.js
var useLocalStorageState = createUseStorageState(() => isBrowser_default ? localStorage : void 0);
var useLocalStorageState_default = useLocalStorageState;

// node_modules/ahooks/es/useLockFn/index.js
var import_react52 = __jrReq("react");
function useLockFn(fn) {
  const lockRef = (0, import_react52.useRef)(false);
  return (0, import_react52.useCallback)((...args) => __awaiter(this, void 0, void 0, function* () {
    if (lockRef.current) {
      return;
    }
    lockRef.current = true;
    try {
      const ret = yield fn(...args);
      return ret;
    } catch (e) {
      throw e;
    } finally {
      lockRef.current = false;
    }
  }), [fn]);
}
var useLockFn_default = useLockFn;

// node_modules/ahooks/es/useLongPress/index.js
var import_react53 = __jrReq("react");
function useLongPress(onLongPress, target, { delay = 300, moveThreshold, onClick, onLongPressEnd } = {}) {
  const onLongPressRef = useLatest_default(onLongPress);
  const onClickRef = useLatest_default(onClick);
  const onLongPressEndRef = useLatest_default(onLongPressEnd);
  const timerRef = (0, import_react53.useRef)(void 0);
  const isTriggeredRef = (0, import_react53.useRef)(false);
  const pervPositionRef = (0, import_react53.useRef)({ x: 0, y: 0 });
  const activePointerIdRef = (0, import_react53.useRef)(void 0);
  const mousePressed = (0, import_react53.useRef)(false);
  const touchPressed = (0, import_react53.useRef)(false);
  const hasMoveThreshold = !!((moveThreshold === null || moveThreshold === void 0 ? void 0 : moveThreshold.x) && moveThreshold.x > 0 || (moveThreshold === null || moveThreshold === void 0 ? void 0 : moveThreshold.y) && moveThreshold.y > 0);
  useEffectWithTarget_default(() => {
    const targetElement = getTargetElement(target);
    if (!(targetElement === null || targetElement === void 0 ? void 0 : targetElement.addEventListener)) {
      return;
    }
    const supportPointerEvent = typeof window !== "undefined" && typeof window.PointerEvent === "function";
    const overThreshold = (event) => {
      const { clientX, clientY } = getClientPosition(event);
      const offsetX = Math.abs(clientX - pervPositionRef.current.x);
      const offsetY = Math.abs(clientY - pervPositionRef.current.y);
      return !!((moveThreshold === null || moveThreshold === void 0 ? void 0 : moveThreshold.x) && offsetX > moveThreshold.x || (moveThreshold === null || moveThreshold === void 0 ? void 0 : moveThreshold.y) && offsetY > moveThreshold.y);
    };
    function getClientPosition(event) {
      if ("TouchEvent" in window && event instanceof TouchEvent) {
        return {
          clientX: event.touches[0].clientX,
          clientY: event.touches[0].clientY
        };
      }
      if (event instanceof MouseEvent) {
        return {
          clientX: event.clientX,
          clientY: event.clientY
        };
      }
      return { clientX: 0, clientY: 0 };
    }
    const createTimer = (event) => {
      timerRef.current = setTimeout(() => {
        onLongPressRef.current(event);
        isTriggeredRef.current = true;
      }, delay);
    };
    const clearTimer2 = () => {
      if (timerRef.current) {
        clearTimeout(timerRef.current);
        timerRef.current = void 0;
      }
    };
    const finishPress = (event, triggerClick = true) => {
      var _a2;
      clearTimer2();
      if (isTriggeredRef.current) {
        (_a2 = onLongPressEndRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onLongPressEndRef, event);
      } else if (triggerClick && onClickRef.current) {
        onClickRef.current(event);
      }
      isTriggeredRef.current = false;
    };
    const onPointerDown = (event) => {
      if (activePointerIdRef.current !== void 0 || event.isPrimary === false) {
        return;
      }
      activePointerIdRef.current = event.pointerId;
      if (hasMoveThreshold) {
        pervPositionRef.current.x = event.clientX;
        pervPositionRef.current.y = event.clientY;
      }
      createTimer(event);
    };
    const onPointerUp = (event) => {
      if (activePointerIdRef.current !== event.pointerId) {
        return;
      }
      activePointerIdRef.current = void 0;
      finishPress(event);
    };
    const onPointerCancel = (event) => {
      if (activePointerIdRef.current !== event.pointerId) {
        return;
      }
      activePointerIdRef.current = void 0;
      finishPress(event, false);
    };
    const onPointerMove = (event) => {
      if (activePointerIdRef.current === event.pointerId) {
        onMove(event);
      }
    };
    const onTouchStart = (event) => {
      if (touchPressed.current) {
        return;
      }
      touchPressed.current = true;
      if (hasMoveThreshold) {
        const { clientX, clientY } = getClientPosition(event);
        pervPositionRef.current.x = clientX;
        pervPositionRef.current.y = clientY;
      }
      createTimer(event);
    };
    const onMouseDown = (event) => {
      var _a2;
      if ((_a2 = event === null || event === void 0 ? void 0 : event.sourceCapabilities) === null || _a2 === void 0 ? void 0 : _a2.firesTouchEvents) {
        return;
      }
      mousePressed.current = true;
      if (hasMoveThreshold) {
        pervPositionRef.current.x = event.clientX;
        pervPositionRef.current.y = event.clientY;
      }
      createTimer(event);
    };
    const onMove = (event) => {
      if (timerRef.current && overThreshold(event)) {
        clearTimer2();
      }
    };
    const onTouchEnd = (event) => {
      if (!touchPressed.current) {
        return;
      }
      touchPressed.current = false;
      finishPress(event);
    };
    const onMouseUp = (event) => {
      var _a2;
      if ((_a2 = event === null || event === void 0 ? void 0 : event.sourceCapabilities) === null || _a2 === void 0 ? void 0 : _a2.firesTouchEvents) {
        return;
      }
      if (!mousePressed.current) {
        return;
      }
      mousePressed.current = false;
      finishPress(event);
    };
    const onMouseLeave = (event) => {
      if (!mousePressed.current) {
        return;
      }
      mousePressed.current = false;
      finishPress(event, false);
    };
    if (supportPointerEvent) {
      targetElement.addEventListener("pointerdown", onPointerDown);
      targetElement.addEventListener("pointerup", onPointerUp);
      targetElement.addEventListener("pointerleave", onPointerCancel);
      targetElement.addEventListener("pointercancel", onPointerCancel);
      if (hasMoveThreshold) {
        targetElement.addEventListener("pointermove", onPointerMove);
      }
      return () => {
        clearTimer2();
        isTriggeredRef.current = false;
        activePointerIdRef.current = void 0;
        targetElement.removeEventListener("pointerdown", onPointerDown);
        targetElement.removeEventListener("pointerup", onPointerUp);
        targetElement.removeEventListener("pointerleave", onPointerCancel);
        targetElement.removeEventListener("pointercancel", onPointerCancel);
        if (hasMoveThreshold) {
          targetElement.removeEventListener("pointermove", onPointerMove);
        }
      };
    }
    const onTouchCancel = (event) => {
      if (!touchPressed.current) {
        return;
      }
      touchPressed.current = false;
      finishPress(event, false);
    };
    targetElement.addEventListener("mousedown", onMouseDown);
    targetElement.addEventListener("mouseup", onMouseUp);
    targetElement.addEventListener("mouseleave", onMouseLeave);
    targetElement.addEventListener("touchstart", onTouchStart);
    targetElement.addEventListener("touchend", onTouchEnd);
    targetElement.addEventListener("touchcancel", onTouchCancel);
    if (hasMoveThreshold) {
      targetElement.addEventListener("mousemove", onMove);
      targetElement.addEventListener("touchmove", onMove);
    }
    return () => {
      clearTimer2();
      isTriggeredRef.current = false;
      targetElement.removeEventListener("mousedown", onMouseDown);
      targetElement.removeEventListener("mouseup", onMouseUp);
      targetElement.removeEventListener("mouseleave", onMouseLeave);
      targetElement.removeEventListener("touchstart", onTouchStart);
      targetElement.removeEventListener("touchend", onTouchEnd);
      targetElement.removeEventListener("touchcancel", onTouchCancel);
      if (hasMoveThreshold) {
        targetElement.removeEventListener("mousemove", onMove);
        targetElement.removeEventListener("touchmove", onMove);
      }
    };
  }, [], target);
}
var useLongPress_default = useLongPress;

// node_modules/ahooks/es/useMap/index.js
var import_react54 = __jrReq("react");
function useMap(initialValue) {
  const getInitValue = () => new Map(initialValue);
  const [map, setMap] = (0, import_react54.useState)(getInitValue);
  const set = (key, entry) => {
    setMap((prev) => {
      const temp = new Map(prev);
      temp.set(key, entry);
      return temp;
    });
  };
  const setAll = (newMap) => {
    setMap(new Map(newMap));
  };
  const remove = (key) => {
    setMap((prev) => {
      const temp = new Map(prev);
      temp.delete(key);
      return temp;
    });
  };
  const reset = () => setMap(getInitValue());
  const get = (key) => map.get(key);
  return [
    map,
    {
      set: useMemoizedFn_default(set),
      setAll: useMemoizedFn_default(setAll),
      remove: useMemoizedFn_default(remove),
      reset: useMemoizedFn_default(reset),
      get: useMemoizedFn_default(get)
    }
  ];
}
var useMap_default = useMap;

// node_modules/ahooks/es/useRafState/index.js
var import_react55 = __jrReq("react");
function useRafState(initialState) {
  const ref = (0, import_react55.useRef)(0);
  const [state, setState] = (0, import_react55.useState)(initialState);
  const setRafState = (0, import_react55.useCallback)((value) => {
    cancelAnimationFrame(ref.current);
    ref.current = requestAnimationFrame(() => {
      setState(value);
    });
  }, []);
  useUnmount_default(() => {
    cancelAnimationFrame(ref.current);
  });
  return [state, setRafState];
}
var useRafState_default = useRafState;

// node_modules/ahooks/es/useMouse/index.js
var initState = {
  screenX: NaN,
  screenY: NaN,
  clientX: NaN,
  clientY: NaN,
  pageX: NaN,
  pageY: NaN,
  elementX: NaN,
  elementY: NaN,
  elementH: NaN,
  elementW: NaN,
  elementPosX: NaN,
  elementPosY: NaN
};
var useMouse_default = (target) => {
  const [state, setState] = useRafState_default(initState);
  useEventListener_default("mousemove", (event) => {
    const { screenX, screenY, clientX, clientY, pageX, pageY } = event;
    const newState = {
      screenX,
      screenY,
      clientX,
      clientY,
      pageX,
      pageY,
      elementX: NaN,
      elementY: NaN,
      elementH: NaN,
      elementW: NaN,
      elementPosX: NaN,
      elementPosY: NaN
    };
    const targetElement = getTargetElement(target);
    if (targetElement) {
      const { left, top, width, height } = targetElement.getBoundingClientRect();
      newState.elementPosX = left + window.pageXOffset;
      newState.elementPosY = top + window.pageYOffset;
      newState.elementX = pageX - newState.elementPosX;
      newState.elementY = pageY - newState.elementPosY;
      newState.elementW = width;
      newState.elementH = height;
    }
    setState(newState);
  }, {
    target: () => document
  });
  return state;
};

// node_modules/ahooks/es/useNetwork/index.js
var import_react56 = __jrReq("react");
var NetworkEventType;
(function(NetworkEventType2) {
  NetworkEventType2["ONLINE"] = "online";
  NetworkEventType2["OFFLINE"] = "offline";
  NetworkEventType2["CHANGE"] = "change";
})(NetworkEventType || (NetworkEventType = {}));
function getConnection() {
  const nav = navigator;
  if (!isObject(nav)) {
    return null;
  }
  return nav.connection || nav.mozConnection || nav.webkitConnection;
}
function getConnectionProperty() {
  const c = getConnection();
  if (!c) {
    return {};
  }
  return {
    rtt: c.rtt,
    type: c.type,
    saveData: c.saveData,
    downlink: c.downlink,
    downlinkMax: c.downlinkMax,
    effectiveType: c.effectiveType
  };
}
function useNetwork() {
  const [state, setState] = (0, import_react56.useState)(() => {
    return Object.assign({ since: void 0, online: navigator === null || navigator === void 0 ? void 0 : navigator.onLine }, getConnectionProperty());
  });
  (0, import_react56.useEffect)(() => {
    const onOnline = () => {
      setState((prevState) => Object.assign(Object.assign({}, prevState), { online: true, since: /* @__PURE__ */ new Date() }));
    };
    const onOffline = () => {
      setState((prevState) => Object.assign(Object.assign({}, prevState), { online: false, since: /* @__PURE__ */ new Date() }));
    };
    const onConnectionChange = () => {
      setState((prevState) => Object.assign(Object.assign({}, prevState), getConnectionProperty()));
    };
    window.addEventListener(NetworkEventType.ONLINE, onOnline);
    window.addEventListener(NetworkEventType.OFFLINE, onOffline);
    const connection = getConnection();
    connection === null || connection === void 0 ? void 0 : connection.addEventListener(NetworkEventType.CHANGE, onConnectionChange);
    return () => {
      window.removeEventListener(NetworkEventType.ONLINE, onOnline);
      window.removeEventListener(NetworkEventType.OFFLINE, onOffline);
      connection === null || connection === void 0 ? void 0 : connection.removeEventListener(NetworkEventType.CHANGE, onConnectionChange);
    };
  }, []);
  return state;
}
var useNetwork_default = useNetwork;

// node_modules/ahooks/es/usePrevious/index.js
var import_react57 = __jrReq("react");
var defaultShouldUpdate = (a, b) => !Object.is(a, b);
function usePrevious(state, shouldUpdate = defaultShouldUpdate) {
  const prevRef = (0, import_react57.useRef)(void 0);
  const curRef = (0, import_react57.useRef)(void 0);
  if (shouldUpdate(curRef.current, state)) {
    prevRef.current = curRef.current;
    curRef.current = state;
  }
  return prevRef.current;
}
var usePrevious_default = usePrevious;

// node_modules/ahooks/es/useRafInterval/index.js
var import_react58 = __jrReq("react");
var setRafInterval = (callback, delay = 0) => {
  if (typeof requestAnimationFrame === "undefined") {
    return {
      id: setInterval(callback, delay)
    };
  }
  let start = Date.now();
  const handle = {
    id: 0
  };
  const loop = () => {
    const current = Date.now();
    handle.id = requestAnimationFrame(loop);
    if (current - start >= delay) {
      callback();
      start = Date.now();
    }
  };
  handle.id = requestAnimationFrame(loop);
  return handle;
};
var cancelAnimationFrameIsNotDefined = (t) => {
  return typeof cancelAnimationFrame === "undefined";
};
var clearRafInterval = (handle) => {
  if (cancelAnimationFrameIsNotDefined(handle.id)) {
    return clearInterval(handle.id);
  }
  cancelAnimationFrame(handle.id);
};
function useRafInterval(fn, delay, options) {
  const immediate = options === null || options === void 0 ? void 0 : options.immediate;
  const fnRef = useLatest_default(fn);
  const timerRef = (0, import_react58.useRef)(void 0);
  const clear = (0, import_react58.useCallback)(() => {
    if (timerRef.current) {
      clearRafInterval(timerRef.current);
    }
  }, []);
  (0, import_react58.useEffect)(() => {
    if (!isNumber(delay) || delay < 0) {
      return;
    }
    if (immediate) {
      fnRef.current();
    }
    timerRef.current = setRafInterval(() => {
      fnRef.current();
    }, delay);
    return clear;
  }, [delay]);
  return clear;
}
var useRafInterval_default = useRafInterval;

// node_modules/ahooks/es/useRafTimeout/index.js
var import_react59 = __jrReq("react");
var setRafTimeout = (callback, delay = 0) => {
  if (typeof requestAnimationFrame === "undefined") {
    return {
      id: setTimeout(callback, delay)
    };
  }
  const handle = {
    id: 0
  };
  const startTime = Date.now();
  const loop = () => {
    const current = Date.now();
    if (current - startTime >= delay) {
      callback();
    } else {
      handle.id = requestAnimationFrame(loop);
    }
  };
  handle.id = requestAnimationFrame(loop);
  return handle;
};
var cancelAnimationFrameIsNotDefined2 = (t) => {
  return typeof cancelAnimationFrame === "undefined";
};
var clearRafTimeout = (handle) => {
  if (cancelAnimationFrameIsNotDefined2(handle.id)) {
    return clearTimeout(handle.id);
  }
  cancelAnimationFrame(handle.id);
};
function useRafTimeout(fn, delay) {
  const fnRef = useLatest_default(fn);
  const timerRef = (0, import_react59.useRef)(void 0);
  const clear = (0, import_react59.useCallback)(() => {
    if (timerRef.current) {
      clearRafTimeout(timerRef.current);
    }
  }, []);
  (0, import_react59.useEffect)(() => {
    if (!isNumber(delay) || delay < 0) {
      return;
    }
    timerRef.current = setRafTimeout(() => {
      fnRef.current();
    }, delay);
    return clear;
  }, [delay]);
  return clear;
}
var useRafTimeout_default = useRafTimeout;

// node_modules/ahooks/es/useReactive/index.js
var import_react60 = __jrReq("react");
var import_isPlainObject = __toESM(require_isPlainObject());
var proxyMap = /* @__PURE__ */ new WeakMap();
var rawMap = /* @__PURE__ */ new WeakMap();
function observer(initialVal, cb) {
  const existingProxy = proxyMap.get(initialVal);
  if (existingProxy) {
    return existingProxy;
  }
  if (rawMap.has(initialVal)) {
    return initialVal;
  }
  const proxy = new Proxy(initialVal, {
    get(target, key, receiver) {
      const res = Reflect.get(target, key, receiver);
      const descriptor = Reflect.getOwnPropertyDescriptor(target, key);
      if (!(descriptor === null || descriptor === void 0 ? void 0 : descriptor.configurable) && !(descriptor === null || descriptor === void 0 ? void 0 : descriptor.writable)) {
        return res;
      }
      return (0, import_isPlainObject.default)(res) || Array.isArray(res) ? observer(res, cb) : res;
    },
    set(target, key, val) {
      const ret = Reflect.set(target, key, val);
      cb();
      return ret;
    },
    deleteProperty(target, key) {
      const ret = Reflect.deleteProperty(target, key);
      cb();
      return ret;
    }
  });
  proxyMap.set(initialVal, proxy);
  rawMap.set(proxy, initialVal);
  return proxy;
}
function useReactive(initialState) {
  const update = useUpdate_default();
  const stateRef = (0, import_react60.useRef)(initialState);
  const state = useCreation_default(() => {
    return observer(stateRef.current, () => {
      update();
    });
  }, []);
  return state;
}
var useReactive_default = useReactive;

// node_modules/ahooks/es/useResetState/index.js
var import_react61 = __jrReq("react");
var useResetState = (initialState) => {
  const initialStateRef = (0, import_react61.useRef)(initialState);
  const initialStateMemo = useCreation_default(() => isFunction(initialStateRef.current) ? initialStateRef.current() : initialStateRef.current, []);
  const [state, setState] = (0, import_react61.useState)(initialStateMemo);
  const resetState = useMemoizedFn_default(() => {
    setState(initialStateMemo);
  });
  return [state, setState, resetState];
};
var useResetState_default = useResetState;

// node_modules/ahooks/es/useResponsive/index.js
var import_react62 = __jrReq("react");
var subscribers = /* @__PURE__ */ new Set();
var info;
var responsiveConfig = {
  xs: 0,
  sm: 576,
  md: 768,
  lg: 992,
  xl: 1200
};
function handleResize() {
  const oldInfo = info;
  calculate();
  if (oldInfo === info) {
    return;
  }
  for (const subscriber of subscribers) {
    subscriber();
  }
}
var listening = false;
function calculate() {
  const width = window.innerWidth;
  const newInfo = {};
  let shouldUpdate = false;
  for (const key of Object.keys(responsiveConfig)) {
    newInfo[key] = width >= responsiveConfig[key];
    if (newInfo[key] !== info[key]) {
      shouldUpdate = true;
    }
  }
  if (shouldUpdate) {
    info = newInfo;
  }
}
function configResponsive(config) {
  responsiveConfig = config;
  if (info)
    calculate();
}
function useResponsive() {
  if (isBrowser_default && !listening) {
    info = {};
    calculate();
    window.addEventListener("resize", handleResize);
    listening = true;
  }
  const [state, setState] = (0, import_react62.useState)(info);
  (0, import_react62.useEffect)(() => {
    if (!isBrowser_default) {
      return;
    }
    if (!listening) {
      window.addEventListener("resize", handleResize);
    }
    const subscriber = () => {
      setState(info);
    };
    subscribers.add(subscriber);
    return () => {
      subscribers.delete(subscriber);
      if (subscribers.size === 0) {
        window.removeEventListener("resize", handleResize);
        listening = false;
      }
    };
  }, []);
  return state;
}
var useResponsive_default = useResponsive;

// node_modules/ahooks/es/useSafeState/index.js
var import_react64 = __jrReq("react");

// node_modules/ahooks/es/useUnmountedRef/index.js
var import_react63 = __jrReq("react");
var useUnmountedRef = () => {
  const unmountedRef = (0, import_react63.useRef)(false);
  (0, import_react63.useEffect)(() => {
    unmountedRef.current = false;
    return () => {
      unmountedRef.current = true;
    };
  }, []);
  return unmountedRef;
};
var useUnmountedRef_default = useUnmountedRef;

// node_modules/ahooks/es/useSafeState/index.js
function useSafeState(initialState) {
  const unmountedRef = useUnmountedRef_default();
  const [state, setState] = (0, import_react64.useState)(initialState);
  const setCurrentState = (0, import_react64.useCallback)((currentState) => {
    if (unmountedRef.current) {
      return;
    }
    setState(currentState);
  }, []);
  return [state, setCurrentState];
}
var useSafeState_default = useSafeState;

// node_modules/ahooks/es/useScroll/index.js
function useScroll(target, shouldUpdate = () => true) {
  const [position, setPosition] = useRafState_default();
  const shouldUpdateRef = useLatest_default(shouldUpdate);
  useEffectWithTarget_default(() => {
    const el = getTargetElement(target, document);
    if (!el) {
      return;
    }
    const updatePosition = () => {
      let newPosition;
      if (el === document) {
        if (document.scrollingElement) {
          newPosition = {
            left: document.scrollingElement.scrollLeft,
            top: document.scrollingElement.scrollTop
          };
        } else {
          newPosition = {
            left: Math.max(window.pageXOffset, document.documentElement.scrollLeft, document.body.scrollLeft),
            top: Math.max(window.pageYOffset, document.documentElement.scrollTop, document.body.scrollTop)
          };
        }
      } else {
        newPosition = {
          left: el.scrollLeft,
          top: el.scrollTop
        };
      }
      if (shouldUpdateRef.current(newPosition)) {
        setPosition(newPosition);
      }
    };
    updatePosition();
    el.addEventListener("scroll", updatePosition);
    return () => {
      el.removeEventListener("scroll", updatePosition);
    };
  }, [], target);
  return position;
}
var useScroll_default = useScroll;

// node_modules/ahooks/es/useSelections/index.js
var import_isPlainObject2 = __toESM(require_isPlainObject());
var import_react65 = __jrReq("react");
function useSelections(items, options) {
  var _a2, _b;
  let defaultSelected = [];
  let itemKey;
  if (Array.isArray(options)) {
    defaultSelected = options;
  } else if ((0, import_isPlainObject2.default)(options)) {
    defaultSelected = (_a2 = options === null || options === void 0 ? void 0 : options.defaultSelected) !== null && _a2 !== void 0 ? _a2 : defaultSelected;
    itemKey = (_b = options === null || options === void 0 ? void 0 : options.itemKey) !== null && _b !== void 0 ? _b : itemKey;
  }
  const getKey = (item) => {
    if (isFunction(itemKey)) {
      return itemKey(item);
    }
    if (isString(itemKey) && (0, import_isPlainObject2.default)(item)) {
      return item[itemKey];
    }
    return item;
  };
  const [selected, setSelected] = (0, import_react65.useState)(defaultSelected);
  const selectedMap = (0, import_react65.useMemo)(() => {
    const keyToItemMap = /* @__PURE__ */ new Map();
    if (!Array.isArray(selected)) {
      return keyToItemMap;
    }
    selected.forEach((item) => {
      keyToItemMap.set(getKey(item), item);
    });
    return keyToItemMap;
  }, [selected]);
  const isSelected = (item) => selectedMap.has(getKey(item));
  const select = (item) => {
    selectedMap.set(getKey(item), item);
    setSelected(Array.from(selectedMap.values()));
  };
  const unSelect = (item) => {
    selectedMap.delete(getKey(item));
    setSelected(Array.from(selectedMap.values()));
  };
  const toggle = (item) => {
    if (isSelected(item)) {
      unSelect(item);
    } else {
      select(item);
    }
  };
  const selectAll = () => {
    items.forEach((item) => {
      selectedMap.set(getKey(item), item);
    });
    setSelected(Array.from(selectedMap.values()));
  };
  const unSelectAll = () => {
    items.forEach((item) => {
      selectedMap.delete(getKey(item));
    });
    setSelected(Array.from(selectedMap.values()));
  };
  const noneSelected = (0, import_react65.useMemo)(() => items.every((item) => !selectedMap.has(getKey(item))), [items, selectedMap]);
  const allSelected = (0, import_react65.useMemo)(() => items.every((item) => selectedMap.has(getKey(item))) && !noneSelected, [items, selectedMap, noneSelected]);
  const partiallySelected = (0, import_react65.useMemo)(() => !noneSelected && !allSelected, [noneSelected, allSelected]);
  const toggleAll = () => allSelected ? unSelectAll() : selectAll();
  const clearAll = () => {
    selectedMap.clear();
    setSelected([]);
  };
  return {
    selected,
    noneSelected,
    allSelected,
    partiallySelected,
    setSelected,
    isSelected,
    select: useMemoizedFn_default(select),
    unSelect: useMemoizedFn_default(unSelect),
    toggle: useMemoizedFn_default(toggle),
    selectAll: useMemoizedFn_default(selectAll),
    unSelectAll: useMemoizedFn_default(unSelectAll),
    clearAll: useMemoizedFn_default(clearAll),
    toggleAll: useMemoizedFn_default(toggleAll)
  };
}
var useSelections_default = useSelections;

// node_modules/ahooks/es/useSessionStorageState/index.js
var useSessionStorageState = createUseStorageState(() => isBrowser_default ? sessionStorage : void 0);
var useSessionStorageState_default = useSessionStorageState;

// node_modules/ahooks/es/useSet/index.js
var import_react66 = __jrReq("react");
function useSet(initialValue) {
  const getInitValue = () => new Set(initialValue);
  const [set, setSet] = (0, import_react66.useState)(getInitValue);
  const updateSet = (updater) => {
    setSet((prevSet) => updater(new Set(prevSet)));
  };
  const add = (key) => {
    if (set.has(key)) {
      return;
    }
    updateSet((newSet) => {
      newSet.add(key);
      return newSet;
    });
  };
  const remove = (key) => {
    if (!set.has(key)) {
      return;
    }
    updateSet((newSet) => {
      newSet.delete(key);
      return newSet;
    });
  };
  const reset = () => setSet(getInitValue());
  return [
    set,
    {
      add: useMemoizedFn_default(add),
      remove: useMemoizedFn_default(remove),
      reset: useMemoizedFn_default(reset)
    }
  ];
}
var useSet_default = useSet;

// node_modules/ahooks/es/useSetState/index.js
var import_react67 = __jrReq("react");
function useSetState(initialState) {
  const [state, setState] = (0, import_react67.useState)(initialState !== null && initialState !== void 0 ? initialState : {});
  const setMergeState = useMemoizedFn_default((patch) => {
    setState((prevState) => {
      const newState = isFunction(patch) ? patch(prevState) : patch;
      return newState ? Object.assign(Object.assign({}, prevState), newState) : prevState;
    });
  });
  return [state, setMergeState];
}
var useSetState_default = useSetState;

// node_modules/resize-observer-polyfill/dist/ResizeObserver.es.js
var MapShim = (function() {
  if (typeof Map !== "undefined") {
    return Map;
  }
  function getIndex(arr, key) {
    var result = -1;
    arr.some(function(entry, index2) {
      if (entry[0] === key) {
        result = index2;
        return true;
      }
      return false;
    });
    return result;
  }
  return (
    /** @class */
    (function() {
      function class_1() {
        this.__entries__ = [];
      }
      Object.defineProperty(class_1.prototype, "size", {
        /**
         * @returns {boolean}
         */
        get: function() {
          return this.__entries__.length;
        },
        enumerable: true,
        configurable: true
      });
      class_1.prototype.get = function(key) {
        var index2 = getIndex(this.__entries__, key);
        var entry = this.__entries__[index2];
        return entry && entry[1];
      };
      class_1.prototype.set = function(key, value) {
        var index2 = getIndex(this.__entries__, key);
        if (~index2) {
          this.__entries__[index2][1] = value;
        } else {
          this.__entries__.push([key, value]);
        }
      };
      class_1.prototype.delete = function(key) {
        var entries = this.__entries__;
        var index2 = getIndex(entries, key);
        if (~index2) {
          entries.splice(index2, 1);
        }
      };
      class_1.prototype.has = function(key) {
        return !!~getIndex(this.__entries__, key);
      };
      class_1.prototype.clear = function() {
        this.__entries__.splice(0);
      };
      class_1.prototype.forEach = function(callback, ctx) {
        if (ctx === void 0) {
          ctx = null;
        }
        for (var _i = 0, _a2 = this.__entries__; _i < _a2.length; _i++) {
          var entry = _a2[_i];
          callback.call(ctx, entry[1], entry[0]);
        }
      };
      return class_1;
    })()
  );
})();
var isBrowser2 = typeof window !== "undefined" && typeof document !== "undefined" && window.document === document;
var global$1 = (function() {
  if (typeof global !== "undefined" && global.Math === Math) {
    return global;
  }
  if (typeof self !== "undefined" && self.Math === Math) {
    return self;
  }
  if (typeof window !== "undefined" && window.Math === Math) {
    return window;
  }
  return Function("return this")();
})();
var requestAnimationFrame$1 = (function() {
  if (typeof requestAnimationFrame === "function") {
    return requestAnimationFrame.bind(global$1);
  }
  return function(callback) {
    return setTimeout(function() {
      return callback(Date.now());
    }, 1e3 / 60);
  };
})();
var trailingTimeout = 2;
function throttle2(callback, delay) {
  var leadingCall = false, trailingCall = false, lastCallTime = 0;
  function resolvePending() {
    if (leadingCall) {
      leadingCall = false;
      callback();
    }
    if (trailingCall) {
      proxy();
    }
  }
  function timeoutCallback() {
    requestAnimationFrame$1(resolvePending);
  }
  function proxy() {
    var timeStamp = Date.now();
    if (leadingCall) {
      if (timeStamp - lastCallTime < trailingTimeout) {
        return;
      }
      trailingCall = true;
    } else {
      leadingCall = true;
      trailingCall = false;
      setTimeout(timeoutCallback, delay);
    }
    lastCallTime = timeStamp;
  }
  return proxy;
}
var REFRESH_DELAY = 20;
var transitionKeys = ["top", "right", "bottom", "left", "width", "height", "size", "weight"];
var mutationObserverSupported = typeof MutationObserver !== "undefined";
var ResizeObserverController = (
  /** @class */
  (function() {
    function ResizeObserverController2() {
      this.connected_ = false;
      this.mutationEventsAdded_ = false;
      this.mutationsObserver_ = null;
      this.observers_ = [];
      this.onTransitionEnd_ = this.onTransitionEnd_.bind(this);
      this.refresh = throttle2(this.refresh.bind(this), REFRESH_DELAY);
    }
    ResizeObserverController2.prototype.addObserver = function(observer2) {
      if (!~this.observers_.indexOf(observer2)) {
        this.observers_.push(observer2);
      }
      if (!this.connected_) {
        this.connect_();
      }
    };
    ResizeObserverController2.prototype.removeObserver = function(observer2) {
      var observers2 = this.observers_;
      var index2 = observers2.indexOf(observer2);
      if (~index2) {
        observers2.splice(index2, 1);
      }
      if (!observers2.length && this.connected_) {
        this.disconnect_();
      }
    };
    ResizeObserverController2.prototype.refresh = function() {
      var changesDetected = this.updateObservers_();
      if (changesDetected) {
        this.refresh();
      }
    };
    ResizeObserverController2.prototype.updateObservers_ = function() {
      var activeObservers = this.observers_.filter(function(observer2) {
        return observer2.gatherActive(), observer2.hasActive();
      });
      activeObservers.forEach(function(observer2) {
        return observer2.broadcastActive();
      });
      return activeObservers.length > 0;
    };
    ResizeObserverController2.prototype.connect_ = function() {
      if (!isBrowser2 || this.connected_) {
        return;
      }
      document.addEventListener("transitionend", this.onTransitionEnd_);
      window.addEventListener("resize", this.refresh);
      if (mutationObserverSupported) {
        this.mutationsObserver_ = new MutationObserver(this.refresh);
        this.mutationsObserver_.observe(document, {
          attributes: true,
          childList: true,
          characterData: true,
          subtree: true
        });
      } else {
        document.addEventListener("DOMSubtreeModified", this.refresh);
        this.mutationEventsAdded_ = true;
      }
      this.connected_ = true;
    };
    ResizeObserverController2.prototype.disconnect_ = function() {
      if (!isBrowser2 || !this.connected_) {
        return;
      }
      document.removeEventListener("transitionend", this.onTransitionEnd_);
      window.removeEventListener("resize", this.refresh);
      if (this.mutationsObserver_) {
        this.mutationsObserver_.disconnect();
      }
      if (this.mutationEventsAdded_) {
        document.removeEventListener("DOMSubtreeModified", this.refresh);
      }
      this.mutationsObserver_ = null;
      this.mutationEventsAdded_ = false;
      this.connected_ = false;
    };
    ResizeObserverController2.prototype.onTransitionEnd_ = function(_a2) {
      var _b = _a2.propertyName, propertyName = _b === void 0 ? "" : _b;
      var isReflowProperty = transitionKeys.some(function(key) {
        return !!~propertyName.indexOf(key);
      });
      if (isReflowProperty) {
        this.refresh();
      }
    };
    ResizeObserverController2.getInstance = function() {
      if (!this.instance_) {
        this.instance_ = new ResizeObserverController2();
      }
      return this.instance_;
    };
    ResizeObserverController2.instance_ = null;
    return ResizeObserverController2;
  })()
);
var defineConfigurable = (function(target, props) {
  for (var _i = 0, _a2 = Object.keys(props); _i < _a2.length; _i++) {
    var key = _a2[_i];
    Object.defineProperty(target, key, {
      value: props[key],
      enumerable: false,
      writable: false,
      configurable: true
    });
  }
  return target;
});
var getWindowOf = (function(target) {
  var ownerGlobal = target && target.ownerDocument && target.ownerDocument.defaultView;
  return ownerGlobal || global$1;
});
var emptyRect = createRectInit(0, 0, 0, 0);
function toFloat(value) {
  return parseFloat(value) || 0;
}
function getBordersSize(styles) {
  var positions = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    positions[_i - 1] = arguments[_i];
  }
  return positions.reduce(function(size, position) {
    var value = styles["border-" + position + "-width"];
    return size + toFloat(value);
  }, 0);
}
function getPaddings(styles) {
  var positions = ["top", "right", "bottom", "left"];
  var paddings = {};
  for (var _i = 0, positions_1 = positions; _i < positions_1.length; _i++) {
    var position = positions_1[_i];
    var value = styles["padding-" + position];
    paddings[position] = toFloat(value);
  }
  return paddings;
}
function getSVGContentRect(target) {
  var bbox = target.getBBox();
  return createRectInit(0, 0, bbox.width, bbox.height);
}
function getHTMLElementContentRect(target) {
  var clientWidth = target.clientWidth, clientHeight = target.clientHeight;
  if (!clientWidth && !clientHeight) {
    return emptyRect;
  }
  var styles = getWindowOf(target).getComputedStyle(target);
  var paddings = getPaddings(styles);
  var horizPad = paddings.left + paddings.right;
  var vertPad = paddings.top + paddings.bottom;
  var width = toFloat(styles.width), height = toFloat(styles.height);
  if (styles.boxSizing === "border-box") {
    if (Math.round(width + horizPad) !== clientWidth) {
      width -= getBordersSize(styles, "left", "right") + horizPad;
    }
    if (Math.round(height + vertPad) !== clientHeight) {
      height -= getBordersSize(styles, "top", "bottom") + vertPad;
    }
  }
  if (!isDocumentElement(target)) {
    var vertScrollbar = Math.round(width + horizPad) - clientWidth;
    var horizScrollbar = Math.round(height + vertPad) - clientHeight;
    if (Math.abs(vertScrollbar) !== 1) {
      width -= vertScrollbar;
    }
    if (Math.abs(horizScrollbar) !== 1) {
      height -= horizScrollbar;
    }
  }
  return createRectInit(paddings.left, paddings.top, width, height);
}
var isSVGGraphicsElement = (function() {
  if (typeof SVGGraphicsElement !== "undefined") {
    return function(target) {
      return target instanceof getWindowOf(target).SVGGraphicsElement;
    };
  }
  return function(target) {
    return target instanceof getWindowOf(target).SVGElement && typeof target.getBBox === "function";
  };
})();
function isDocumentElement(target) {
  return target === getWindowOf(target).document.documentElement;
}
function getContentRect(target) {
  if (!isBrowser2) {
    return emptyRect;
  }
  if (isSVGGraphicsElement(target)) {
    return getSVGContentRect(target);
  }
  return getHTMLElementContentRect(target);
}
function createReadOnlyRect(_a2) {
  var x = _a2.x, y = _a2.y, width = _a2.width, height = _a2.height;
  var Constr = typeof DOMRectReadOnly !== "undefined" ? DOMRectReadOnly : Object;
  var rect = Object.create(Constr.prototype);
  defineConfigurable(rect, {
    x,
    y,
    width,
    height,
    top: y,
    right: x + width,
    bottom: height + y,
    left: x
  });
  return rect;
}
function createRectInit(x, y, width, height) {
  return { x, y, width, height };
}
var ResizeObservation = (
  /** @class */
  (function() {
    function ResizeObservation2(target) {
      this.broadcastWidth = 0;
      this.broadcastHeight = 0;
      this.contentRect_ = createRectInit(0, 0, 0, 0);
      this.target = target;
    }
    ResizeObservation2.prototype.isActive = function() {
      var rect = getContentRect(this.target);
      this.contentRect_ = rect;
      return rect.width !== this.broadcastWidth || rect.height !== this.broadcastHeight;
    };
    ResizeObservation2.prototype.broadcastRect = function() {
      var rect = this.contentRect_;
      this.broadcastWidth = rect.width;
      this.broadcastHeight = rect.height;
      return rect;
    };
    return ResizeObservation2;
  })()
);
var ResizeObserverEntry = (
  /** @class */
  /* @__PURE__ */ (function() {
    function ResizeObserverEntry2(target, rectInit) {
      var contentRect = createReadOnlyRect(rectInit);
      defineConfigurable(this, { target, contentRect });
    }
    return ResizeObserverEntry2;
  })()
);
var ResizeObserverSPI = (
  /** @class */
  (function() {
    function ResizeObserverSPI2(callback, controller, callbackCtx) {
      this.activeObservations_ = [];
      this.observations_ = new MapShim();
      if (typeof callback !== "function") {
        throw new TypeError("The callback provided as parameter 1 is not a function.");
      }
      this.callback_ = callback;
      this.controller_ = controller;
      this.callbackCtx_ = callbackCtx;
    }
    ResizeObserverSPI2.prototype.observe = function(target) {
      if (!arguments.length) {
        throw new TypeError("1 argument required, but only 0 present.");
      }
      if (typeof Element === "undefined" || !(Element instanceof Object)) {
        return;
      }
      if (!(target instanceof getWindowOf(target).Element)) {
        throw new TypeError('parameter 1 is not of type "Element".');
      }
      var observations = this.observations_;
      if (observations.has(target)) {
        return;
      }
      observations.set(target, new ResizeObservation(target));
      this.controller_.addObserver(this);
      this.controller_.refresh();
    };
    ResizeObserverSPI2.prototype.unobserve = function(target) {
      if (!arguments.length) {
        throw new TypeError("1 argument required, but only 0 present.");
      }
      if (typeof Element === "undefined" || !(Element instanceof Object)) {
        return;
      }
      if (!(target instanceof getWindowOf(target).Element)) {
        throw new TypeError('parameter 1 is not of type "Element".');
      }
      var observations = this.observations_;
      if (!observations.has(target)) {
        return;
      }
      observations.delete(target);
      if (!observations.size) {
        this.controller_.removeObserver(this);
      }
    };
    ResizeObserverSPI2.prototype.disconnect = function() {
      this.clearActive();
      this.observations_.clear();
      this.controller_.removeObserver(this);
    };
    ResizeObserverSPI2.prototype.gatherActive = function() {
      var _this = this;
      this.clearActive();
      this.observations_.forEach(function(observation) {
        if (observation.isActive()) {
          _this.activeObservations_.push(observation);
        }
      });
    };
    ResizeObserverSPI2.prototype.broadcastActive = function() {
      if (!this.hasActive()) {
        return;
      }
      var ctx = this.callbackCtx_;
      var entries = this.activeObservations_.map(function(observation) {
        return new ResizeObserverEntry(observation.target, observation.broadcastRect());
      });
      this.callback_.call(ctx, entries, ctx);
      this.clearActive();
    };
    ResizeObserverSPI2.prototype.clearActive = function() {
      this.activeObservations_.splice(0);
    };
    ResizeObserverSPI2.prototype.hasActive = function() {
      return this.activeObservations_.length > 0;
    };
    return ResizeObserverSPI2;
  })()
);
var observers = typeof WeakMap !== "undefined" ? /* @__PURE__ */ new WeakMap() : new MapShim();
var ResizeObserver = (
  /** @class */
  /* @__PURE__ */ (function() {
    function ResizeObserver2(callback) {
      if (!(this instanceof ResizeObserver2)) {
        throw new TypeError("Cannot call a class as a function.");
      }
      if (!arguments.length) {
        throw new TypeError("1 argument required, but only 0 present.");
      }
      var controller = ResizeObserverController.getInstance();
      var observer2 = new ResizeObserverSPI(callback, controller, this);
      observers.set(this, observer2);
    }
    return ResizeObserver2;
  })()
);
[
  "observe",
  "unobserve",
  "disconnect"
].forEach(function(method) {
  ResizeObserver.prototype[method] = function() {
    var _a2;
    return (_a2 = observers.get(this))[method].apply(_a2, arguments);
  };
});
var index = (function() {
  if (typeof global$1.ResizeObserver !== "undefined") {
    return global$1.ResizeObserver;
  }
  return ResizeObserver;
})();
var ResizeObserver_es_default = index;

// node_modules/ahooks/es/utils/useLayoutEffectWithTarget.js
var import_react68 = __jrReq("react");
var useEffectWithTarget2 = createEffectWithTarget_default(import_react68.useLayoutEffect);
var useLayoutEffectWithTarget_default = useEffectWithTarget2;

// node_modules/ahooks/es/utils/useIsomorphicLayoutEffectWithTarget.js
var useIsomorphicLayoutEffectWithTarget = isBrowser_default ? useLayoutEffectWithTarget_default : useEffectWithTarget_default;
var useIsomorphicLayoutEffectWithTarget_default = useIsomorphicLayoutEffectWithTarget;

// node_modules/ahooks/es/useSize/index.js
function useSize(target) {
  const [state, setState] = useRafState_default(() => {
    const el = getTargetElement(target);
    return el ? { width: el.clientWidth, height: el.clientHeight } : void 0;
  });
  useIsomorphicLayoutEffectWithTarget_default(() => {
    const el = getTargetElement(target);
    if (!el) {
      return;
    }
    const resizeObserver = new ResizeObserver_es_default((entries) => {
      entries.forEach((entry) => {
        const { clientWidth, clientHeight } = entry.target;
        setState({ width: clientWidth, height: clientHeight });
      });
    });
    resizeObserver.observe(el);
    return () => {
      resizeObserver.disconnect();
    };
  }, [], target);
  return state;
}
var useSize_default = useSize;

// node_modules/ahooks/es/useTextSelection/index.js
var import_react69 = __jrReq("react");
var initRect = {
  top: NaN,
  left: NaN,
  bottom: NaN,
  right: NaN,
  height: NaN,
  width: NaN
};
var initState2 = Object.assign({ text: "" }, initRect);
function getRectFromSelection(selection) {
  if (!selection) {
    return initRect;
  }
  if (selection.rangeCount < 1) {
    return initRect;
  }
  const range = selection.getRangeAt(0);
  const { height, width, top, left, right, bottom } = range.getBoundingClientRect();
  return {
    height,
    width,
    top,
    left,
    right,
    bottom
  };
}
function useTextSelection(target) {
  const [state, setState] = (0, import_react69.useState)(initState2);
  const stateRef = (0, import_react69.useRef)(state);
  const isInRangeRef = (0, import_react69.useRef)(false);
  stateRef.current = state;
  useEffectWithTarget_default(() => {
    const el = getTargetElement(target, document);
    if (!el) {
      return;
    }
    const mouseupHandler = () => {
      let selObj = null;
      let text = "";
      let rect = initRect;
      if (!window.getSelection) {
        return;
      }
      selObj = window.getSelection();
      text = selObj ? selObj.toString() : "";
      if (text && isInRangeRef.current) {
        rect = getRectFromSelection(selObj);
        setState(Object.assign(Object.assign(Object.assign({}, state), { text }), rect));
      }
    };
    const mousedownHandler = (e) => {
      if (e.button === 2) {
        return;
      }
      if (!window.getSelection) {
        return;
      }
      if (stateRef.current.text) {
        setState(Object.assign({}, initState2));
      }
      isInRangeRef.current = false;
      const selObj = window.getSelection();
      if (!selObj) {
        return;
      }
      selObj.removeAllRanges();
      isInRangeRef.current = el.contains(e.target);
    };
    el.addEventListener("mouseup", mouseupHandler);
    document.addEventListener("mousedown", mousedownHandler);
    return () => {
      el.removeEventListener("mouseup", mouseupHandler);
      document.removeEventListener("mousedown", mousedownHandler);
    };
  }, [], target);
  return state;
}
var useTextSelection_default = useTextSelection;

// node_modules/ahooks/es/useThrottle/index.js
var import_react71 = __jrReq("react");

// node_modules/ahooks/es/useThrottleFn/index.js
var import_throttle2 = __toESM(require_throttle());
var import_react70 = __jrReq("react");
function useThrottleFn(fn, options) {
  var _a2;
  if (isDev_default) {
    if (!isFunction(fn)) {
      console.error(`useThrottleFn expected parameter is a function, got ${typeof fn}`);
    }
  }
  const fnRef = useLatest_default(fn);
  const wait = (_a2 = options === null || options === void 0 ? void 0 : options.wait) !== null && _a2 !== void 0 ? _a2 : 1e3;
  const throttled = (0, import_react70.useMemo)(() => (0, import_throttle2.default)((...args) => {
    return fnRef.current(...args);
  }, wait, options), []);
  useUnmount_default(() => {
    throttled.cancel();
  });
  return {
    run: throttled,
    cancel: throttled.cancel,
    flush: throttled.flush
  };
}
var useThrottleFn_default = useThrottleFn;

// node_modules/ahooks/es/useThrottle/index.js
function useThrottle(value, options) {
  const [throttled, setThrottled] = (0, import_react71.useState)(value);
  const { run } = useThrottleFn_default(() => {
    setThrottled(value);
  }, options);
  (0, import_react71.useEffect)(() => {
    run();
  }, [value]);
  return throttled;
}
var useThrottle_default = useThrottle;

// node_modules/ahooks/es/useThrottleEffect/index.js
var import_react72 = __jrReq("react");
function useThrottleEffect(effect, deps, options) {
  const [flag, setFlag] = (0, import_react72.useState)({});
  const { run } = useThrottleFn_default(() => {
    setFlag({});
  }, options);
  (0, import_react72.useEffect)(() => {
    return run();
  }, deps);
  useUpdateEffect_default(effect, [flag]);
}
var useThrottleEffect_default = useThrottleEffect;

// node_modules/ahooks/es/useTimeout/index.js
var import_react73 = __jrReq("react");
var useTimeout = (fn, delay) => {
  const timerCallback = useMemoizedFn_default(fn);
  const timerRef = (0, import_react73.useRef)(null);
  const clear = (0, import_react73.useCallback)(() => {
    if (timerRef.current) {
      clearTimeout(timerRef.current);
    }
  }, []);
  (0, import_react73.useEffect)(() => {
    if (!isNumber(delay) || delay < 0) {
      return;
    }
    timerRef.current = setTimeout(timerCallback, delay);
    return clear;
  }, [delay]);
  return clear;
};
var useTimeout_default = useTimeout;

// node_modules/ahooks/es/useTitle/index.js
var import_react74 = __jrReq("react");
var DEFAULT_OPTIONS = {
  restoreOnUnmount: false
};
function useTitle(title, options = DEFAULT_OPTIONS) {
  const titleRef = (0, import_react74.useRef)(isBrowser_default ? document.title : "");
  (0, import_react74.useEffect)(() => {
    document.title = title;
  }, [title]);
  useUnmount_default(() => {
    if (options.restoreOnUnmount) {
      document.title = titleRef.current;
    }
  });
}
var useTitle_default = useTitle;

// node_modules/ahooks/es/useTrackedEffect/index.js
var import_react75 = __jrReq("react");
var diffTwoDeps = (deps1, deps2) => {
  return deps1 ? deps1.map((_, idx) => !Object.is(deps1[idx], deps2 === null || deps2 === void 0 ? void 0 : deps2[idx]) ? idx : -1).filter((ele) => ele >= 0) : deps2 ? deps2.map((_, idx) => idx) : [];
};
var useTrackedEffect = (effect, deps) => {
  const previousDepsRef = (0, import_react75.useRef)(void 0);
  (0, import_react75.useEffect)(() => {
    const changes = diffTwoDeps(previousDepsRef.current, deps);
    const previousDeps = previousDepsRef.current;
    previousDepsRef.current = deps;
    return effect(changes, previousDeps, deps);
  }, deps);
};
var useTrackedEffect_default = useTrackedEffect;

// node_modules/ahooks/es/useUpdateLayoutEffect/index.js
var import_react76 = __jrReq("react");
var useUpdateLayoutEffect_default = createUpdateEffect(import_react76.useLayoutEffect);

// node_modules/ahooks/es/useVirtualList/index.js
var import_react77 = __jrReq("react");
var useVirtualList = (list, options) => {
  const { containerTarget, wrapperTarget, itemHeight, overscan = 5 } = options;
  const itemHeightRef = useLatest_default(itemHeight);
  const size = useSize_default(containerTarget);
  const scrollTriggerByScrollToFunc = (0, import_react77.useRef)(false);
  const [targetList, setTargetList] = (0, import_react77.useState)([]);
  const [wrapperStyle, setWrapperStyle] = (0, import_react77.useState)({});
  const getVisibleCount = (containerHeight, fromIndex) => {
    if (isNumber(itemHeightRef.current)) {
      return Math.ceil(containerHeight / itemHeightRef.current);
    }
    let sum = 0;
    let endIndex = 0;
    for (let i = fromIndex; i < list.length; i++) {
      const height = itemHeightRef.current(i, list[i]);
      sum += height;
      endIndex = i;
      if (sum >= containerHeight) {
        break;
      }
    }
    return endIndex - fromIndex;
  };
  const getOffset = (scrollTop) => {
    if (isNumber(itemHeightRef.current)) {
      return Math.floor(scrollTop / itemHeightRef.current);
    }
    let sum = 0;
    let offset = 0;
    for (let i = 0; i < list.length; i++) {
      const height = itemHeightRef.current(i, list[i]);
      sum += height;
      if (sum >= scrollTop) {
        offset = i;
        break;
      }
    }
    return offset + 1;
  };
  const getDistanceTop = (index2) => {
    if (isNumber(itemHeightRef.current)) {
      const height2 = index2 * itemHeightRef.current;
      return height2;
    }
    const height = list.slice(0, index2).reduce((sum, _, i) => sum + itemHeightRef.current(i, list[i]), 0);
    return height;
  };
  const totalHeight = (0, import_react77.useMemo)(() => {
    if (isNumber(itemHeightRef.current)) {
      return list.length * itemHeightRef.current;
    }
    return list.reduce((sum, _, index2) => sum + itemHeightRef.current(index2, list[index2]), 0);
  }, [list]);
  const calculateRange = () => {
    const container = getTargetElement(containerTarget);
    if (container) {
      const { scrollTop, clientHeight } = container;
      const offset = getOffset(scrollTop);
      const visibleCount = getVisibleCount(clientHeight, offset);
      const start = Math.max(0, offset - overscan);
      const end = Math.min(list.length, offset + visibleCount + overscan);
      const offsetTop = getDistanceTop(start);
      setWrapperStyle({
        height: totalHeight - offsetTop + "px",
        marginTop: offsetTop + "px"
      });
      setTargetList(list.slice(start, end).map((ele, index2) => ({
        data: ele,
        index: index2 + start
      })));
    }
  };
  useUpdateEffect_default(() => {
    const wrapper = getTargetElement(wrapperTarget);
    if (wrapper) {
      Object.keys(wrapperStyle).forEach((key) => wrapper.style[key] = wrapperStyle[key]);
    }
  }, [wrapperStyle]);
  (0, import_react77.useEffect)(() => {
    if (!(size === null || size === void 0 ? void 0 : size.width) || !(size === null || size === void 0 ? void 0 : size.height)) {
      return;
    }
    calculateRange();
  }, [size === null || size === void 0 ? void 0 : size.width, size === null || size === void 0 ? void 0 : size.height, list]);
  useEventListener_default("scroll", (e) => {
    if (scrollTriggerByScrollToFunc.current) {
      scrollTriggerByScrollToFunc.current = false;
      return;
    }
    e.preventDefault();
    calculateRange();
  }, {
    target: containerTarget
  });
  const scrollTo = (index2) => {
    const container = getTargetElement(containerTarget);
    if (container) {
      scrollTriggerByScrollToFunc.current = true;
      container.scrollTop = getDistanceTop(index2);
      calculateRange();
    }
  };
  return [targetList, useMemoizedFn_default(scrollTo)];
};
var useVirtualList_default = useVirtualList;

// node_modules/ahooks/es/useWebSocket/index.js
var import_react78 = __jrReq("react");
var ReadyState;
(function(ReadyState2) {
  ReadyState2[ReadyState2["Connecting"] = 0] = "Connecting";
  ReadyState2[ReadyState2["Open"] = 1] = "Open";
  ReadyState2[ReadyState2["Closing"] = 2] = "Closing";
  ReadyState2[ReadyState2["Closed"] = 3] = "Closed";
})(ReadyState || (ReadyState = {}));
function useWebSocket(socketUrl, options = {}) {
  const { reconnectLimit = 3, reconnectInterval = 3 * 1e3, manual = false, onOpen, onClose, onMessage, onError, protocols } = options;
  const [latestMessage, setLatestMessage] = (0, import_react78.useState)();
  const [readyState, setReadyState] = (0, import_react78.useState)(ReadyState.Closed);
  const onOpenRef = useLatest_default(onOpen);
  const onCloseRef = useLatest_default(onClose);
  const onMessageRef = useLatest_default(onMessage);
  const onErrorRef = useLatest_default(onError);
  const readyStateRef = useLatest_default(readyState);
  const reconnectTimesRef = (0, import_react78.useRef)(0);
  const reconnectTimerRef = (0, import_react78.useRef)(void 0);
  const websocketRef = (0, import_react78.useRef)(void 0);
  const reconnect = () => {
    var _a2;
    if (reconnectTimesRef.current < reconnectLimit && ((_a2 = websocketRef.current) === null || _a2 === void 0 ? void 0 : _a2.readyState) !== ReadyState.Open) {
      if (reconnectTimerRef.current) {
        clearTimeout(reconnectTimerRef.current);
      }
      reconnectTimerRef.current = setTimeout(() => {
        connectWs();
        reconnectTimesRef.current++;
      }, reconnectInterval);
    }
  };
  const connectWs = () => {
    if (reconnectTimerRef.current) {
      clearTimeout(reconnectTimerRef.current);
    }
    if (websocketRef.current) {
      websocketRef.current.close();
    }
    const ws = new WebSocket(socketUrl, protocols);
    setReadyState(ReadyState.Connecting);
    ws.onerror = (event) => {
      var _a2;
      if (websocketRef.current !== ws) {
        return;
      }
      reconnect();
      (_a2 = onErrorRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onErrorRef, event, ws);
      setReadyState(ws.readyState || ReadyState.Closed);
    };
    ws.onopen = (event) => {
      var _a2;
      if (websocketRef.current !== ws) {
        return;
      }
      (_a2 = onOpenRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onOpenRef, event, ws);
      reconnectTimesRef.current = 0;
      setReadyState(ws.readyState || ReadyState.Open);
    };
    ws.onmessage = (message) => {
      var _a2;
      if (websocketRef.current !== ws) {
        return;
      }
      (_a2 = onMessageRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onMessageRef, message, ws);
      setLatestMessage(message);
    };
    ws.onclose = (event) => {
      var _a2;
      (_a2 = onCloseRef.current) === null || _a2 === void 0 ? void 0 : _a2.call(onCloseRef, event, ws);
      if (websocketRef.current === ws) {
        reconnect();
      }
      if (!websocketRef.current || websocketRef.current === ws) {
        setReadyState(ws.readyState || ReadyState.Closed);
      }
    };
    websocketRef.current = ws;
  };
  const sendMessage = (message) => {
    var _a2;
    if (readyStateRef.current === ReadyState.Open) {
      (_a2 = websocketRef.current) === null || _a2 === void 0 ? void 0 : _a2.send(message);
    } else {
      throw new Error("WebSocket disconnected");
    }
  };
  const connect = () => {
    reconnectTimesRef.current = 0;
    connectWs();
  };
  const disconnect = () => {
    var _a2;
    if (reconnectTimerRef.current) {
      clearTimeout(reconnectTimerRef.current);
    }
    reconnectTimesRef.current = reconnectLimit;
    (_a2 = websocketRef.current) === null || _a2 === void 0 ? void 0 : _a2.close();
    websocketRef.current = void 0;
  };
  (0, import_react78.useEffect)(() => {
    if (!manual && socketUrl) {
      connect();
    }
  }, [socketUrl, manual]);
  useUnmount_default(() => {
    disconnect();
  });
  return {
    latestMessage,
    sendMessage: useMemoizedFn_default(sendMessage),
    connect: useMemoizedFn_default(connect),
    disconnect: useMemoizedFn_default(disconnect),
    readyState,
    webSocketIns: websocketRef.current
  };
}
var useWebSocket_default = useWebSocket;

// node_modules/ahooks/es/useWhyDidYouUpdate/index.js
var import_react79 = __jrReq("react");
function useWhyDidYouUpdate(componentName, props) {
  const prevProps = (0, import_react79.useRef)({});
  (0, import_react79.useEffect)(() => {
    if (prevProps.current) {
      const allKeys = Object.keys(Object.assign(Object.assign({}, prevProps.current), props));
      const changedProps = {};
      allKeys.forEach((key) => {
        if (!Object.is(prevProps.current[key], props[key])) {
          changedProps[key] = {
            from: prevProps.current[key],
            to: props[key]
          };
        }
      });
      if (Object.keys(changedProps).length) {
        console.log("[why-did-you-update]", componentName, changedProps);
      }
    }
    prevProps.current = props;
  });
}
var useWhyDidYouUpdate_default = useWhyDidYouUpdate;

// node_modules/ahooks/es/useMutationObserver/index.js
var useMutationObserver = (callback, target, options = {}) => {
  const callbackRef = useLatest_default(callback);
  useDeepCompareWithTarget_default(() => {
    const element = getTargetElement(target);
    if (!element) {
      return;
    }
    const observer2 = new MutationObserver(callbackRef.current);
    observer2.observe(element, options);
    return () => {
      observer2 === null || observer2 === void 0 ? void 0 : observer2.disconnect();
    };
  }, [options], target);
};
var useMutationObserver_default = useMutationObserver;

// node_modules/ahooks/es/useTheme/index.js
var import_react80 = __jrReq("react");
var ThemeMode;
(function(ThemeMode2) {
  ThemeMode2["LIGHT"] = "light";
  ThemeMode2["DARK"] = "dark";
  ThemeMode2["SYSTEM"] = "system";
})(ThemeMode || (ThemeMode = {}));
var useCurrentTheme = () => {
  const matchMedia = isBrowser_default ? window.matchMedia("(prefers-color-scheme: dark)") : void 0;
  const [theme, setTheme] = (0, import_react80.useState)(() => {
    if (isBrowser_default) {
      return (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia.matches) ? ThemeMode.DARK : ThemeMode.LIGHT;
    } else {
      return ThemeMode.LIGHT;
    }
  });
  (0, import_react80.useEffect)(() => {
    const onThemeChange = (event) => {
      if (event.matches) {
        setTheme(ThemeMode.DARK);
      } else {
        setTheme(ThemeMode.LIGHT);
      }
    };
    if (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia.addEventListener) {
      matchMedia.addEventListener("change", onThemeChange);
    } else if (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia.addListener) {
      matchMedia.addListener(onThemeChange);
    }
    return () => {
      if (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia.removeEventListener) {
        matchMedia.removeEventListener("change", onThemeChange);
      } else if (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia.removeListener) {
        matchMedia.removeListener(onThemeChange);
      }
    };
  }, []);
  return theme;
};
function useTheme(options = {}) {
  const { localStorageKey } = options;
  const [themeMode, setThemeMode] = (0, import_react80.useState)(() => {
    const preferredThemeMode = (localStorageKey === null || localStorageKey === void 0 ? void 0 : localStorageKey.length) && localStorage.getItem(localStorageKey);
    return preferredThemeMode || ThemeMode.SYSTEM;
  });
  const setThemeModeWithLocalStorage = (mode) => {
    setThemeMode(mode);
    if (localStorageKey === null || localStorageKey === void 0 ? void 0 : localStorageKey.length) {
      localStorage.setItem(localStorageKey, mode);
    }
  };
  const currentTheme = useCurrentTheme();
  const theme = themeMode === ThemeMode.SYSTEM ? currentTheme : themeMode;
  return {
    theme,
    themeMode,
    setThemeMode: useMemoizedFn_default(setThemeModeWithLocalStorage)
  };
}

// scripts/_remaining-vendor-tmp/entry-9sqtS.mjs
var entry_9sqtS_default = void 0 !== void 0 ? void 0 : es_exports;
/*! Bundled license information:

screenfull/dist/screenfull.js:
  (*!
  * screenfull
  * v5.2.0 - 2021-11-03
  * (c) Sindre Sorhus; MIT License
  *)

js-cookie/dist/js.cookie.mjs:
  (*! js-cookie v3.0.8 | MIT *)
*/

  var out = module.exports
  if (out && typeof out === "object" && out.__esModule && "default" in out) {
    var names = Object.keys(out).filter(function (k) {
      return k !== "default" && k !== "__esModule"
    })
    if (names.length) return out
    return out.default
  }
  return out
})()

if (typeof __mod === "function") {
  helpers.export(r, "default", function () { return __mod })
  r.default = __mod
} else if (__mod && typeof __mod === "object") {
  for (var __k in __mod) {
    if (Object.prototype.hasOwnProperty.call(__mod, __k) && __k !== "__esModule") {
      ;(function (key) {
        helpers.export(r, key, function () { return __mod[key] })
        r[key] = __mod[key]
      })(__k)
    }
  }
  r.default = __mod.default !== undefined ? __mod.default : __mod
  if (__mod.default !== undefined) {
    helpers.export(r, "default", function () { return __mod.default })
  }
} else {
  r.default = __mod
  helpers.export(r, "default", function () { return __mod })
}
