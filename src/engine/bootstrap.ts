/**
 * Plasmo shell notes.
 *
 * Fill engine SoT: `../src/` (Jobright), bundled to `assets/helper-app.js`.
 * Activate: `~contents/bootstrap` injects that bundle — do not import `src/` via `~`.
 *
 * Residual Clean-TS discovers under `~contents/crawler` are test helpers only.
 */
