/**
 * TypeScript facade naming for the Parcel autofill site factory.
 * Runtime bundle uses contents/crawler/factory.js (Parcel module format).
 *
 * Activate injects assets/helper-app.js (see src/helper-entry.ts).
 * @see ./ported site modules under ../sites/
 */
export {}
