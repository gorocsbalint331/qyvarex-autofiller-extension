/**
 * OBSOLETE — Greenhouse TypeScript is bundled directly by bundle-engine-helper.mjs
 * (see scripts/lib/ts-to-parcel.mjs). Kept only for reference; do not rely on synced .js.
 *
 * Historical: Sync greenhouse.ts → greenhouse.js (Parcel module format).
 */
console.error(
  "[sync-greenhouse-from-ts] obsolete: edit .ts files and run npm run bundle:helper",
)
process.exit(1)
