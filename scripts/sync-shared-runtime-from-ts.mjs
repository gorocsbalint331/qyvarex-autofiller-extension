/**
 * OBSOLETE — constants.ts / utils/string.ts are compiled in-bundle by
 * bundle-engine-helper.mjs (scripts/lib/ts-to-parcel.mjs).
 */
console.error(
  "[sync-shared-runtime] obsolete: edit .ts files and run npm run bundle:helper",
)
process.exit(1)
