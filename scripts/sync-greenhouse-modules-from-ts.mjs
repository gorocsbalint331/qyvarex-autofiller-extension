/**
 * OBSOLETE — greenhouse/*.ts is compiled in-bundle by bundle-engine-helper.mjs.
 */
console.error(
  "[sync-greenhouse-modules] obsolete: edit .ts files and run npm run bundle:helper",
)
process.exit(1)
