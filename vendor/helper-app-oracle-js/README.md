# Archived JS oracle (helper-app-src)

Former readable JS tree restored from Parcel `helper-app`. Kept for reference while the new TypeScript package lives at [`../helper-app-src`](../helper-app-src).

Do not treat this folder as the live engine. Prefer:

- Runtime (Parcel): `../helper-app`
- Runtime (TS, opt-in): `../helper-app-src` + `npm run bundle:helper:ts`
