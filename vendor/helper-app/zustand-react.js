/**
 * Parcel module id: 7OAlZ
 * Resolved path: zustand-react.js
 * Dependencies:
 *   react -> 329PG  =>  react-reexport.js
 *   zustand-vanilla -> ei7yf  =>  zustand-vanilla.js
 *
 * npm-aligned (zustand create / useStore) using React.useSyncExternalStore.
 * Generated by scripts/replace-small-vendors-with-npm.mjs — do not hand-edit.
 */

var React = e("react")
var vanilla = e("zustand-vanilla")

var identity = function (x) {
  return x
}

function useStore(api, selector) {
  if (selector === void 0) selector = identity
  var slice = React.useSyncExternalStore(
    api.subscribe,
    function () {
      return selector(api.getState())
    },
    function () {
      return selector(api.getInitialState())
    }
  )
  React.useDebugValue(slice)
  return slice
}

function createImpl(createState) {
  var api = vanilla.createStore(createState)
  var useBoundStore = function (selector) {
    return useStore(api, selector)
  }
  Object.assign(useBoundStore, api)
  return useBoundStore
}

var create = function (createState) {
  return createState ? createImpl(createState) : createImpl
}

r.create = create
r.useStore = useStore
