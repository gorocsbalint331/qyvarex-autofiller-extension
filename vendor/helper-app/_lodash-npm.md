# lodash via npm

- Package: `lodash-es` (`engine/package.json` / `node_modules`)
- Facade: `helper-app/lodash-es.js` (Parcel id `p4RBe`)
- Dump removed: `helper-app/vendor/lodash/`
- First-party keeps `e("lodash-es")` — resolves to this facade
- Regenerate: `npm run replace:lodash`
